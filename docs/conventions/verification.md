# 検証手順

変更を確定させる前に通す検査を定める。

## 言葉

この文書は次の2つを区別する。**混ぜて書かない。**

| 言葉 | 指すもの |
|---|---|
| 実行 | 実行器(`verify.ps1`・`verify-live.ps1` 等)を1回走らせ、その表の検査を一通り通すこと |
| 検査 | 下の表の1行。実行器が1件ずつ走らせる単位 |

**検査の中をどう割るかは検査ごとに違い、呼び名はその検査の実装が持つ。** この文書へ総称を作らない
——総称を被せると、どの実装のどの単位を指しているのかが読み手に決められなくなる。指すときは
実装の呼び名で指す(実機動作確認は[件](../../scripts/live-host.ps1)、受入シナリオは
[段](../../catalog/authored/acceptance-scenarios.json)、自動E2E検査は
[能力対応表の行](../../catalog/authored/tool-map.json))。

## 時間の上限

**1回の実行が6分を超えたら、その実行を不合格とする。** 上限は
[checks.ps1](../../scripts/checks.ps1) が持ち、下に挙げるどの実行器にも同じ値が効く——常設の検査と
実機に触る検査の実行器だけでなく、途中で走らせる群の実行器も同じである。使い切った時点で残りの
検査は始めず、走らせていないものとして数える。

速さは検査の質の一部である——通すのに分単位が要る実行は、変更のたびには通されなくなり、通され
ない検査は無いのと変わらない。1件ずつではなく実行の全体で見るのは、遅さを検査の数へ逃がせない
ようにするためである。

## 常設の検査

```
pwsh -File scripts/verify.ps1
```

これ1本ですべて走る。**落ちた検査も走らせていない検査も無ければ終了コード0**、あれば1で、その名前を
並べる(要る出来上がりが揃わなかった検査は走らせない)。**通すのは変更を確定させる前の1回**とする。

| 検査 | 合格条件 |
|---|---|
| ビルド | 警告・エラー0 |
| スクリプト構文 | エラー0 |
| スクリプト構文(PowerShell) | エラー0 |
| 文書のリンク | 終了コード0 |
| 除外一覧の導出 | 終了コード0 |
| 実行時リフレクション | 終了コード0 |
| 整形 | 差分0 |
| テスト | 全テスト成功 |
| 台帳とSDKの照合 | 終了コード0 |
| 日本語名の照合 | 終了コード0 |
| 型役割の照合 | 終了コード0 |
| 共通契約割当の照合 | 終了コード0 |
| 値の表現の照合 | 終了コード0 |
| 危険操作の照合 | 終了コード0 |
| 能力対応表の照合 | 終了コード0 |
| 提供対象の網羅 | 終了コード0 |
| スキーマ定義の照合 | 終了コード0 |
| ツールの説明文の照合 | 終了コード0 |
| サンプル値の照合 | 終了コード0 |
| 発見可能性の照合 | 終了コード0 |
| スキーマ対応の照合 | 終了コード0 |
| 行の検査の網羅 | 終了コード0 |
| 規則適合検査 | 終了コード0 |
| 受入シナリオの照合 | 終了コード0 |
| 受入の実行器の照合 | 終了コード0 |
| ブリッジの単独起動 | 終了コード0 |
| 配布パッケージの生成 | 終了コード0 |

## 途中で走らせる群

作業の途中は、変えたものの群だけを走らせる。どの検査がどの群かは
[check-set.ps1](../../scripts/check-set.ps1) が決める。

| 変えたもの | 実行器 |
|---|---|
| Markdown | `pwsh -File scripts/verify-docs.ps1` |
| `catalog/` | `pwsh -File scripts/verify-catalog.ps1` |
| `src/`・`tests/`・`scripts/` | `pwsh -File scripts/verify.ps1`(全件) |

## 実機に触る検査

**エディタかMCPクライアントに触る変更のときに通す。** 実機のエディタを起こして操作するので、
常設の検査とは分けて走らせる。

```
pwsh -File scripts/verify-live.ps1
```

| 検査 | 合格条件 |
|---|---|
| 実機動作確認 | 終了コード0 |
| 自動E2E検査 | 終了コード0 |
| ブリッジの実機動作確認 | 終了コード0 |
| 参照クライアントの実機動作確認 | 終了コード0 |
| 受入シナリオ | 終了コード0 |

## 不合格時の対応

- 不合格のまま変更を確定させない。修正が恒久文書の定める設計値に影響するときは、その設計値を
  決め直して恒久文書へ反映してから再実行する。
- 配布パッケージの生成が、第三者ライセンス表示の標準の本文を読めない旨で落ちた場合: 依存が増えて
  新しいライセンスが入っている。その本文をSPDXライセンスリストから
  `catalog/observed/licenses/<SPDXの綴り>.txt` へ転記してから再実行する。**組み立ては外部へ問い
  合わせない**ので、本文は追跡下に無いと使えない。
- 実機に触る検査の不合格: ホストのログを読んで原因を掴む。ログの所在は
  [操作役のスクリプト](../../scripts/host-control.ps1)の `status` が表示する。
- 受入シナリオか参照クライアントの実機動作確認で、画像が大きすぎて通らなかった場合: 送り出す
  画像の長辺の上限を下げる。**画像を応答から外す形へは改めない**——画像が得られることは
  [要求](../specs/requirements.md)が掲げる作業の成功の条件で、外せば作業そのものが成り立たない。
  最小まで下げても通らないなら、通らない理由を掴むところからやり直す。

## 検査の組み方

- **自動化できない確認は、この文書へ置かない。** 確かめられない範囲として仕様書へ記す——手順と
  して書くと、検査していないものを検査済みに見せることになる。
- **検査からだけ使う入口を、ビルド構成で分けない。** 配布物へ入れたうえで既定は閉じておき、開く
  条件を実行時に置く。構成で分けると、配布するものと検査したものが別物になる。
- **足した検査が落ちることを確かめる。** その変更だけを一度外し、検査が落ちてから戻す。外すのは
  自分が入れたものに限る——始めた時点から在った変更は触らない。落ちない検査は無い方がよい。
- **相手がどう読むかを見る検査を、自前の形状判定で代用しない。** 判定の基準を取り違えれば検査にも
  同じ穴が空く。実際に読む相手を通して確かめる。
- **入力から導ける値を、追跡下のデータとして置かない。** 要るときにその場で導く。**例外は、
  生成元と取り直し方が追跡下にあって、その場で導いた値との一致を常設の検査が見ること**と、
  **その差分からしか分からない不具合があること**の両方を満たすときだけとする。
