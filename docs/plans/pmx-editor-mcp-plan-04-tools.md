# 実装計画書04: ツール群実装

能力台帳(`docs/specs/pmx-editor-mcp-capability-ledger.md`)の「提供」全能力をMCPツールとして
実装する。全ツールに共通するドメイン契約(共通契約)と、能力からツールへの写像規則をここで
確定し、完了時に共通契約仕様書(`docs/specs/pmx-editor-mcp-common-contract.md`)と
ツール仕様書(`docs/specs/pmx-editor-mcp-tools.md`)へ移送する。

## 前提と裏取り

1. **ツール化対象と担当区分**
   - 出典: 能力台帳(提供467能力に担当区分モデル/セッション/ビュー/変形・モーションを割当済み)
   - 確かめた結果: 台帳は生成の完全性ゲート(未処理型ゼロ+ドキュメントXML突き合わせ)を
     通過しており、要調査は残っていない。
2. **基盤契約**
   - 出典: IPC仕様書(IPCプロトコル)・アーキテクチャ仕様書(UIスレッド委譲・危険操作の公開
     方針・ツール登録機構・エラー変換)
   - 確かめた結果: ホストの直列処理(同時実行1)・エラー本文「コード: メッセージ」形式は
     ブリッジ基盤までの確定値とスパイク実測に基づく。ツール登録は、ブリッジ基盤が
     `McpServerToolCreateOptions` を渡す生成関数の形に確定させている(属性は採らない)。
3. **モデルアクセスの往復**
   - 出典: 能力台帳の「実機確認で確かめたこと」
   - 確かめた結果: UIスレッドへのInvoke経由で GetCurrentState からモデル情報(モデル名・
     頂点数)を取得してMCPクライアントへ返す往復が実機で成立した。GetCurrentState が複製を
     返し Update で反映する方式は一次資料で確認済み。Update・Undoの粒度は未実測のため、
     本計画は「Undo粒度がUpdate単位になること」を確定値とせず検証項目とする(下記のUndo
     契約参照)。
4. **SDK個別APIの仕様**
   - 出典: PMXエディタ配布物に同梱のSDKドキュメントXMLとプラグイン解説書(能力台帳の
     生成に用いた一次資料と同一)
   - 確かめた結果: 本計画の設計値が依拠する次の事実を該当記載で確認した:
     型別の解放API(UIモデルのRelease・イベントコネクタとイベントリスナの各Release・
     テキストコントロールのDispose)/UIモデルイベントリスナがUIモデルとイベントコネクタの
     両方に依存して作成されること/View・UIモデルの公開イベント種別/テクスチャ用Bitmapの
     32bit ARGB要件/LockUndoの保証範囲がUpdate時のUndo登録に限られること。
5. **Cプラグイン連携コネクタの保持可能期間**
   - 出典: 検証専用プラグインを実機のPMXエディタへ配置して行った実測
   - 確かめた結果: `GetCPluginRunArgsClone`(引数はホストプラグイン自身のモジュールパス)で
     取得したRunArgs複製の `Connector` を1個だけ保持し、`PXCBridge.BuilderInitialize`/
     `PrimitiveBuilderInitialize`・選択頂点インデックス取得・`GetCurrentPmx`・`ViewCtrl`・
     `SystemCtrl`・イベントコネクタの生成と解放、の各経路がいずれもその1個で成立した。
     名前付きパイプの切断・再接続をまたいでも、`InitializePMX` によるモデル初期化を
     はさんでも、同じコネクタで成立した。`ViewCtrl` から得たビュー制御のクライアント領域
     サイズは 730×562 であった(この値は出力画像の既定値の選定で参照する)。再取得するとRunArgs複製は別インスタンスになるが
     `Connector` は同一インスタンスを指す(参照比較で一致)。`ModulePath` は引数に渡した
     文字列がそのまま入るだけである(SDKドキュメントの記述どおり)。
     → 設計値「起動時1回取得・常駐保持・終了時破棄」は成立する。
   - **未検証として残るもの**: 経過時間は93秒までしか確認していない(数時間〜終日の放置後は
     未検証)/モデルの差し替えは `InitializePMX` のみで、PMXファイルを開き直した場合は
     未検証/エディタ終了時の後始末(破棄経路)は未検証。実施先は次のとおり。
     - 長時間放置: 自動E2E検査の `--soak` 経路(8時間。下記「行単位の検査は自動実行する」)。
       **これで覆えるのは数時間の放置までで、24時間の放置は未検証のまま残る**
       (残存リスクとして受容する)。
     - 終了時の後始末: 自動E2E検査の最終段でエディタを終了し、ホストのログにCプラグイン
       コネクタの破棄と全ハンドル解放の記録があることで判定する。この2つはホストのログの
       記録内容(起動・停止・接続・切断・エラー・処理タイムアウト)に含まれていないため、
       記録内容への追加をホスト基盤への追加要件として実装ステップで立てる。
     - **長時間放置とPMXファイルの開き直しに共通する注意**: 待機後・開き直し後にAPIが成功する
       ことだけを合格条件に
       すると、実装がその都度 `GetCPluginRunArgsClone` を再取得していても合格してしまい、
       「取得1回・常駐保持」の回帰検査にならない。**ホストのログへ「Cプラグインコネクタの取得」も
       記録し、検査の全区間を通じて取得の記録が1件だけであることを判定条件に含める**
       (この記録もホスト基盤への追加要件に含める)。
     - PMXファイルの開き直し: CAP-015(`IPEFormConnector.OpenPMXFile`)を用いる。一次資料
       (SDKドキュメントXML)で同シグネチャは PMXファイルパスを引数に取り真偽値を返すことを
       確認済みで、ファイルダイアログは開かない。よって自動E2E検査で消化する——検査専用の
       一時フォルダへ保存したPMXを開き直し、その後に**保持中の `IPXCPluginConnector` を
       直接の出所とするツール(選択頂点インデックスの取得。CAP-252)と、そのコネクタから
       接続初期化時に導出してホストが常駐保持する常駐アクセスオブジェクトを出所とするツール
       (ビュー制御のクライアント領域サイズの取得。CAP-277)**をそれぞれ呼んで正常応答に
       なることを確かめる(Cプラグイン連携でない経路のツールでは、保持中のコネクタが失効して
       いても成功するため検査にならない)。
     成立しない場合は取得タイミングの見直しを設計変更としてユーザーへ報告する。
6. **MCPクライアント側の応答サイズ上限**
   - 出典: Claude Code の MCP ドキュメント(`code.claude.com/docs/en/mcp` の
     「MCP output limits and warnings」節)
   - 確かめた結果: Claude Code はMCPツール出力が10,000トークンを超えると警告を表示し、
     既定の上限は25,000トークンである(環境変数 `MAX_MCP_OUTPUT_TOKENS` で変更可。警告の
     しきい値は固定)。注釈なしでしきい値を超えた結果は、会話へそのまま載らずディスクへ
     退避されてファイル参照に置き換えられる。ツールが `tools/list` 応答の
     `_meta["anthropic/maxResultSizeChars"]` を宣言すると、テキストコンテンツについては
     `MAX_MCP_OUTPUT_TOKENS` に依らずその値がしきい値になる(**ハード上限は500,000文字**)。
     画像コンテンツは注釈の対象外で、`MAX_MCP_OUTPUT_TOKENS` の対象のままである。
   - 突き合わせた実測: `IPXVertex` の全公開プロパティを本計画の値表現(SDXベクトル=数値配列・
     参照=index)で写像した頂点1件の、空白を除いたJSONは**431バイト**であった。1件がこの大きさ
     なので、一覧を500件返せば215,500バイトになる。しきい値はトークン数で定められており
     バイト数から一意に換算できないため、この大きさが上限に収まるかは確かめずには言えない。
     一覧系ツールの `limit` は、上限に収まることを確かめられる形で要素型ごとに定める。
   - **未検証**: `_meta["anthropic/maxResultSizeChars"]` を宣言したときの実挙動(しきい値が
     宣言値へ置き換わること)は出典の記述のみで、自分では測っていない。宣言手段の方は
     ブリッジ基盤が一次資料で確定させている(生成関数のオプションへJSONオブジェクトを渡す)ので
     未検証ではない。したがって応答サイズ予算は設定で変更可能に
     実装し、宣言が効くことを**宣言あり・なしのA/B対照**で
     **MCP経路の検査**で確認する——ホストのパイプ経路を叩く自動E2E検査ではクライアント側の
     退避を観測できないため、`claude -p` のヘッドレス実行で確かめる。**効かない場合は、
     文字数での予算設計そのものが成り立たない**——しきい値がトークン数で決まり、トークン数と
     文字数の換算は一次資料が定めていないため、安全な文字数を導けない。この場合は応答サイズ
     予算の再設計(固定モデルに対する代表応答のトークン数を実測し、その実測に基づいて予算を
     定める)を設計変更としてユーザーへ報告する。
   - **未検証**: 画像コンテンツが25,000トークンに対してどう数えられるか(寸法からの見積りか
     Base64の文字数か)は出典に記載が無く、測っていない。**出力画像の既定値はこの数え方に
     依存する**ため、既定の応答が警告になるかを実機で確かめ、必要なら既定を下げる(下記
     「値の表現」の出力画像)。

   → IPCの16MiB上限は名前付きパイプの上限であって応答サイズの設計目標ではない(実効
   上限はトークン数で定められており、バイト数の上限とは単位が異なる)。本計画の応答サイズ
   設計はこの上限から定める(下記「ページングと応答サイズ」)。
7. **ツール数と遅延読み込み**
   - 出典: Claude Code の MCP ドキュメント(`code.claude.com/docs/en/mcp` の
     「Scale with MCP tool search」節)と Tool search tool のドキュメント
     (`platform.claude.com/docs/en/agents-and-tools/tool-use/tool-search-tool`)
   - 確かめた結果: Claude Code はサーバーごとの固定のツール数上限を課さず、遅延読み込み
     (tool search)が既定で有効である。遅延対象のツールは1要求あたり最大10,000件。検索は
     ツール名・説明・引数名・引数説明を対象とし、既定で上位5件を返す。ツール説明文と
     サーバー説明は各2KB(バイト)で切り詰められる。遅延読み込みには `tool_reference` 対応
     モデル(Claude Sonnet 4.5 / Haiku 4.5 / Opus 4.5 以降)が要り、`ENABLE_TOOL_SEARCH` の
     `auto` は定義合計が文脈の10%に達した時点で遅延へ切り替わる。
   - 突き合わせた実測: ツール1,354件ぶんの、説明を1行の最小限にしたツール定義JSONは
     **372,671バイト**(1件平均275バイト)、ツール名だけのJSONは**26,327バイト**であった。
     **この1,354件は、除外一覧を確定する前の提供シグネチャ1,447件を「1能力=1ツール・プロパティ
     対はget/setの2ツール・要素型ごとに単数取得を持つ」という数え方で集約した値である**。
     **この2つのバイト数は現在の対象の大きさを表さない**——現在の提供対象1,053シグネチャ
     (下記「実装の進め方」)は、旧列挙が落としていた入れ子公開型やCAP-473・CAP-474の
     シグネチャを含んでいて旧集合との包含関係が無く、1件あたりの定義長も一様でないので、
     件数の差からJSON全体の大小は決まらないからである。
     → **現在の対象でのツール数と定義サイズは未計数である。** ステップ1で写像規則を確定した
     うえで計り直し、確定値をツール仕様書へ記録する。**再計数の結果が上限10,000件を超える
     場合は設計変更としてユーザーへ報告する。** ツール数が制約にならないという結論は、この
     再計数が上限以下になることを条件とする。もうひとつの条件は**遅延読み込みが働くこと**で
     ある: 非対応モデルや `ENABLE_TOOL_SEARCH=false` ではツール定義の全量が文脈へ載るため、
     本計画は対応モデルでの利用を前提とし、その旨をツール仕様書へ記載する。残る制約は
     **検索で意図したツールを引けるか**であり、下記「能力→ツールの写像規則」のツール名・
     説明文の設計で扱う。

## 仕様書とコードの食い違い

本計画が実装するツールのプロダクトコードはまだ無い。突き合わせ範囲: 能力台帳・アーキテクチャ
仕様書・IPC仕様書・検証手順書の全文。食い違いは無い。

## 確定する設計値

### 共通契約(共通契約仕様書へ移送)

- **状態の取得**: 読み取り系ツールはホスト側で値をキャッシュせず、呼び出しのたびに
  読み取り元から取得する。読み取り元は対象で決まる: 現在のPMX状態は `GetCurrentState`/
  ビュー・システム情報等はそのコネクタ/長寿命オブジェクトはハンドルの実体。
- **編集の分類と状態契約**: エディタの状態または外部へ作用するツールを3分類し、分類ごとに
  コミット点と失敗時の状態(未変更/変更済み/結果不明)を確定する。分類は次の順に評価し
  **最初に該当した一つ**とする(排他)。いずれの分類も処理全体を**単一のUIスレッド
  Invoke内**で実行する(途中にエディタの手動操作が割り込まない)。
  - 1: **複製編集型**(GetCurrentState→複製を変更→Update の経路を使うもの): コミット点は
    `Update` の成功。`Update` 前の例外は未変更。`Update` 自体の例外は結果不明(その旨を
    エラー本文に含む)。`Update` 成功後の表示更新の失敗は変更済みとして、警告を付けた
    成功応答を返す。
  - 2: **直接変更型**(上記以外で、SDK呼び出しがモデルデータ・長寿命オブジェクト・外部
    (ファイル等)へ作用するもの。頂点編集・ウェイト編集・初期化・UIモデル変更・保存等):
    コミット点は当該SDK呼び出しの正常返却。呼び出し前の例外は未変更、呼び出し中の例外は
    結果不明(その旨をエラー本文に含む)。
  - 3: **表示・セッション型**(上記以外の表示・設定・セッション操作。モデルデータ・
    長寿命オブジェクト・ファイルへは作用しない): 呼び出し前の検証エラーは未変更。
    呼び出し中の例外は結果不明として扱う(その旨をエラー本文に含む。モデルデータ・
    ファイルが変更されないことのみ保証する)。
  - タイムアウト・キャンセル時の状態は、ホストへの要求送信の前後で決まる(この注意を
    共通契約仕様書に明記する): 要求送信前の失敗・キャンセル(待ち行列・接続確立・
    handshake中。接続確立・handshake中のブリッジ125秒超過を含む)は未変更。要求送信後の
    タイムアウト・キャンセル・切断(HOST_-32002・BRIDGE_TIMEOUT・送信後キャンセル・
    BRIDGE_CONNECTION_LOST)は結果不明であり、クライアントは読み取りツールで状態を
    確認する。接続の扱いはIPC仕様書とアーキテクチャ仕様書の確定契約どおり: ホストの120秒タイムアウト
    (HOST_-32002)では接続は維持され、遅延完了した処理の結果は破棄されて届かない。
    ブリッジの125秒超過・要求送信後のキャンセルではブリッジがその接続を破棄し、次回
    呼び出しで再接続する。
  - 警告はエンベロープの任意フィールド `warnings`(文字列配列)で返す(成功・失敗の
    どちらのエンベロープにも付けられる。ブリッジはMCPツール結果の本文に警告を併記する)。
- **表示更新の選択**: 複製編集型ツールが `Update` 後に呼ぶ表示更新は行ごとに確定する:
  能力対応表の複製編集型の行に必須列「更新指定」(`Update` へ渡す部分更新指定と、その後に
  呼ぶ表示更新APIの組)を設ける。既定は全体 `Update` の後に **モデル実体・フォーム一覧・
  描画のすべてを更新する(UpdateModel・UpdateList・UpdateView)** とし、部分更新を使う
  行は対応する部分指定と必要な更新のみを記す。一次資料で表示更新が不要とされる経路は
  その旨を記して省略する。
- **競合の制約**: エディタ上の手動編集とMCP操作の競合は検出しない(検出手段がSDKに無い)。
  この制約は共通契約仕様書に明記し、ツールは常に呼び出し時点の最新状態へ適用する。
- **インデックス参照**: 現在のPMX状態にあるオブジェクトの指定は、呼び出し時点の状態に
  おけるインデックス(0始まり)で行う。範囲外はドメインエラー `TOOL_INDEX_OUT_OF_RANGE`。
- **長寿命オブジェクトのハンドル**: PMX状態の外で生存するSDKオブジェクト(UIモデル・
  イベントコネクタ・イベントリスナ・テキストコントロール・ビルダ生成物等)は、ホストが
  接続単位のハンドルID(正整数)で保持し、ツールはハンドルIDで参照する。
  - 所有者はホスト。接続の切断・エディタ終了時にホストが全ハンドルを自動解放する。
  - ハンドルは生成に関与した依存元ハンドルの集合(複数可。例: UIモデルイベントリスナは
    生成元のUIモデルと、引数に取るイベントコネクタの両方)を記録し、解放は子→依存元の
    逆依存順で行う。いずれかの依存元の明示解放時は、依存する子を先に自動解放して失効
    させる。
  - 型別の解放方法: UIモデル=Release/イベントコネクタ=PXCBridgeのReleaseEventConnector/
    Viewイベントリスナ=IPXEventConnectorのReleaseViewEventListener/UIモデルイベント
    リスナ=IPXUIModelのReleaseEventListener/テキストコントロール=Dispose/ビルダ生成
    データ=参照破棄のみ。
  - 解放済み・不明・**期待する型と不一致**のハンドルは `TOOL_INVALID_HANDLE`。二重解放は
    2回目を `TOOL_INVALID_HANDLE` とする。
  - **タイムアウト破棄時の後始末**: ホストの120秒タイムアウト(HOST_-32002)で遅延完了
    した結果を破棄する場合、当該要求の処理が生成したハンドル(依存物を含む)はホストが
    破棄時に解放・失効させる(クライアントへ届かないハンドルIDを残さない。この後始末は
    ステップ1のテスト対象に含める)。
  - **解放失敗時の規則**: 逆依存順解放の途中で解放APIが例外を出した場合、当該ハンドルは
    台帳上**失効として記録**し(SDK側の解放は結果不明)、後続の解放は続行する。
    `session_release_handle` は失敗したハンドルを `warnings` に列挙した成功応答を返し、
    再試行はしない(失効済みのため以後は `TOOL_INVALID_HANDLE`)。切断・エディタ終了時の
    自動解放も同様に続行・記録する。
- **イベントの配送**: Viewイベント・UIモデルイベントはpull方式とする。ホストは購読中の
  イベントを接続単位のキュー(上限1000件。超過時は古い順に破棄)に蓄積する。push通知は
  提供しない。取得ツール `view_poll_events` の契約:
  - 引数: `limit`(既定100・最小1・最大1000。範囲外・非整数は `TOOL_INVALID_ARGUMENT`)。
  - 応答: `events`(各要素は `seq`=接続内で1から単調増加する連番・`type`=イベント種別の
    識別子・`sourceHandle`=そのイベントを購読しているリスナのハンドルID・`payload`=
    イベント固有値)・`dropped`=前回取得以降にキュー超過で破棄した件数(返却時に0へ
    リセット)・`remaining`=取得後のキュー残数。クライアントは `seq` の欠番と `dropped` で
    欠落を検知できる。
  - `type` の識別子はVMEテンプレートと同じ規則「宣言型名(IPX/IPE接頭辞を除き
    snake_case化)+ピリオド+イベント名(snake_case化)」で機械導出する(Viewリスナと
    UIモデルリスナの同名イベントも衝突しない)。全 `type` 分岐にわたる識別子の一意性を
    シグネチャ単位ゲートで検査する。
  - 排出は応答サイズ予算のホスト側判定点(下記の `value` ゲート)に収まる
    件数だけを1回の応答として分割せずに行う(超える場合は件数を減らして返す。単一イベントで超える場合は
    そのイベントを破棄して `dropped` に計上する)。
- **Undo契約**: 複製編集型のツールは1呼び出し=1回の `Update` 呼び出し。`LockUndo`/
  `UnlockUndo` は独立ツールとして公開しない(呼び出しをまたぐロックの残留を防ぐ)。
  **この選択のトレードオフを保証の境界として明記する**: 1回の複製編集型ツール呼び出しの中の
  集合操作は対象が何件でも1回のUndoで戻せるが、**複数のツール呼び出しにまたがる編集は
  複数のUndoになり、1つへまとめる手段は無い**(頂点の追加とウェイトの設定は別のツールなので
  別々のUndoになる)。`suppressUndo` は呼び出しをまたぐ統合の手段ではない(その呼び出しの中で
  Undo登録を抑えるだけである)。呼び出しをまたぐトランザクション・バッチのツールは、
  タイムアウト・切断・途中失敗・ロックの回収まで含めた別設計になるため**本計画の範囲外**とし、
  必要になった時点で別途設計する。
  代わりに**複製編集型ツール共通**の任意引数 `suppressUndo: true` に写像し、指定時は
  その呼び出し内で Lock→編集→Unlock を必ず対で実行する(編集・Updateの成否や例外の有無に
  よらず、最後に必ずUnlockする)。Unlockの失敗はロックを残留させない処置を行う: その場で
  1回再試行し、なお失敗した場合は「Undoロック残留」をホスト常駐状態(接続をまたいで保持)に
  記録して当該応答に明記する(Update成功後なら警告付き成功、それ以外は各分類の失敗時状態の
  ままエラー本文に残留を追記)。残留記録がある間は、各ツール呼び出しの冒頭・接続終了時の
  後始末・新規接続のhandshake直後、のそれぞれでUnlockの回収を試み、成功したら記録を解除して
  以後の最初の応答に警告で報告する。呼び出し冒頭の回収に失敗した場合、当該ツールが
  複製編集型・直接変更型なら `TOOL_OPERATION_FAILED`(Undoロック残留中のため中止)で実行
  せず返し、読み取り・表示・セッション型は実行して応答に警告を付す。台帳の
  CAP-004/005は、担当どおりセッション群の対応表に記載し、
  割当先を特定ツールではなく「共通引数 suppressUndo(共通契約)」とする(能力対応表には
  共通契約への割当を表す行種別を設け、シグネチャ単位ゲートはこの割当も割当済みと数える)。
  一次資料の
  LockUndo の保証はUpdate時のUndo登録に限られるため、直接変更型・表示・セッション型では
  `suppressUndo` を受け付けない(指定時は `TOOL_INVALID_ARGUMENT`)。「Undo粒度がUpdate
  単位になること」はエディタのUndo操作を要するため、手動確認一覧へ種別「契約」・(a)の類型で
  登録し、検証手順書の受入シナリオ「編集と取り消し」で検証する。確定した内容(Undo粒度が
  Update単位であること)はツール仕様書へ記す。
- **ドメインエラーの搬送**: ホストのツール系メソッドは、ドメインエラーをJSON-RPCの
  `error` ではなく `result` 内のエンベロープで返す: 成功は `{"ok":true,"value":...}`、
  失敗は `{"ok":false,"error":{"code":"TOOL_...","message":"..."}}`(どちらも任意で
  `warnings`)。
  ブリッジは `ok:false` をMCPツール結果の `isError`+「コード: メッセージ」本文へ変換する。
  成功時の変換も次のとおり確定する: MCPクライアントへ内部エンベロープは見せず、`value` を
  JSON文字列化したテキストコンテンツ1件として返す(構造化出力機能は使わない)。
  `warnings` がある場合は同じテキストコンテンツの末尾に「警告: 」行として併記する。
  画像を返すツールは画像コンテンツを先頭に置き、構造化値・警告があれば後続のテキスト
  コンテンツに載せる。`tool-schemas.json` の出力スキーマはMCP公開結果(`value` の構造)を
  記述する。
  ツール処理内で捕捉した例外(SDK呼び出し失敗・ファイル入出力失敗等)は
  `TOOL_OPERATION_FAILED` として例外の内容と状態契約上の帰結(未変更/変更済み/結果不明)を
  本文に含めて返す。JSON-RPCの `error`(アーキテクチャ仕様書のエラー契約で HOST_ 形式へ変換)に残るのは、要求の解釈・
  ディスパッチ・応答生成などホスト基盤の異常(IPC仕様書のエラーコード表)だけとする。
- **ツールエラーコード**: `TOOL_INDEX_OUT_OF_RANGE`(範囲外)/`TOOL_INVALID_ARGUMENT`
  (引数の値が不正)/`TOOL_INVALID_HANDLE`(ハンドル不正)/`TOOL_CONFIRM_REQUIRED`
  (危険操作で `confirm: true` が無い)/`TOOL_NOT_APPLICABLE`(現在の状態・提供範囲で
  適用不能。理由を本文に含む)/`TOOL_OPERATION_FAILED`(実行失敗。上記)/
  `TOOL_RESPONSE_TOO_LARGE`(応答サイズ予算に収まらない。一覧系は件数を減らしても
  収まらない場合、それ以外は単体で超える場合。下記)/`TOOL_REQUEST_TOO_LARGE`
  (要求全体が要求サイズ予算に収まらない。下記)。
- **ページングと応答サイズ**: 応答サイズの実効上限はMCPクライアント側で決まる(前提6)。
  IPCの16MiB上限は名前付きパイプの上限であって、ここでの設計目標ではない。
  - **応答サイズ予算**: 設定値(MCPクライアントへ渡るテキストコンテンツ全体の文字数)で、
    **未設定時の既定は100,000文字**。全ツールの `tools/list` 応答へ `_meta["anthropic/maxResultSizeChars"]` として
    同じ値を宣言し(ブリッジが付与する)、ゲートも同じ単位(文字数)で行う。宣言値とゲートを
    同値・同単位に揃えることで、バイト数・トークン数への換算に依らず判定できる。
    - 選定: 注釈のハード上限は500,000文字だが、これをそのまま使うと1応答で文脈の大半を
      占める。**ハード上限500,000文字の5分の1にあたる100,000文字を採る**——上限いっぱいを
      常用しないための余白を4倍取る、という比の方針である。**この値が文脈占有量について
      何かを保証するものではない**: 500,000文字はクライアント側の文字数の上限であって文脈窓の
      大きさではなく、応答が実際に消費する文脈量は内容によって変わる。トークン数との換算は
      一次資料が定めていないため根拠に使わない(宣言により、しきい値の判定は文字数で決まる
      ので換算は不要である)。予算値は設定で変更可能に実装する(前提6の未検証への備え)。
    - **計測対象と予約枠**: ブリッジが出力するテキストコンテンツは「`value` のJSON文字列化」
      に「`warnings` の『警告: 』行」を連結したものである(下記「ドメインエラーの搬送」)。
      **ホスト側の判定点(以下「`value` ゲート」)は `value` のシリアライズ結果に対する
      「予算 − 警告枠2,000文字」**であり、以下「予算内に収まる」はこの `value` ゲートを
      指す(**予算は設定値なので固定の文字数では書かない**。既定の予算100,000文字なら
      98,000文字になる)。`warnings` は
      「切り詰めた旨の注記を含めて」連結後2,000文字を超えないよう末尾を切り詰める。
      ブリッジは連結後の最終テキストを**予算そのもの**で再検査し、超える場合は
      `TOOL_RESPONSE_TOO_LARGE` を返す(ホスト側の見落としに対する最後の砦)。
  - **一覧系ツールの引数と応答**: `offset`(既定0・最小0)と `limit`(最小1)を受け取り、
    `total`(総数)・`items`・`nextOffset`(続きがある場合のみ)を返す。負数・非整数・
    範囲外は `TOOL_INVALID_ARGUMENT`。`offset` が `total` 以上の場合は空の `items` の
    正常応答とする。要素型の一覧系ツールは**集合の対象指定**と `fields` も受け取り、
    `total` は解決した対象集合の件数、`offset`・`limit` はその集合に対する位置と件数を指す。
    `view_poll_events` の `limit`(既定100・最大1000)は下記の型別逆算の
    例外とする(イベントは1件あたりの大きさが型で決まらず、予算内で分割しない排出により件数を制御
    するため)。例外である旨をスキーマ正本に記録し、規則適合検査の期待値に含める。
  - **`limit` の既定と最大は要素型ごとに逆算する**(全型共通の固定値は持たない。要素型に
    より1件あたりの大きさが1桁以上違うため): 共通契約の整備で要素型ごとに**1件あたりの想定文字数**
    を機械算出する。算出規則は、値範囲・長さ上限を持つ項目はその最大長、**長さ上限を持たない
    文字列項目(名前・コメント等)は想定長256文字**、配列・入れ子は要素の想定文字数×想定要素数
    (**要素数の上限が一次資料にあればその値、無ければ想定8要素**)とする。
    **256文字と8要素は実測でも仕様上の上限でもなく、`limit` の既定と最大を機械的に算出する
    ために置く取り決めである**。この2値の当否は応答が上限に収まることの保証には関わらない
    ——想定を超える応答は下記の件数縮小が吸収するので、誤差が効くのは安全性ではなく既定件数の
    使い勝手だけである。実運用で既定件数が過大・過小と分かった場合は、この取り決めを見直す
    (見直しは設計変更としてユーザーへ報告する)。そのうえで
    `最大` = (`value` ゲート − 一覧応答の枠1,000) ÷ 1件あたり想定文字数(切り捨て・最小1)、
    `既定` = `最大` の半分(切り捨て・最小1)とする。「一覧応答の枠」は `value` の内側の
    `total`・`nextOffset`・配列の構文分に充てる(`value` ゲートは `value` 全体に対する枠で
    あり、両者は入れ子の関係にある)。型別の値は**予算の設定値から算出する**ので、予算を
    変えた場合はスキーマ正本の再生成が要る(この依存を共通契約仕様書へ明記する)。型別の値は機械生成してスキーマ正本へ書き出し、
    手書きしない。**想定長は上限ではない**ため実際の応答が予算を超えることはありうる。
    それは下記の件数縮小が吸収する。
  - **要求サイズ予算**: 要求に載る可変長配列の要素数上限を逆算するための予算で、
    **固定値8,000,000バイト**とする。**設定可能にしない**——この値はスキーマの要素数上限を
    生成するためだけに使い、応答予算のようにMCPクライアント側の事情で変える理由がなく、
    根拠にするIPCの上限も固定だからである。設定可能にすればホストとブリッジで値が食い違って
    受理件数がずれうるので、同期の契約を持たずに済む固定値を採る。応答側と単位が違うのは、
    要求はホストへ渡る前にIPCを通るため、制約がMCPクライアントの文字数ではなく名前付きパイプが
    運べるバイト数だからである。**16MiBのIPC上限のおよそ半分を採る**——上限いっぱいを常用
    しないための余白を2倍取る、という比の方針で、応答側の予算の採り方と同じ考えによる。
    要求エンベロープ・JSONのエスケープ・UTF-8符号化の増分はこの余白が吸収する。
  - **要素数上限は全ての可変長配列へ逆算する**: 対象は**スキーマ正本が持つ配列フィールドの
    全体**から機械列挙する(名前を手書きで並べない——並べると、あとから増やした配列
    (親の指定・VMEの対象・追加のグループ等)が列挙から漏れて素通りする)。1件あたりの想定
    バイト数は**想定文字数×4**
    (UTF-8の1文字あたり最大バイト数)で算出し、`最大` = 要求サイズ予算 ÷ 1件あたり想定
    バイト数(切り捨て・最小1)とする。型別の値は機械生成してスキーマ正本へ書き出す。
  - **構造トークンの上限からも逆算し、小さい方を採る**: IPC仕様書は要求1件の構造トークン
    (オブジェクトと配列の開き括弧・要素とメンバーの区切り)に上限を課しており、ホスト基盤の
    判定は要求がツールへ渡る前に効くので、バイト数から逆算した件数がこれを超えても受理
    されない。逆算はスキーマ正本から機械的に行う:
    (a) **1件あたりの最大構造トークン数**は要素のスキーマから再帰で求める——オブジェクトは
    「開き1 + メンバーの区切り(メンバー数 − 1、メンバーが無ければ0)+ 各メンバーの値の
    再帰値」、配列は同じ形で「開き1 + 最大件数 − 1 + 各要素の再帰値」、判別和は分岐の
    最大値、任意メンバーは在る場合を採る。空のオブジェクト・配列も開きの1トークンを持つ。
    (b) **要求の包みの予約**は、エンベロープと固定メンバーが使うトークン数を上限から差し引く。
    可変長配列のメンバー自身もここで数える——(a)の再帰値を0と置いたうえで、メンバーの区切りは
    可変長配列も含めたメンバーの総数で数える。配列の手前の区切りはM×(t + 1)に入らないため、
    ここで予約しないと配列の数だけ上限を超えうる。
    (c) **同時に指定できる可変長配列が複数ある要求**では、残りを配列の数で等分して各配列の
    予算とする(配列ごとに上限いっぱいを許すと合計で超えるため)。
    `最大` = 配列の予算 ÷(1件あたりの最大構造トークン数 + 1)(切り捨て・最小1)。
    **分母に1を足すのは要素の区切りぶん**——M件の配列が使うトークンは「開き1 + 区切り
    M − 1 + 要素の中身 M × t」で、これは `M ×(t + 1)` に等しい。分母を t のままにすると、
    1件あたりのトークンが小さい要素で最大2倍の過大になり、契約どおりの最大件数の要求が
    IPCの上限で拒まれる。
  - **要求全体のサイズも照合する**: 配列ごとの上限は各配列が単独で予算を使い切る前提の
    安全側の値なので、複数の可変長配列を持つ要求では合計が予算を超えうる。ホストは受信した
    要求のUTF-8バイト数を要求サイズ予算と照合し、超える場合は
    **`TOOL_REQUEST_TOO_LARGE`**(要求のサイズと予算を本文に含む)を返す。
    **判定の位置は「ツールメソッドの処理の最初」**——ホスト基盤が定める要求の判定順のうち、
    引数の検査まで通った後、実行の冒頭に置く。これはドメインエラーであり、ホスト基盤の
    JSON-RPCエラーではないので、搬送も他のドメインエラーと同じく `result` のエンベロープで
    行う(ツールへディスパッチされた後でなければエンベロープを返せない)。したがって
    **優先順位はホスト基盤の判定順がそのまま上位**であり、IPCの上限超過・JSON構文不正・
    要求構造の不正・handshake関連・未知メソッド・不正な引数は、要求が要求サイズ予算を
    超えていても**それぞれのJSON-RPCエラーが先に返る**。ブリッジがIPC上限超過を送信前に
    弾く経路も同じくこの判定より先である。ツール以外のメソッドはこの判定を持たない。
    **ツール内部の共通前処理との順序も確定する**: この判定は**Undoロック残留の回収より前**に
    置く。回収はSDK呼び出し(`UnlockUndo`)を伴うのに対し、サイズ判定は要求を受理するかどうか
    だけを見るので、先に済ませれば「SDKへ触れる前」「状態は常に未変更」がそのまま保てる。
    したがってサイズ超過の呼び出しでは回収を試みず `TOOL_REQUEST_TOO_LARGE` を返し、応答に
    回収の警告は載らない。残留記録はそのまま残り、**次にこの判定を通過したツール呼び出しの
    冒頭か、先に到来した接続終了時の後始末・新規接続のhandshake直後**のいずれかで回収される
    (サイズ超過が続く間は回収の機会がツール呼び出し側には来ない、という意味である)。
    Undo契約の「各ツール呼び出しの冒頭」はこの判定を通過した呼び出しを指す。
  - **`fields` による項目の絞り込みを `limit` の上限へ反映する**: 1件あたり想定文字数は
    項目ごとの想定文字数の和なので、`fields` を指定した要求では和の対象がその項目だけに
    なる。そこで**スキーマ上の `最大` は「読み取り可能な項目のうち最小の1項目だけを返す
    場合」の逆算値**(その要素型で `fields` が取りうる最も小さい想定文字数から算出した値)
    とし、**`既定` は全項目時の逆算値の半分**のままとする(既定の呼び出しの使い勝手は
    変えない)。要求ごとの実効件数は下記の予算超過時の挙動——`value` ゲートに収まる件数だけ
    返し `nextOffset` と `warnings` で縮小を明示する——がそのまま吸収するので、`fields` の
    組合せごとの上限をスキーマで持つ必要はない。この扱いによって、項目を絞った読み取りでは
    1呼び出しで返せる件数がスキーマ上も増える。
  - **予算超過時の挙動**: ホストは `value` ゲートに収まる件数だけを返し、`nextOffset` と
    `warnings`(要求件数と実際に返した件数を明記)で縮小を明示する。既定の呼び出しが
    エラーにならないことを優先する。先頭1件だけで `value` ゲートを超える場合に限り
    `TOOL_RESPONSE_TOO_LARGE` を返し、その旨を本文に含める。一覧系以外のツールで
    `value` が単体で `value` ゲートを超えた場合も `TOOL_RESPONSE_TOO_LARGE` とする。
- **値の表現**: 次の対応表で確定する。
  - SDXベクトル/行列: 数値配列(V2=[x,y]・V3=[x,y,z]・V4/Q=[x,y,z,w]・M=行優先16要素)。
  - PMD名前空間の数値型(IPEVector2・IPEVector3・IPEVector4・IPEQuaternion・IPEMatrix):
    SDXの同種と同じ数値配列。ビューのカメラ・頂点編集・変形の各値とVMDキーの位置・回転が
    この型で、CAP-463が非対応とするのは型そのものをツール化しないことであって、値の受け渡しは
    ここで写像する。入力側はホストが実体を組み立て、組み立てに使う経路自体はツールにしない
    (ブラシと同じ形)。経路は型ごとに次のとおり確定する。IPEVector2・IPEVector3・IPEVector4 は
    IPEBuilder の CreateVector2・CreateVector3・CreateVector4。IPEQuaternion と IPEMatrix は
    生成メンバーが無いので、それぞれを実装する公開クラス PEPlugin.SDX.Q・PEPlugin.SDX.M を
    ホストが直接構築する。
  - 列挙型: 列挙子名の文字列。色: [r,g,b] または [r,g,b,a] の0〜1数値。
  - バイト列: Base64文字列。
  - 戻り値のないシグネチャ(void): 成功エンベロープの `value` は null とし、出力スキーマは
    null型として生成する。**例外は要素型の公開メソッドの集合呼び出しで**、`return`・`out`・
    `ref` をいずれも持たないメソッドの `value` は `{invoked: <呼び出した要素数>}` とする
    (1回の呼び出しが対象集合の全要素に及ぶので、何件へ及んだかが唯一の結果情報になる)。
    出力スキーマ・ディスパッチの呼び出し記録の正規化値・E2E検査のハッシュは、いずれもこの
    同じ値を用いる。
  - 寸法・座標系のDrawing値: Size=[幅,高さ]・Point=[x,y]・Rectangle=[x,y,幅,高さ]。
  - フォント: `{family, size, style}`(styleは列挙子名)。ブラシ: Brush型の引数は色配列で
    指定し、ホストが単色ブラシを構築する(入力表現が色のみのため非単色ブラシは自然に
    対象外となる。シグネチャ自体は提供され、除外は不要)。
  - SDKオブジェクト参照(親ボーン・剛体の関連ボーン・SoftBodyの材質等): 要素の
    インデックス(整数)で表現し、「関連なし」はJSONの null で表現する。
    **範囲を検証する対象のPMXは経路で決まる**: `pmxHandle` を指定した経路ではそのハンドルが
    指すPMX、それ以外——現在のPMX状態を対象とする経路と、`pmxHandle` を持たない
    `handles`・`parentHandles` の経路——では**現在のPMX状態**とする。未追加の要素・親は
    やがて現在のPMXへ追加されるので、参照先は現在のPMXのリスト以外にありえない。この規則は
    更新オブジェクトの参照型項目・SDKメソッドの参照型引数・参照コレクションの `refIndices` の
    すべてに同じく適用する(参照先のリストは型役割表または値写像から機械導出する)。更新スキーマでは
    **項目の省略=変更しない**、**null指定=関連の解除**と区別する(nullを許容しない
    プロパティへのnull指定は `TOOL_INVALID_ARGUMENT`)。null許容の判定は一次資料の記載を
    正としてスキーマ正本の項目にnullable属性で記録し(参照型プロパティは既定で
    null許容)、参照型項目のnullable属性の存在をスキーマ対応検査に含める。null解除の
    実挙動は行単位の自動E2E検査の「null解除」で検証する。
  - バージョン(System.Version): 文字列(Versionの標準表記)。
  - null許容の値型(System.Nullable<T>): 値があればTの表現、無ければ null。
  - System.Object(共有オブジェクト等): 任意のJSON値(null・真偽・数値・文字列・配列・
    オブジェクトの再帰構成)として写像し、ホストはJSON値と相互変換可能な形で保持する。
    取得した値がJSONで表現できない.NETオブジェクトの場合は `TOOL_NOT_APPLICABLE`
    (表現不能の旨を本文に含む)を返す。
  - 出力画像(ビューポート取得等): PNGのBase64とし、MCPの画像コンテンツとして返す。画像
    コンテンツは `anthropic/maxResultSizeChars` の対象外で `MAX_MCP_OUTPUT_TOKENS`
    (既定25,000トークン)の対象のままである(前提6)。応答サイズ予算はテキストコンテンツの
    値なので画像へは適用せず、**寸法で上限を定める**: 任意引数 `maxLongSide`(既定**512**・
    最小256・最大2048。範囲外・非整数は `TOOL_INVALID_ARGUMENT`)を受け取り、長辺がこれを
    超える場合は縦横比を保って縮小する。縮小した場合は元寸法と縮小後寸法を `warnings` に
    載せる。既定512を採る根拠は、**画像の数え方が未検証である以上、既定値は上限超過の
    可能性が低い側に置く**という判断である(前提6。寸法に比例する数え方なら余裕があるが、Base64の
    文字数で数えるなら大きい画像は上限を超えうる)。512は内容の判別に足りる大きさであり
    (前提5の実測で確認したビューポートの実寸730×562に対して約0.7倍)、より大きい画像が
    要る利用は `maxLongSide` で最大2048まで明示的に引き上げられる。**検証の範囲は既定512で
    クライアント側の警告が出ないことに限る**——`maxLongSide` は縮小のみで拡大はせず、
    ビューポートの実寸を超える指定をしても実寸の画像しか返らないので、長辺2048の応答は
    実機のビューポートをそれ以上に広げない限り作れない。したがって**最大2048の妥当性は
    未検証として残す**(引き上げは利用者の明示指定であり、警告が出ても既定の利用は損なわれ
    ない)。既定512で警告が出る場合は既定を下げる。
  - 入力画像(テクスチャ設定等): PNGまたはBMPのBase64で受け取り、ホストで32bit ARGBの
    Bitmapへ変換する(最大4096x4096。変換不能・形式不正は `TOOL_INVALID_ARGUMENT`)。
  - **網羅ゲート**: 提供対象全シグネチャの引数・戻り値型を機械的に列挙し、型→JSON表現の
    対応表を共通契約仕様書に網羅掲載する(未対応型ゼロを実装ステップ1の完了条件とする。
    上記で表現できない型が現れた場合は、計画外の設計変更としてユーザーへ報告する)。
- **危険操作**: `confirm: true` 必須の対象は次の3基準で判定する(アーキテクチャ仕様書の
  危険操作の公開方針の具体化。
  能力対応表の必須列「危険操作フラグ」に記録し、台帳の備考への「危険操作」追記も本計画の
  実装ステップで行う):
  (a) エディタ終了(CAP-006)。
  (b) ファイルへ書き込むシグネチャ(上書きの有無によらず常に確認必須。既知の該当:
  CAP-012/016のPMD・PMX保存、CAP-013の材質名保存、CAP-166のビュー設定保存、CAP-339の
  IPXPmxのToFile、CAP-445のIPEVmdのToFile)。
  (c) 現在のPMX状態を一括初期化するシグネチャ(CAP-007/014の初期化、CAP-339の
  IPXPmxのClearを現在モデル対象——`pmxHandle` 省略時——で呼ぶ場合。`pmxHandle` 指定時の
  Clearはメモリ上の生成物が対象のため確認不要)。
  対応表作成時に各群の全シグネチャへこの基準を適用し、該当行にフラグを付ける(既知の
  該当に漏れが見つかった場合も基準側を正とする)。基準(a)(b)(c)は機械述語として
  スクリプトに実装し、述語から導出した期待フラグ集合と対応表のフラグ・台帳の危険操作
  備考の三者一致をシグネチャ単位ゲートで検査する(フラグの欠落・過剰を独立に検出する)。
  述語は意味的な過剰一致を避けるため次のとおり確定する:
  (a)=フォームコネクタのClose。(b)=メンバー名がSaveで始まるかToFileであるもの
  (Save始まりでファイルへ書き込まないメンバーが存在しないことをステップ1のシグネチャ
  列挙で確認し、存在した場合は述語を宣言型×メンバー名の明示集合へ置き換える)。
  (c)=フォームコネクタのInitializePMD・InitializePMXと、IPXPmxのClearのcurrent経路の行
  (宣言型×メンバー名で特定。IPXMaterialMorphOffset等の別型のClearは該当しない)。

### VME(モーションイベント)の提供範囲(ツール仕様書へ移送)

- VMEの「宣言的な記述」は**事前定義テンプレート方式**とする: 提供するイベントは、VME
  Operator系能力が持つ**数値引数のオーバーロード**(値・速度・加速度等の定数指定)に対応する
  テンプレートをJSONで指定する形(`{type, targets, frames, params}`)に限定する。
- デリゲート・ラムダを要するオーバーロード(StateProc系)と任意式の評価は提供しない。
  該当オーバーロードは除外一覧ファイルに登録し、能力台帳の該当能力の備考に「デリゲートを
  要するシグネチャは非対応」と明記して正本間の不一致を解消する(いずれも実装ステップ1で
  行う。シグネチャ単位ゲートの割当対象からも除外される)。
- テンプレートは裁量を残さず機械導出する: `type` は「宣言型名(IPE接頭辞を除き
  snake_case化)+ピリオド+メンバー名(snake_case化)」とし、同名メソッドを持つ別
  Operator型と衝突しない一意な識別子にする。`params` は該当シグネチャの引数を値表現の
  対応表で写像したもの、**`targets` は操作対象を一意に特定する組の配列**(1件以上・重複なし。
  同じテンプレートを解決順に全対象へ配る。単数だと複数ボーンへの適用が対象数に比例した
  呼び出しになるためで、`type` とアクセサ経路の機械導出は単数のときと変えない。**全対象の
  解決と全引数の検証を適用の前に済ませ、1件でも不正なら1件も適用しない**。応答は
  **`{applied: <適用した対象の件数>}`** とする(数値そのものではなくオブジェクトにそろえるのは、
  出力スキーマ・ディスパッチ記録の正規化値・応答ハッシュ・行単位の検査の期待値が同じ形を
  参照できるようにするためである)。件数の上限は要求サイズ予算から逆算する。行単位の検査は
  対象1件と2件の両分岐を実行し、後方の対象が不正なとき先頭の対象にも適用が残らないことを
  判定する)。組は
  `{kind, name, path}`
  (`kind`=アクセサ種別(ボーン・モーフ・カメラ・照明等)、`name`=対象名(カメラ等の
  単一対象では省略)、`path`=同一対象内に同種Operatorへの経路が複数ある場合のアクセサ名
  snake_case(カメラの位置・注視点・上方向等。一意なら省略))、`frames` は
  該当シグネチャのフレーム引数に従う(単一フレーム版は要素1つ、範囲版は[開始,終了]の
  要素2つ、フレーム引数を持たないシグネチャは `frames` を省略とし、`frames` の有無・
  要素数でオーバーロードを機械判別する)。**フレーム引数の識別はリフレクションで得られる
  引数名を正とし、名前集合は閉じたものとして確定する**(frame・start・end・frameSt・
  frameEd。ステップ1のシグネチャ列挙で実際の引数名を照合して確定し、この集合に無い
  フレーム相当の名前が現れた場合はユーザーへ報告する。同じ型の引数が複数あるオーバー
  ロードも引数名で判別する)。`frames` へ写像した引数は `params` から除外し、残りの引数を `params` とする。
  引数名から判別できない組み合わせが現れた場合は計画外の設計変更としてユーザーへ報告する。
  同一 `type` の同名オーバーロードは `frames` の有無・要素数と `params` の構成で機械判別
  できる場合に同一テンプレートへ集約する。テンプレート一覧は、VMDアクセサ階層(対象種別→アクセサ経路→
  Operator)とOperatorシグネチャの機械列挙から生成してツール仕様書に記載する(アクセサ
  経路の網羅と「`type`+`targets` の要素の kind/path+引数判別」の組の一意性をシグネチャ単位
  ゲートで検査する。ホストはこの組からSDK呼び出し先を一意に解決する)。
- 評価順・フレームの意味論はSDKのVME実行系に従う(独自の評価器は作らない)。

### 能力→ツールの写像規則(ツール仕様書へ移送)

- ツール名は `<群プレフィクス>_<snake_case動詞句>`。群プレフィクスは `model` / `session` /
  `view` / `motion`(台帳の担当 モデル/セッション/ビュー/変形・モーション に対応)。
  動詞句はシグネチャのメンバー名をPascalCaseからsnake_caseへ**機械変換**したものとする。
  出所修飾は常に**後置**とする: 型の公開メソッド(規則(b))は衝突の有無によらず型役割表の
  要素名詞(単数形)をメンバー名の後ろに付け(例: `model_clear_pmx`・`model_normalize_pmx`)、
  コネクタメソッドは同一群内で名前が衝突する場合のみ出所の型名(インターフェース名から
  IPX/IPE接頭辞を除きsnake_case化)をメンバー名の後ろに付けて区別する。この変換は
  スクリプトが行い、対応表のツール名が変換結果と一致することを規則適合検査で照合する
  (取り違え割当は名前不一致で検出される)。ツール名の群内一意も機械検査する(重複ゼロ)。
- **ツール説明文と検索での発見可能性**: ツールは多数になり(確定値はステップ1で再計数する)、
  MCPクライアントは定義を
  遅延読み込みして**ツール名・説明・引数名・引数説明に対する検索**で必要なものだけを
  読み込む(前提7)。したがって説明文は文脈節約のための飾りではなく検索の入力であり、次を
  設計値とする。
  - 説明文は機械生成とし、先頭に「対象(型役割表の要素名詞)・動作・出所の型名」をこの順で
    置く。続けて台帳備考の契約注記(利用非推奨等)と一次資料の記載を載せる。
  - **プロパティを集約したツール(要素型の取得・更新ツールとコネクタのプロパティ集約ツール)は、
    埋め込まれた全プロパティの名前とその日本語名(日本語名の正本)を索引語として
    説明文に列挙する**。プロパティ単位のツールを設けない写像では、これが利用者の語彙から
    ツールへ至る唯一の索引だからである。列挙を含めると2,000バイト上限を超える型が現れた場合は、
    説明文を削って収めず、**計画外の設計変更としてユーザーへ報告する**(索引語を落とすと
    その型のプロパティが検索から消えるため、上限との折り合いは設計判断になる)。
  - 説明文は**UTF-8で2,000バイト以内**に収める(クライアントが各2KBで切り詰めるため。説明文
    には日本語の契約注記を載せるので文字数でなくバイト数で数える。一次資料は「2KB」としか
    記しておらず2,000バイトか2,048バイトかを区別していないので、**安全側の2,000バイト**を
    採る)。上限超過は生成スクリプトのゲートで不合格とする。
  - **出所修飾の検査**: 「群内で衝突する動詞句」を機械的に定める——ツール名から出所修飾を
    除いた動詞句が同一群内で2件以上に現れるものを抽出する(語の例示に頼らない閉じた述語)。
    抽出された動詞句を持つツールについて、ツール名に出所修飾が後置されていること、および
    説明文の先頭に対象と出所の語が現れることをゲートで検査する。
  - **検索で意図したツールを引けるか**はゲートでは検査できないため、`claude -p` の
    ヘッドレス実行による**群単位の検査**として自動実行する(下記「行単位の検査は
    自動実行する」の末尾)。
- **メソッド能力は1メソッド=1ツール**とする。メソッドは引数の構成と意味が個別なので、
  集約すると引数スキーマが分岐の集合になり、検索での発見可能性(前提7)も失われる。
- **プロパティ能力は1対1で写さず、宣言型ごとに集約する**。コネクタ型の公開プロパティは
  型単位で読み取り1ツール・更新1ツールの2つへ集約し(操作対象型に対する規則(a)と同じ形)、
  読み取りは任意引数 `fields` で返す項目を選び、更新は**項目の省略=変更しない**で複数項目を
  1呼び出しで変更する。プロパティ1件ごとの `get_<名>` / `set_<名>` は設けない。
  ツール名は型役割表がコネクタ型ごとに持つ2件(要素名詞に `get_` / `update_` を前置した
  もの)を正本とし、メンバー名からの機械変換の例外として規則適合検査の期待値も型役割表から
  取る(規則(d)の `create_<要素名詞>` と同じ扱い)。集約先が宣言型ごとに分かれるため、
  プロパティ名が同一群内で衝突する場合(タブ選択・位置等が複数コネクタにある場合)も
  ツール名は衝突せず、出所修飾は要らない。集約されたプロパティは能力対応表では行種別
  `schemaEmbedded` として当該2ツールへ埋め込まれる。
  **プロパティ対を `get_`/`set_` の2ツールへ写す案は採らない**: ツール数がプロパティ数の
  2倍になるうえ、複数項目の変更が項目数に比例した呼び出しになる。プロパティは引数を持たず
  値の読み書きしかしないので、メソッドと違い集約しても引数スキーマは分岐しない。
- **共通契約フローへの割当**: 現在PMXの取得・反映そのものであるシグネチャ
  (GetCurrentState・Update、およびPXCBridgeの同等のPMX取得・更新)は、独立ツールを
  設けず「共通契約への割当」行種別で対応表に記載する(CAP-004/005と同じ扱い)。これらは
  全編集ツールの複製編集フローと読み取りツール群の状態取得として内部実行され、IPXPmx
  全体をJSON値として往復させることはしない(モデル内容の読み書きはlist/update系
  ツールが担う)。同様に、PXCBridgeのビルダ初期化(BuilderInitialize・
  PrimitiveBuilderInitialize)はホストの起動時に内部実行する共通契約割当(内部フロー:
  接続初期化)とし、初期化に用いる `IPXCPluginConnector` は、ホスト起動時に
  GetCPluginRunArgsClone(引数はホストプラグイン自身のモジュールパス)で取得した
  RunArgs複製から1回だけ得て常駐期間中保持し、エディタ終了時に破棄する(再取得は
  しない)。GetCPluginRunArgsClone(CAP-078)自体も独立ツールとして公開せず共通契約割当
  (接続初期化)とし、台帳が要求する利用非推奨の注記は共通契約仕様書のこのフローの
  記述に明記する。**常駐アクセスオブジェクトの取得**(PXCBridgeのビルダ・プリミティブ
  ビルダ・ビュー制御・システム制御など、コネクタ型のオブジェクトを返すシグネチャ)も
  独立ツールを設けず共通契約割当(内部フロー: 接続初期化。戻り値はホスト保持の常駐
  アクセスオブジェクトへ束縛)とする——それらのメンバー機能はコネクタ型のメンバー写像で
  個別ツールに現れるため、取得自体は公開しない。**通常プラグイン側の入口**も同様に扱う:
  ホストはBootupで受領した IPERunArgs を常駐保持し(この保持要求はホストの常駐基盤への
  追加要件として、ステップ1でアーキテクチャ仕様書に記載する)、各コネクタ・ビルダは
  IPERunArgs のルート(ホスト・コネクタ・ビュー等の経路専用型)からのプロパティ解決で
  取得する。経路専用型はコネクタ型として型役割表に載せ、その取得プロパティ・経路は
  共通契約割当(接続初期化。戻り値は常駐アクセスオブジェクトへ束縛)とする。**自動注入引数**: SDKシグネチャに現れる
  `IPXCPluginConnector` 型の
  入力引数(PXCBridgeの各メソッド等)は、行種別によらず(通常ツール行・共通契約割当行の
  双方で)ツールスキーマへ公開せず、ホストが常駐保持するコネクタを自動注入する。
  スキーマ対応検査は、通常ツール行では自動注入対象型の引数を公開スキーマの期待項目から
  除外して行に自動注入の記録があることを検査し、共通契約割当行では当該引数を束縛先
  スロット「自動注入コネクタ」へ束縛する(下記の閉集合に含める)。
  また、SDKの解放メソッド(UIモデルのRelease・イベントコネクタ/リスナの
  Release系・テキストコントロールのDispose)は独立ツール化せず共通契約割当とし、
  **`assignment` は `tool`・対象名は `session_release_handle`**、**`slotBinding` は
  ハンドル解放フローの各スロット**とする(割当先とスロット束縛を混同しないよう、分岐は
  `tool` の一択に固定する。解放は常に合成ツール経由で
  行い、ハンドル台帳の失効・依存子の先行解放と連動させる)。共通契約割当の行は
  「スキーマ上の対応」の代わりに**2つのフィールドを別々に**必須で持つ(同じ名前で二重に
  定義しない)。**`assignment`** はその行がどこへ割り当てられるかで、値は下記の行種別の節が
  定める3値の判別和(`tool`/`commonArg`/`internalFlow`)である。**`slotBinding`** は
  **シグネチャの全引数・戻り値(方向含む)それぞれの束縛内容**である。
  内部フローごとの**束縛先スロットは閉集合**として共通契約仕様書に定義する(複製編集
  フロー={PMX複製・更新種別(更新オブジェクト指定)・更新インデックス・Undoロック
  フラグ}/状態取得={PMX複製(戻り値)}/接続初期化={RunArgs複製(戻り値。常駐保持する
  コネクタとモジュールパスはこの複製のプロパティから抽出する)・モジュールパス(入力)・
  常駐アクセスオブジェクト(コネクタ型を返すシグネチャの戻り値)}/
  ハンドル解放={対象ハンドル}。加えて全フロー共通のスロットとして「自動注入コネクタ」=
  `IPXCPluginConnector` 型の入力引数への常駐コネクタの束縛、および「所有側オブジェクト」=
  ホストが保持するコネクタ・親オブジェクトへのレシーバーの束縛)。束縛検査の対象集合は、
  シグネチャの引数・戻り値に加えて、インスタンスメソッドの**レシーバー(this)**を含む。
  レシーバーの束縛規則: レシーバー自身が解放・操作の対象である場合(引数なしの
  Release・Dispose等)は「対象ハンドル」へ、それ以外(各コネクタ・引数ありの解放APIの
  所有側等)は「所有側オブジェクト」へ束縛する。スキーマ対応
  検査はこの行種別に対して、シグネチャから機械導出した引数・戻り値集合と束縛内容の
  **全単射**(欠落・重複ゼロ)、方向の一致に加え、各引数の期待スロット(引数の型・方向・
  名前からスロット種類を機械決定する。一意に決定できないシグネチャはユーザー報告)との
  一致を検査する。共通契約割当行の行単位検査は、束縛先を利用する代表ツールの自動E2E検査
  (suppressUndo指定の編集・状態取得を伴う読み取り・Cプラグイン初期化フローの成立・
  ハンドル解放・切断時に自動解放が実行されること)をその行の検査記録とする。Cプラグイン
  初期化フローの「取得1回で全連携APIが機能すること・IPC再接続後も同一コネクタを再利用
  できること」は前提5で実測済みであり、自動E2E検査では再現の確認にとどめる。
  エディタ終了経路の自動解放と終了時の後始末は、検査最終段のエディタ終了のあとホストの
  ログに全ハンドル解放と破棄の記録があることで判定する。
- **引数方向の写像**: 行キーとスキーマ正本の各項目は引数方向(in/out/ref)を持つ。
  `out` 引数は応答の項目へ写像し入力には含めない。`ref` 引数は要求と応答の両方の項目へ
  写像する。方向どおりの入出力割当をスキーマ対応検査で検査する。
- **オーバーロードのJSON判別**: 同一ツールへ集約する同名オーバーロードは、JSON写像後の
  入力(in・ref の必須項目の名前集合・型・配列要素数。`out` は含めない)で相互に機械判別
  できなければならない。
  判別できない組(例: 文字列版とバイト列版がどちらもJSON文字列になる場合)は同一ツールへ
  集約せず、相違する引数の**表現識別名**を付けた別ツールへ機械的に分割する。表現識別名は
  値の表現の対応表の各項目が持つ機械用のsnake_case識別子(文字列=text・バイト列=
  base64等。型→表現識別名の対応は共通契約仕様書の対応表で一意に固定する)であり、
  分割サフィックスはこの識別名を用いる(共有データ設定の分割例: 末尾に text / base64)。全ツールの全分岐のJSON判別可能性をシグネチャ単位ゲートで検査する。
- データ型能力(型単位)は、型の役割ごとの規則で写像する。役割の定義と評価順は
  [型役割仕様書](../specs/pmx-editor-mcp-type-roles.md)が持つ。
  0. **コネクタ型**: 呼び出し側が実体を用意しないので、レシーバーとして公開入力を持たない。
     メンバーはコネクタのメソッド・プロパティの規則(メソッドは1メソッド=1ツール、
     プロパティは宣言型ごとの取得・更新の2ツールへ集約)で写像する。
  1. **イベント引数型**: 独立ツールを作らず、読み取り可能な全プロパティを該当イベントの
     `payload` スキーマへ埋め込む。
  2. **ハンドル操作型**: ハンドル参照を前提に規則(a)(b)を適用する。
     この型のインスタンスメンバーに対応するツールは、対象を指すレシーバーを**必須入力
     `handles`**(1件以上・重複なしのハンドル配列)として公開し、**操作対象型と同じ集合契約を
     適用する**——取得は入力順の配列、更新は共通値・要素ごと値の二択で応答は更新件数、
     メソッドは共通引数・要素ごと引数の二択で応答は入力順の配列(戻り値と出力引数を持たない
     場合は呼び出し件数)、全ハンドルと全引数の事前検証、部分適用しない規則、ディスパッチの
     呼び出し記録は1ツール呼び出しにつき1件。単数の `handle` にすると、VMDのキーやUIモデルの
     操作が対象数に比例した呼び出しになり、操作対象型だけを集合にした意味が失われる。
     解決順は要求に並べた順とする(位置を持たないため昇順の基準がない)。SDK呼び出しが途中で
     例外を出した場合の扱いは直接変更型の状態契約どおり(結果不明)である。
  3. **操作対象型**: 規則(a)(b)を適用する。
     この役割の型がビルダ生成物(ハンドル)としても現れる場合は、別ツールを設けず
     **対象切り替え引数**で同一ツールが両経路を扱う(下記)。
  4. **DTO型**: 独立ツールを作らず、プロパティを方向別(応答へは読み取り可能な全項目・
     入力へは書き込み可能な全項目)に関連ツールの入出力スキーマへ埋め込む。
- **型役割表**: [型役割仕様書](../specs/pmx-editor-mcp-type-roles.md)の役割判定は意味的
  条件を含むため、判定結果は機械可読の型役割表を正本として確定する(ステップ1で作成。表の形と
  検査は同仕様書が持つ。
  **経路一覧**: 接続の根から各コネクタ・ビルダへ至る一歩の列は、能力IDを持たないため
  能力対応表の行にはせず、ステップ1で機械可読の経路一覧として共通契約仕様書に確定し、接続初期化の束縛対象・
  過不足ゲートの母集合として照合する。経路一覧の各項目は**一意な接続経路キー**を持つ(行の操作軸を表す下記の「操作経路キー」とは別の識別子であり、同じ語を使わない)。
  **経路一覧の網羅ゲート**: リフレクションの到達グラフ(接続の根から到達できるコネクタ型・
  ビルダ型の集合)を機械導出し、**型役割表のコネクタ型・ビルダ型のうち到達グラフに現れるもの**が
  経路一覧にちょうど1件の経路を持つこと(欠落・重複ゼロ)を、経路一覧とは
  独立のこの到達グラフと照合して検査する(一覧の手書きの縮小は照合で検出される)。
  到達グラフに現れないコネクタ型——他のコネクタ型の基底インターフェース——は、メンバーの名前を
  つないだ経路を自分では持たないので、経路一覧に**0件**であることを併せて検査する。
  経路専用型・コネクタ型の型役割挙動の検査は接続経路キー単位で記録し、検査記録の網羅ゲートは
  **能力行キーと操作経路キーの組の全件**と**接続経路キーの全件**の双方の記録が揃うことを
  検査する(2つの集合は別々に数え、接続経路と操作軸の直積は取らない——接続の成立は操作軸に
  依らないためである)。
  提供対象に現れない実装専用型は含まない。出現する全型が「値写像対象」と「型役割対象」の
  どちらか一方に過不足なく分類されること(重複・未分類ゼロ)をゲートで検査し、値写像
  対象は値の表現を正、型役割対象は型役割表を正とする)。**表の項目は、下記をそれぞれ実装する
  段で型役割仕様書の形を広げて足す**——足す前に書いても読む側が拒む。規則(a)の集約に使う要素の
  名詞(単数形・複数形。例: vertex/vertices)と、その型を扱うCRUDツール名(list/update/add/remove。
  `get_<単数形>` は設けない)。コネクタ型の項目にはプロパティ集約ツール名2件(`get_`/`update_`)。
  読み取り可能な公開プロパティごとの日本語名は、型ごとの項目とは別の正本が持つ。値は
  [日本語名の正本](../specs/pmx-editor-mcp-property-names.json)、付け方の規則と検査は
  [日本語名仕様書](../specs/pmx-editor-mcp-property-names.md)が定める。

  **ハンドル発行能力**も型役割表が持つ(これも上記のとおり足す段で形を広げる): 各シグネチャへ
  `issuanceKind`(`constructor` /
  `factory` / `receiverBound` の3値)と**判定根拠**を記録し、この集合の全シグネチャへ集合生成の
  規則を適用する。生成ツールだけを対象にすると、ビルダのファクトリが単数のまま取り残される。
  各項目は**判定根拠**(一次資料の該当記載への参照)を必須で持ち、根拠の欠落をゲートで
  不合格とする。役割判定の意味的条件そのものは静的なゲートでは検査できないため、行単位の
  自動E2E検査の「型役割」(ハンドル操作型=2個生成して異なるハンドルになり解放後は
  `TOOL_INVALID_HANDLE` になること/コネクタ型=公開入力なしで呼べること)で検証する。
  規則適合検査は型役割表を独立の期待値として参照し、役割対象の全型が役割を持つこと(未割当
  ゼロ)と、対応表の担当・ツール名が型役割表と写像規則から導出される期待値に一致する
  ことを検査する。
- **対象切り替え引数**: PMXデータを対象とするツールは任意引数 `pmxHandle` を持つ。
  省略時は現在のPMX状態を対象とする(編集は複製編集型: GetCurrentState→Update)。指定時は
  ハンドルが指すPMXオブジェクトを対象とし、編集はその実体を直接変更する(直接変更型として
  扱う。Update・表示更新・Undo登録は行わず、`suppressUndo` は不可)。
  **`pmxHandle` は単数のままとし、集合にしない**——これは対象の集合ではなく「どのPMXを見るか」の
  切り替えであり、集合にすると要素の対象指定との直積になって `values`・`argsList` の対応順が
  二重になる。現在のPMX状態は常に1つで、生成物のPMXオブジェクトは変換・検査の中間物として
  1つずつ扱う想定なので、複数のPMXを1呼び出しで同時に編集する用途が無い。複数の生成物PMXを
  順に扱う場合だけPMXの数に比例した呼び出しになるが、その中身の要素操作は1回で済む。
- **要素の対象指定は集合とする**: PMXの要素型(頂点・材質・ボーン等)を対象とするツールは、
  **要素1件ではなく要素の集合**を受け取る。指定の形は次の4つで、相互排他とする(2つ以上の
  指定・どれも無しは `TOOL_INVALID_ARGUMENT`)。
  - `indices`: 整数配列(現在のPMX状態または `pmxHandle` 先のリスト位置)。
  - `range`: `{start, count}`(`start` 最小0・`count` 最小1)。一覧のページングの
    `offset`/`limit` とは別物なので、名前を分けて取り違えを防ぐ。
  - `all`: `true`(対象リストの全要素)。
  - `handles`: ハンドル配列(ビルダ生成した未追加要素)。
  **操作種別ごとに許容する形は次のとおりで、型役割表のコレクション区分から機械導出する**
  (許容外の指定は `TOOL_INVALID_ARGUMENT`。スキーマ対応検査はこの導出結果を期待値とする)。
  - 取得・更新、および要素型の公開メソッド由来のツール: 4形すべて(未追加の生成物に対しても
    値の読み書きとメソッド呼び出しができるため)。
  - 削除: `indices`・`range`・`all` の3形。`handles` は許容しない——ハンドルは未追加要素を
    指すので対象リストには存在せず、追加済みの要素はハンドルが消費済みで指せない。
  - 追加: **PMX直下のリスト**は、所有コレクションが `handles` のみ、参照コレクションが対象
    指定を持たず追加する参照先のインデックス値の配列を**別キー `refIndices`** で受け取る。
    **入れ子リスト**は親ごとのグループ `assignments` で受け取る(下記の入れ子リストの規則)。
  **`indices` は常に対象リスト内の位置を指す**(参照コレクションでも同じ)。参照コレクションの
  要素の値そのものを指す入力は `refIndices` にだけ現れ、対象指定と混じらない——同じキーが
  位置と値の両方を意味すると、参照値リストに対する要求が一意に読めなくなるためである。
  参照コレクションの削除も位置で行い、**解決した対象を一括で取り除く**(位置を1件ずつ順に
  削除して後続の位置がずれることはない。解決は削除の前に済んでいる)。
  **要素数1以上を課す配列**は、対象・親・生成物・VMEの対象を指す配列——すなわち
  「その配列が空だと操作の対象が決まらない配列」とする(`indices`・`handles`・`refIndices`・
  `parentIndices`・`parentHandles`・`assignments` とその中の子配列・VMEの `targets`)。
  空配列は `TOOL_INVALID_ARGUMENT`。**対象集合に長さが従う配列**(`values`・`argsList`)には
  課さない(対象0件では空配列が正しいため)。この区別は**スキーマ正本の配列フィールドごとに
  記録**し、`minItems` の有無をスキーマ対応検査の期待値に含める(スキーマが空配列を許したまま
  実行時だけが弾く実装を防ぐ)。
  **同じ要素を2回以上指す指定(`indices` の重複・`handles` の重複)も `TOOL_INVALID_ARGUMENT`
  とする**——除去して先へ進めると、要求で指定された配列の要素数と `values` の長さが食い違い、
  ハンドルの重複では1件目の消費によって2件目が必ず失敗する。要求の誤りとして解決前に返す。
  **解決後の集合の順序**は、要求に並びのある形(`indices`・`handles`)は要求に現れた順、
  並びを持たない形(`range`・`all`)はインデックスの昇順とする(`values` とメソッド引数配列の
  対応はこの順で決まる)。
  **検証はすべて適用の前に行い、部分適用をしない**: 対象の解決(範囲・ハンドルの有効性)と
  値・引数の全項目の検証(項目名・値範囲・参照インデックスの範囲・null許容)を対象集合の
  全件について先に済ませ、**1件でも不正なら何も変更せずに** `TOOL_INDEX_OUT_OF_RANGE` /
  `TOOL_INVALID_HANDLE` / `TOOL_INVALID_ARGUMENT` を返す。失敗時の状態は「未変更」で確定
  する。**共通指定(`value`・`args`)は対象件数によらず1回必ず全項目を検証する**——対象が
  たまたま0件(`all` が空のリストを解決した場合)でも省略しない。要求の値が不正であることは
  対象件数と無関係に決まるので、空集合のときだけ不正な値が通るのは一貫しない。要素ごと指定
  (`values`・`argsList`)は対象と同じ長さなので、対象0件では検証も0回になる。
  **この事前検証は直接変更型(`pmxHandle` 指定・`handles` 経路)にも同じく課す**
  ——複製を介さない経路では、検証を実体への適用より前に済ませない限り途中まで適用された
  状態が残るためである。
  `add_` ツールは `handles` 指定の生成物を対象リストへ順に追加し、**追加の成功はハンドルを
  消費して失効させる**(以後の参照は `TOOL_INVALID_HANDLE`。追加後はインデックスで参照し、
  同じ内容を再度追加するには再生成する)。全ハンドルの有効性は上記のとおり追加の前に検証
  するので、要求の不備による失敗では1件も追加されずハンドルは全て有効なまま残る。検証を
  通ったあとのSDK呼び出しが例外を出した場合は直接変更型の状態契約どおり結果不明とし、
  その時点までに成功した件数をエラー本文に含める(成功したぶんのハンドルは消費済み)。
  **`add_` の応答は `{added: <件数>, indices: <追加後の位置の配列>}`** とする(順序は追加した
  順)。追加はハンドルを消費して失効させるので、これを返さないとクライアントは追加した要素を
  以後どのインデックスで参照すればよいかを決められない(更新系が位置の列を返さないのは、
  そちらでは対象が要求の指定で確定していて新しい情報が無いためである)。**追加件数の上限は、
  要求側の逆算値と `indices` 応答の逆算値の小さい方**とする——要求サイズ予算(8,000,000バイト)
  から逆算した件数は、応答サイズ予算(既定100,000文字)に収まる保証にならない。上限を要求側
  だけで決めると、追加が成功したあとに応答で `TOOL_RESPONSE_TOO_LARGE` となり、**位置を
  返せないまま変更だけが残る**(以後その要素を参照できない)。
  要素間の参照(ボーン参照等)はインデックスのみで指定し、**未追加の生成物への参照は提供
  しない**。**理由**: 未追加のオブジェクトどうしの参照を許すと、インデックスとハンドルの
  判別和・参照元と参照先が最終的に同じPMXへ属することの保証・参照先ハンドルの解放や消費との
  依存関係・循環参照を含む追加順序・反映時点で全参照先が所属済みであることの保証・失敗時の
  ロールバックまでを決める必要があり、呼び出し粒度ではなく未追加オブジェクトグラフの
  トランザクション設計になる。**これは本計画の範囲外とし、必要になった時点で別途設計する**。
  範囲外にしても構築の呼び出し回数は要素数に比例しない——生成と追加と更新をそれぞれ集合で
  行えるので、**(1)全要素を一括生成 (2)参照未設定のまま一括追加 (3)応答の `indices` を使って
  参照を一括更新**、の3段階で済む(要素数によらず段数は変わらない。この回避手順が成立する
  ために `add_` の応答が `indices` を返す)。
- **一括更新の値の与え方**: update系ツールは相互排他の2通りで値を受け取る(両方指定・
  どちらも無しは `TOOL_INVALID_ARGUMENT`)。
  - `values`: 対象集合と**同じ長さ**の更新オブジェクト配列(順序は解決後の対象集合の順)。
    長さ不一致は `TOOL_INVALID_ARGUMENT`。長さは対象集合に従うので、`all` が空のリストを
    解決したときは空配列が正しい——**`values` と `argsList` には `minItems` を置かない**
    (置くと、共通指定の `value`・`args` なら通る空集合への呼び出しが要素ごと指定でだけ
    拒否され、二択の対称性が崩れる)。要素数の上限だけを持つ。
  - `value`: 対象集合の**全要素へ同じ内容**を適用する更新オブジェクト1つ。
  どちらの更新オブジェクトも項目の**省略=変更しない**とし、既存の参照項目の
  「null指定=関連の解除」の区別もそのまま適用する。
  **削除系の応答は `{removed: <取り除いた要素数>}`** とする(解決後の対象集合の件数。件数を
  返さないと、件数の増減を期待値とする効果を入力から機械導出できない)。
  **更新系の応答は `{updated: <件数>}` だけとし、対象の位置やハンドルの列は返さない**
  ——対象は要求の指定で確定しているので返す必要がなく、返すと応答が対象件数に比例して
  応答サイズ予算と両立しなくなる。**`updated` は更新を適用した対象の件数**(解決後の対象集合の
  件数)であり、値が実際に変わった件数ではない——同じ値の再設定や全項目を省略した更新
  オブジェクトでも適用した事実は変わらず、変化の有無を数えるには適用前の値をSDKから読み戻す
  必要があって、得られる情報の割に高くつく。
  **`Update` の呼び出し回数は経路で決まる**: 複製編集型の経路(`pmxHandle` 省略のインデックス
  経路)は対象が何件でも**1呼び出し=1回の `Update`**で、Undoも1回で戻る(共通契約のUndo契約
  どおり)。直接変更型の経路(`pmxHandle` 指定・`handles`)は `Update` を呼ばずUndo登録も
  行わないので**0回**である。行単位の検査の期待値も経路ごとにこの値を採る。
  `values` の要素数上限は**要求サイズ予算**(下記)から逆算する——`values` は要求に載る値なので、
  応答サイズ予算ではなくIPCが運べる要求の大きさが制約だからである。1件あたりの想定バイト数は
  **更新可能な全項目を持つ更新オブジェクト**の想定文字数から算出し(項目を省略した要求でも
  必ず収まる安全側の値。読み取り側の `limit` 最大が最小1項目を基準に取るのと対になる)、
  逆算結果をスキーマ正本へ書き出す。上限超過は `TOOL_INVALID_ARGUMENT`(要求件数と上限を
  本文に含む)とする。`value` には要素数の上限を設けない(要求の大きさが対象件数に依存
  しないため、`all` と組んでも要求は一定の大きさに収まる)。**要素ごとに異なる値を上限を
  超える件数へ与える要求だけは分割が残る**が、この場合は要求本文そのものが対象件数に比例
  するので、上限の有無によらず1回の要求には収まらない(同じ値を配る要求は `value` で分割
  なしに表せる)。**メソッドの `argsList` にも同じ規則で上限を置く**(応答を返さないメソッド
  でも要求は対象件数に比例するため)。
- **要素の対象指定を集合にする理由**: 単数の `index` を対象指定とする案は、要素集合への
  変更が**要素数に比例したツール呼び出し**になる。しかも複製編集型の1呼び出しは
  「現在PMXの複製取得→変更→`Update`」であり(前提3)、要素1件の変更ごとにモデル全体の
  複製と `Update` を1回ずつ払う。Undoも要素数ぶん積まれ、利用者が1回のUndoで戻せない。
  読み取り側には `list_` があるのに書き込み側に対応する形が無いという非対称も、SDKにも
  共通契約にも根拠がない。**したがって単数指定は採らない**(単数の変更は要素1件の集合として
  表せるので、単数用のツールを別に設ける必要もない)。なお、この経路の実行時間は
  測っていない——ここで確定しているのは呼び出し回数と `Update` 回数が要素数に比例するという
  規則上の帰結だけである。
- **経路別の行展開**: 行キーの経路成分は独立した3軸で表す。
  - **PMX対象軸** `pmx=current`(`pmxHandle` 省略)/`pmx=handle`(指定)。
  - **要素軸** `elem=index`(`indices`・`range`・`all` のインデックス経路)/
    `elem=handle`(`handles` 経路)。要素の対象指定を持つツールのみ。
  - **親軸** `parent=index`(`parentIndices`・`parentRange`・`parentAll` のインデックス経路)/
    `parent=handle`(`parentHandles` 経路)。入れ子リストのツールのみ。**親の件数は軸にしない**
    (1件でも複数件でも同じ行を検査する。件数は経路の違いではないので行を分ける理由がない)。
  許容組合せの導出述語: **pmx軸を持つ(=`pmxHandle` を許容する)のは、対象解決の起点が
  インデックス経由の行のみ**であり、起点は親軸があれば親、なければ要素、どちらも無ければ
  PMX全体とする。すなわち (a)要素軸・親軸を持たないPMX対象ツールの全行、(b)親軸を持つ
  ツールの `parent=index` の行、(c)親軸を持たず要素軸を持つツールの `elem=index` の行。
  それ以外の行(親軸なしで `elem=handle`、または `parent=handle`)は対象がハンドル実体で
  確定するため pmx軸を持たない(`pmxHandle` の指定は `TOOL_INVALID_ARGUMENT`)。
  `parent=handle` の行の `elem=index` は親ハンドル内のリスト位置を指す。機械生成は、
  ツールが持つ引数種別から許容される軸値の直積をこの述語で絞って行を展開し、スキーマ
  対応検査は各行のキーの必須性・相互排他・許容組合せをこの述語から導出した期待集合と
  照合する。各行が独自の編集分類・危険操作フラグ・
  `suppressUndo` 可否を持ち、分類・危険性の経路分岐を行単位で表現する(行単位の
  検査はこの展開により親ハンドル経路——未追加の親への追加を含む——も対象に
  なる)。
- 規則(a)(b)(c)(d):
  (a) プロパティ群 → その型を扱う**取得ツール1つと更新ツール1つ**(例:
  `model_list_vertices` / `model_update_vertices`)。どちらも上記の**要素の対象指定
  (集合)**を受け取る。**単数取得の `get_<単数形>` は設けない**——集合指定と `fields` を
  持つ取得ツールがあれば、1件の取得は要素1件の集合として同じツールで表せるので、
  `list_` と `get_` を分ける根拠が無い。取得ツールは任意引数 `fields`(返す項目名の配列。
  既定は読み取り可能な全項目)を持ち、ページング(`offset`・`limit`)は**解決した対象集合に
  対して**適用する。公開フィールドはプロパティと同じ規則で
  扱う。**アクセサ方向を機械判定**し、getterのみ・readonlyの項目は読み取り専用として
  取得応答にのみ現れ、updateスキーマから除外する。setterのみの項目は更新のみ。
  コレクション型のgetterプロパティはコレクション規則(一覧・add/remove)で扱い、
  update対象にしない。スキーマ対応検査の「全プロパティ存在」は方向別の期待
  (応答=読み取り可能な全項目・update入力=書き込み可能な全項目)で照合し、`fields` に
  指定できる項目名の集合が読み取り可能な全項目と一致することも検査する。**入れ子リストの
  親情報(親の識別子と親内位置)は合成のメタデータなので、応答スキーマの期待には加え、
  `fields` の集合の一致検査からは除く**(下記の入れ子リストの規則)。
  (b) 型の公開メソッド → 1メソッド=1ツール(例: `model_normalize_pmx`・`model_clear_pmx`。
  所属は常に型の担当群とし、台帳の担当を正とする——ファイル入出力メソッドも例外に
  しない)。**レシーバーが操作対象型の要素(頂点・材質・ボーン等)であるメソッドは、
  レシーバーを上記の集合の対象指定で受け取り、1呼び出しで対象集合の全要素に対して呼ぶ**。
  引数と戻り値の写像は update系と同じ二択・同じ順序規則にそろえる。
  - 引数: **入力に写すのは `in`・`ref` の引数**で、`args`(対象集合の**全要素へ同じ引数**を
    配る引数オブジェクト1つ)または `argsList`(対象集合と**同じ長さ**の引数オブジェクト配列。
    順序は解決後の対象集合の順)へ入れる。相互排他で、両方指定・どちらも無しは
    `TOOL_INVALID_ARGUMENT`。長さ不一致も `TOOL_INVALID_ARGUMENT`。**`in`・`ref` の引数を
    1つも持たないメソッド**(引数なし、または出力専用引数だけを持つもの)は
    `args`・`argsList` のどちらも受け取らない。
    スキーマ対応検査は、SDKの `in`・`ref` 引数を **`args` および `argsList` の要素オブジェクトの
    直下**の項目と照合する(この入れ子を期待値に含める)。
  - 応答: **`return`・`out`・`ref` を持つメソッド**は、**対象集合と同じ長さの配列**で返す
    (順序は解決後の対象集合の順)。いずれも持たないメソッドの応答は
    `{invoked: <呼び出した要素数>}` とする。
  - **`postcondition` は集合を1つの効果として表す**: **効果は対象件数によらずシグネチャ単位で
    1件のまま**とし、`effectKey` に要素番号を含めない——含めると効果集合が対象件数に依存し、
    シグネチャ単位で機械導出した集合との完全一致ゲートが成り立たなくなる。観測は
    **1回の比較**で行い、そのため観測側の `valuePath` に**配列射影
    `items[].<プロパティ名>`** を許す。**行に書く `expected` の参照元は SDK引数の名前空間で
    引数名のまま(`sdkArg:<引数名>`)**とし、`args`・`argsList` のどちらに入るかは書かない
    ——検査生成器が
    実行する分岐から解決する(`args` 指定なら `args` 直下の単一値、`argsList` 指定なら
    `argsList` の各要素から取った配列)。こうすると、同じ行の2つの分岐を1つの
    `postcondition` で表せる。突き合わせは既存の `equals` のままで、期待が単一値・観測が
    配列のときは配列の全要素と単一値を比べる(下記の `expected` の規則)。
  - **事前検証にレシーバーの実行時型の適合を含める**: 抽象基底のコレクション(モーフ
    オフセット等)では、対象集合に宣言型と適合しない具象型の要素が混じりうる。適合しない
    要素が1件でもあれば、適用の前に `TOOL_NOT_APPLICABLE`(不適合の位置と実行時型を本文に
    含む)を返し、1件も適用しない。**エラーコードの使い分けは経路で決まり、判定順もこれで
    固定する**: `handles` 経路は先にハンドル台帳の型を見て、期待する型と違えば共通契約どおり
    `TOOL_INVALID_HANDLE`(この経路では実行時型の適合判定まで進まない)。インデックス経路で
    解決した要素の実行時型が宣言型に適合しない場合が `TOOL_NOT_APPLICABLE` である。
    行単位の自動E2E検査は、異なる具象型を混ぜた対象集合で両方の経路を通す。
  - **`Update` の呼び出し回数は「編集分類 × 経路」から導出する**: 能力対応表がシグネチャごとに
    持つ編集分類のうち複製編集型の行だけが、複製編集型の経路(`pmxHandle` 省略のインデックス
    経路)で `Update` を1回呼ぶ。読み取り分類の行は経路によらず0回、直接変更型の行と
    直接変更型の経路(`pmxHandle` 指定・`handles`)も0回である。行単位の検査の期待値もこの
    組合せから機械導出する。
  - **ディスパッチの呼び出し記録は1ツール呼び出しにつき1件**とし、その正規化値は
    **応答として組み立てた値**(上記の配列、または `{invoked: <件数>}`)とする。記録は
    対象集合の全要素へのSDK呼び出しが正常に返った後の継続でのみ発行し、途中で例外が出た
    場合は記録しない(要素ごとに記録すると件数が対象数に依存し、応答ハッシュとの1対1の
    照合が成り立たなくなる)。
  - 上記のほか、事前検証・部分適用しない規則は update系と同じものを適用する。
  レシーバーがPMX全体・コネクタであるメソッドは集合の対象を持たず、単一のレシーバーで呼ぶ
  (ハンドル操作型のレシーバーは型役割2が定めるとおり集合である)。
  (c) 型・コネクタの公開イベント → `view_poll_events` の `type` 分岐へ写像する
  (イベントメンバー単位に展開し、対応表の行には担当する `type` 値と `payload` の
  スキーマを記す。リスナ作成ツールは対象イベント全種にホスト側ハンドラを登録して
  キューへ積み、リスナ解放時に購読を解除する)。
  (d) 公開コンストラクタ → ハンドル操作型の型は生成ツール(ハンドル返却)へ写像する。
  ツール名は `create_<型役割表の要素名詞(単数形)>` とする(メンバー名変換の例外規則)。
  その型と異なる宣言型のメソッドで戻り値の型がその型に一致するものが提供対象に存在する場合は、
  そのメソッド側を生成ツールとし、コンストラクタは除外一覧のコンストラクタ重複カテゴリで
  除外する。複数コンストラクタは
  同一ツールのスキーマ分岐とし、引数構成で機械判別する(判別不能はユーザー報告)。
  イベント引数型・DTO型のコンストラクタは独立ツールを作らず、埋め込み先スキーマの入力
  からホストが内部で構築する(担当は「スキーマへの埋め込み」)。
- **ハンドルを発行する能力は1回で複数を生成できる**: 上記の生成ツールに限らず、**ハンドルを
  新規に発行するシグネチャすべて**へ次を適用する。所有コレクションへの追加は生成物ハンドルの
  消費に限られるので、生成が単数のままだとN要素の追加が**生成N回+追加1回**になり、要素側を
  集合にした意味が失われるためである。
  **対象は型役割表の `issuanceKind` で確定する**——「戻り値の型」だけでは決まらないからである
  (公開コンストラクタには戻り値が無く、ビルダのファクトリが返す型は操作対象型に分類され、
  既存のハンドルを返すだけの取得メンバーは新規発行ではない)。`issuanceKind` は次の3値で、
  型役割表の項目に**判定根拠**(一次資料の該当記載)を必須で持たせ、根拠の欠落をゲートで
  不合格とする。
  - `constructor`: 公開コンストラクタ由来の生成ツール(レシーバーを持たない)。
  - `factory`: **コネクタ型のメソッドで、新規ハンドルを返すもの**。各ビルダもコネクタ型に
    入るので、`IPXPmxBuilder` の各生成メソッド・VMDとVMEの各ビルダに加え、`PXCBridge` の
    UIモデル登録・イベントコネクタ生成のような**コネクタから発行されるハンドル**もこの1値で
    覆える(ビルダ型という別の分類を型役割へ足す必要はない)。レシーバーは呼び出し側が用意
    しないので公開入力を持たない。
  - `receiverBound`: レシーバーがハンドル操作型で、**別の新規ハンドルを返す**メソッド
    (UIモデルのイベントリスナ生成、イベントコネクタのViewイベントリスナ生成等)。
  - **引数の有無は「公開引数」で判定する**: 自動注入する引数(`IPXCPluginConnector` 型等)と
    共通契約へ束縛する引数を除いた**公開入力の集合**が空かどうかで分ける。SDKシグネチャの
    引数の有無で判定してはならない——`PXCBridge` のイベントコネクタ生成はSDK上は引数を1つ
    取るが、その引数は自動注入されるので公開入力は0件である。
  - **`constructor` と `factory`**: 公開引数が0件の生成は `count`(最小1)だけを受け取って
    その数だけ生成する(`args`・`argsList` は持たない)。公開引数が1件以上の生成は
    **`args`(全件へ同じ引数)と `count` の組**、または **`argsList`(要素ごとの引数オブジェクト
    配列。長さが生成数になる)** の二分岐とし、相互排他とする(両方指定・どちらも無しは
    `TOOL_INVALID_ARGUMENT`。`args` は `count` と組で使い、`argsList` は `count` を取らない)。
  - **`receiverBound`**: 生成数を**レシーバー1件につき1個に固定**し、`count` を持たない
    (レシーバー集合と生成数の両方を独立に取ると、積を作るのか総数なのか、`argsList` の長さが
    どちらに対応するのかが決まらない。1対1に固定すれば、レシーバーの集合指定だけで生成数が
    決まる)。引数は `args`(全レシーバーへ共通)と `argsList`(レシーバーと同じ長さ)の二択と
    する。**公開引数が0件のメソッドはどちらも受け取らない**(要素メソッドと同じ扱い。
    Viewイベントリスナ生成のように公開引数を持たない発行が実在する)。応答はレシーバーと
    同じ長さ・同じ順序のハンドル配列である。
  - **応答**は生成順(`receiverBound` ではレシーバーの解決順)のハンドル配列とする。
  - **全引数を生成の前に検証する**(部分適用しない規則と同じ)。検証を通ったあとのSDK呼び出しが
    途中で例外を出した場合は、**それまでに生成したハンドルを型別の解放規則で後始末してから
    失敗を返す**(クライアントへ届かないハンドルを残さない。タイムアウト破棄時の後始末と同じ
    考え方)。
  - **件数の上限は分岐ごとに逆算する**——要求が件数に比例するかどうかが分岐で違うためである。
    - `count` のみ、および `args` と `count` の組: **応答のハンドル配列から逆算した上限**。
      生成数を増やしても要求本文は大きくならないので、要求側の逆算は掛けない。
    - `argsList`: **要求側(引数オブジェクトの配列)と応答側の逆算値の小さい方**。
    - `receiverBound`: **レシーバーのハンドル配列(要求側)・応答のハンドル配列(応答側)・
      `argsList` を取る場合はその要求側**、の逆算値のうち**最小**。
    **発行のスキーマは、全配列へ要求サイズ予算だけから上限を書く一般則の明示的な例外とし**、
    分岐ごとに次を機械生成する(一般則のままでは、応答側の上限の方が小さい分岐に要求側だけの
    大きい上限が付き、配列でない `count` には上限が生成されない)。
    - `count` の `maximum` = その分岐の生成件数上限。
    - `argsList` の `maxItems` = その分岐の生成件数上限。
    - `receiverBound` のレシーバー配列と `argsList` の `maxItems` = その分岐の生成件数上限。
    - 共通の `args` はオブジェクト1つなので生成件数の上限を持たない(件数は `count` が持つ)。
    要求全体のUTF-8バイト数の照合は、これとは別の最終的なゲートとしてそのまま働く。
  - **ディスパッチの呼び出し記録は1ツール呼び出しにつき1件**とし、正規化値は応答として
    組み立てたハンドル配列とする。記録は全件の生成が正常に返った後の継続でのみ発行し、
    途中で例外が出て後始末へ回った場合は記録しない(要素メソッドの集合呼び出しと同じ扱い。
    生成ごとに記録すると件数が生成数に依存し、応答ハッシュとの1対1の照合が成り立たない)。
  - **`add_` ツールが生成値を直接受け取る案は採らない**(生成・プロパティ設定・追加を1ツールへ
    合成することになり、合成ツールを2件に閉じた規則・シグネチャ単位の割当・効果の機械導出を
    まとめて変えることになる。生成を集合化すればN要素の追加は2回で済み、波及はスキーマの
    配列化と `handleCreated` の配列対応にとどまる)。
- ビルダ系能力(PMX/VMD/VMEオブジェクトの生成)は、生成物を長寿命オブジェクトの
  ハンドルとして返すツールへ写像する(後続ツールはハンドルIDで参照する。未解放分は
  切断時に自動解放する)。
- **合成ツール**: SDKシグネチャに対応しない共通契約由来のツールは `view_poll_events`
  (イベント取得)と `session_release_handle`(明示解放)の
  2つで閉じる。**解放も集合で受け取る**: 引数は**1件以上・重複なしのハンドルID配列**で、任意の型の
  ハンドルを混ぜてよい。生成を集合化した以上、後始末が1件ずつでは片側だけに件数比例が残る。
  全ハンドルの存在と型を解放の前に検証し(1件でも不正なら何も解放せず `TOOL_INVALID_HANDLE`)、
  指定ハンドルとその依存子の**閉包**を作って重複を除き、逆依存順に各ハンドルをちょうど1回
  解放する(親と子の両方が明示指定されていても二重解放しない)。**応答は台帳上で失効させた
  ハンドルIDの一覧を、失効させた順で返す**——SDKの解放APIが例外を出したハンドルも、下記の
  解放失敗時の規則どおり台帳上は失効するので**この一覧に含める**(失効したかどうかが以後の
  参照可否を決めるのに対し、SDK側の解放の成否は結果不明にとどまるため、一覧の基準は台帳側に
  そろえる)。SDKの解放に失敗したハンドルは、それとは別に `warnings` へ列挙する。両者は能力対応表に行種別「合成ツール」として登録し、スキーマ正本にも
  載せる(過不足ゲートの期待集合に合成ツール2件を含める。これ以外の合成ツールを追加する
  場合は計画外の設計変更としてユーザーへ報告する)。
- 追加・削除を持つリストは、PMX直下の8種(頂点・材質・ボーン・モーフ・表示枠・剛体・
  Joint・SoftBody)に限らず、提供型のコレクション型プロパティを機械列挙して要素型ごとに
  `add_` / `remove_` ツールを設ける。入れ子リスト(材質の面・ボーンのIKリンク・モーフの
  オフセット等)のツールは、**親要素の指定も集合とし**、専用キー
  `parentIndices` / `parentRange` / `parentAll` / `parentHandles`(相互排他)で受け取る。
  **`parentHandles` を持つかどうかは、直接所有者型にハンドル発行能力があるかで決まる**
  (下記の親ハンドルの規則)。この述語は**通常の親セレクター・`assignments` の親項目・
  規則適合検査・行単位の自動E2E検査のすべてが同じものを参照する**(3形か4形かを場所ごとに
  書くと食い違う)。
  操作対象の要素自体の集合指定(操作種別ごとに許容される形)とはキーを分離する
  (未追加の親ハンドルへ未追加要素のハンドルを追加する要求も一意に表現できる)。
  **親を集合にする理由**: 入れ子リストの要素は親の下にしか存在しない型がある。面は
  `IPXMaterial` の面リストにだけあり、PMX直下に面のリストは無い(一次資料で確認済み)。
  親が単数だと、モデル全体の面を読むだけで**材質数に比例した呼び出し**になり、要素側を
  集合にした意味が失われる。IKリンク・モーフオフセットも同じ入れ子構造である。
  - **解決順**は親集合の順、その中で子集合の順とする(辞書順)。`values`・`argsList` はこの
    平坦化した順に対応する。ページングも平坦化後の集合へ適用する。
  - **一覧の各項目は親の識別子と親内位置を持つ**(`parentIndex` または `parentHandle` と
    `indexInParent`)。平坦化順だけでは親ごとの子の件数が分からず、ページングが親の境界の
    途中から始まると、返った子がどの親のどの位置かを復元できない。件数を知るために親ごとの
    一覧を呼び直すと、解消したはずの親数への比例が戻ってしまう。この2項目は1件あたりの想定
    文字数に算入する。**これらはSDKの公開プロパティではない合成のメタデータ**なので、
    次を規則として置く: **親の集合指定を持つ経路の一覧応答にだけ現れる必須項目**とし
    (子の `handles` 経路は親に属さないので持たない)、**`fields` の対象にしない**
    (指定の有無によらず常に返す)。取得ツールの出力スキーマは、読み取り可能な公開プロパティに
    この合成項目を加えて導出し、`fields` に指定できる項目名の集合との一致検査からは除外する
    (この例外をスキーマ対応検査の期待値へ明記する)。
  - **子の `handles` 経路は親の指定を取らない**(指定は `TOOL_INVALID_ARGUMENT`)。
    `handles` が指すのは未追加の生成物で、まだどの親にも属していないので、親で絞る意味が
    ない。親セレクターと子セレクターの許容組合せは、この除外を含めて機械導出する。
  - **`add_` は親ごとのグループで受け取る**: 専用キー `assignments`(1件以上の配列)とし、
    親セレクターと子セレクターは持たない。追加は生成物ハンドルを消費するので、同じハンドルを
    複数の親へ配ることができず、どの親にどの生成物を入れるかを表す入力が要るためである。
    **これを持たないと、材質ごとに面を足す・複数のモーフへオフセットを足す・複数のボーンへ
    IKリンクを足す、といった操作が親数に比例した呼び出しになる**。契約は次で閉じる。
    - **親軸は要求全体で1つ**: `assignments` は「全要素が `parentIndex` を持つ」か「全要素が
      `parentHandle` を持つ」かの**判別和**とし、混在は `TOOL_INVALID_ARGUMENT`。行キーは
      親軸を1つしか持たず、インデックス経路は複製編集型・親ハンドル経路は直接変更型で
      `Update`・Undo・失敗時の状態の期待値が違うので、混在した要求はどの行にも属せない。
      `parentHandle` の分岐を持つかどうかは、通常の親セレクターと同じ述語(直接所有者型に
      ハンドル発行能力があること)で決まる。
    - **同じ親を2つ以上のグループへ書けない**: 親の重複は `TOOL_INVALID_ARGUMENT` とする。
      重複を許すと、効果の観測で親の射影を読み戻しツールへ渡したときに同じ親が2回並び、
      集合指定の重複禁止に当たって検査が成立しない。1つの親へ複数の生成物を入れる要求は、
      そのグループの子配列に並べれば表せるので、重複を許す必要がない。
    - **子配列は区分で決まる**: 所有コレクションは `handles`、参照コレクションは `refIndices`
      のみを持つ(型役割表の区分から機械導出し、スキーマの判別和として書き出す)。
    - **事前検証はグループをまたいで行う**: 全グループの親と子を平坦化し、**ハンドルが
      グループをまたいで重複していないこと**も含めて適用の前に検証する(グループごとの存在
      確認だけでは、先のグループで消費したハンドルを後のグループが使う要求が検証を通り、
      途中まで適用された状態が残る)。1件でも不正なら1件も追加しない。
    - **件数の上限は全グループの子件数の合計へ適用する**(各配列の要素数上限だけでは合計を
      抑えられない)。上限は追加系と同じく要求側と `indices` 応答側の逆算値の小さい方を採る。
    - 応答の `indices` はグループの順・その中の子の順に対応し、`added` は総件数である。
      Undoも1呼び出しで1回である。
  - **親の解決に要る情報**は型役割表が持つ(下記)。`parentAll` を解決するには、PMXルートから
    親要素のリストへ至る所有経路が要るためである。
  型役割表の項目にコレクション型プロパティの一覧(プロパティ名・要素型・親型・**親の所有経路**・
  **区分**)を持たせ、スキーマとディスパッチをそこから機械導出する。
  **親の所有経路は走査段階の列として定義する**——単なるプロパティ名の列では、ボーンのIKリンク
  (PMX直下のボーンリスト → ボーンのIK → IKのリンクリスト)のようにコレクションの走査と単数
  プロパティが混じる経路を表せないからである。各段は次の2種のいずれかとする。
  - **コレクション段**(プロパティ名・要素型): そのリストを先頭から走査する。
  - **単数段**(プロパティ名・型): そのプロパティを1つたどる。**値が無い要素はそこで打ち切り、
    親の列に含めない**(IKを持たないボーンは親にならない)。
  **親のインデックス空間は、この経路を走査して得られた親要素の列の位置**とする(走査順に0から
  数える。ボーンの位置ではない)。`parentAll` はこの列の全体、`parentRange` はこの列に対する
  範囲、`parentIndices` はこの列に対する位置である。
  **`parentHandles` が指すのはコレクションの直接所有者型のハンドル**であり、**この形は
  直接所有者型にハンドル発行能力があるコレクションでのみ許容する**(型役割表の
  `issuanceKind` の有無から機械導出し、無いコレクションでは `parentHandles` を許容形から
  外す)。IKリンクの直接所有者 `IPXIK` にはSDKに生成経路が無いので、IKリンクの親指定は
  インデックス経路の3形だけになる。起点側の型(ボーン)のハンドルを親として受け取り、そこから
  単数段をたどらせる案は採らない——親のインデックス空間が走査後の列なのにハンドルだけ起点の型を
  指すことになり、同じキーが2つの空間を意味するためである。区分は次の2種:
  **所有コレクション**(要素の実体を保持。頂点・材質・IKリンク等。`add_` は生成物
  ハンドルを消費して実体を追加する)/**参照コレクション**(既存要素へのインデックス参照を
  保持。SoftBodyのピン等。`add_` は `refIndices` で受けたインデックス値を末尾へ追加し、
  `remove_` は対象指定(位置)で解決した要素を取り除く。どちらも生成物ハンドルは取らない)。
  **参照コレクションの項目は、参照先の要素型と参照先リストの所在を必須で持つ**——要素型が
  整数でも、その整数がどのリストの位置かは区分だけからは決まらないためである(SoftBodyの
  ピンは頂点リストを参照する)。判定は一次資料の記載を正として型役割表へ記録し、根拠の欠落を
  ゲートで不合格とする。`refIndices` の範囲検証・スキーマの期待値・ディスパッチはこの項目から
  機械導出する。**どのPMXのリストに対して範囲を検証するかは、値の表現がSDKオブジェクト参照に
  ついて定める規則に従う**(参照コレクションに固有の規則は置かない——同じ判断を二重に持つと
  片方だけ直したときに食い違うため)。親ハンドルの経路が `pmxHandle` を持たないのは対象の
  解決の規則であって、参照値の検証先は対象の解決とは別に決まる。区分の判定は一次資料の記載を正として型役割表に記録し、区分
  どおりの挙動を行単位の自動E2E検査の「所有/参照コレクション」で検証する。
- **抽象要素型のコレクション**: コレクションの要素型が抽象基底(モーフオフセット等)の
  場合、型役割表の当該コレクション項目に**許容具象型の一覧**を必須で持たせる。許容
  具象型の機械述語: 型役割表の母集合内で当該基底型を継承し、他の型から継承されない
  **葉インターフェース**を列挙する(スクリプトがこの列挙と一覧の一致を照合する)。
  get系の応答要素には実行時型の判別子 `itemType`(具象型の識別名。型名のsnake_case変換)を
  含め、add/update系は `itemType` によるスキーマ分岐で具象型を選択する。`itemType` 分岐へ
  割り当てるのは具象型の**プロパティ・フィールド**に限り、具象型の**公開メソッド**は
  規則(b)どおり要素の対象指定を持つ独立ツールへ写像する(例: 材質モーフオフセットの
  初期化メソッドは独立ツール)。VME等の型階層にも同じ規則を適用する。
- **能力対応表**(ツール仕様書の一部)が対応の正本: 台帳の提供能力IDごとに、**提供対象の
  全シグネチャ**(型単位能力は公開メンバー単位——イベントメンバーを含む——、同名
  オーバーロードはシグネチャ単位)へ展開した行を持ち、各行に次を記す:
  担当(ツール名/pollの `type` 分岐/スキーマへの埋め込み(埋め込み先のツールまたは
  `type` 分岐を記す)/共通契約への割当のいずれか)・スキーマ上の対応(引数の組・分岐。
  共通契約割当の行はこの代わりに `assignment` と `slotBinding` の2フィールドを持つ)・
  **編集分類**(エディタの状態または外部(ファイル等)へ作用するシグネチャは複製編集型/
  直接変更型/表示・セッション型のいずれか一つ。どちらへも作用しないものは「読み取り」)・
  **更新指定**(複製編集型の行に必須。`Update` の部分更新指定と表示更新APIの組)・
  **危険操作フラグ**(危険操作の3基準に該当する行に必須)・
  **`postcondition`**(**直接ディスパッチ行に必須**。上記の閉じた構造。適用域は下記の
  行種別の表による)・**備考**(任意。台帳備考が
  要求する注記——利用非推奨等——の転記。ツール仕様書とツール説明文の生成に含める)。
  転記必須の判定は機械化する: ステップ1で台帳の該当備考に固定接頭辞「契約注記:」を
  付けて構造化し(利用非推奨等を既存の自由記述から洗い出して付与)、スクリプトは
  「契約注記:」を持つCAPの全行に備考転記があることを検査する(欠落は不合格)。
  1能力が複数ツールに対応する場合は複数行。台帳備考で非対応と明記したシグネチャは
  割当対象外。能力対応表と全ツールの入出力スキーマは機械可読形式の正本
  `docs/specs/pmx-editor-mcp-tool-map.json`(対応表)と
  `docs/specs/pmx-editor-mcp-tool-schemas.json`(スキーマ)で管理し(JSON構造の定義は
  ステップ1で共通契約仕様書に記載)、ツール仕様書への掲載とブリッジのツール登録定義は
  この正本から生成する。
- **行キーの材料**: 行キーは**宣言型・メンバー名・総称型引数の数・引数の型と方向の列**で決まる。
  総称型引数の数を含めるのは、引数の列が同じでその数だけが違うオーバーロードを言語が許すため
  である。**変換演算子(メンバー名が op_Implicit・op_Explicit)はこれに戻り値の型を加える**
  ——これらがすべて同じで戻り値の型だけが違うオーバーロードを言語が許すのは変換演算子だけで、
  SDKにも実在する(`PEPlugin.SDX.V3` と `PEPlugin.SDX.V4` が、それぞれ描画ライブラリの2つの型へ
  変換する組を持つことをリフレクション列挙で確かめた)。加えるかどうかはメンバー名で閉じており、
  衝突の有無では変えない。この材料でSDKの全公開シグネチャの行キーが重複ゼロになることは、
  列挙器が書き出した1,745行で確かめた。
- **シグネチャ行の機械生成**: 展開行は手書きせず、能力台帳の生成に用いたものと同じ
  プロセス内リフレクション列挙の方式で一次資料から機械生成する(列挙・照合スクリプトは
  実装ステップ1で整備する)。能力対応表の行集合が機械生成した提供対象シグネチャ集合と
  一致すること(過不足ゼロ)をスクリプトで照合し、展開漏れ・余分な行を検出する。
  - **除外シグネチャの正本**: 提供対象から除くシグネチャは、上記「行キーの材料」の組で
    特定する機械可読の除外一覧ファイル
    `docs/specs/pmx-editor-mcp-excluded-signatures.json`(ツール仕様書の付属物)へ
    一元管理し、生成側・対応表側の双方がこの一覧だけを参照する(手書き判断を二重に
    行わない)。**各項目は除外を許す根拠の種類(資格)を必須で持つ**。資格(i)の項目は
    ベースライン正本が凍結した組そのものが根拠なので、その能力IDを持ちカテゴリを持たない。
    **資格(ii)の項目は除外理由カテゴリを必須で持ち**、カテゴリは次の閉集合とする
    (それぞれ機械判定可能な述語を持つ): PMD系(引数または戻り値にPMDデータ型を含む。
    メンバー名だけがPMDを含むもの——SavePMDFile等のコネクタメソッド——は該当しない)/
    PMDモデル系(引数または戻り値にPMDモデル本体 IPEPmd を含み、PMD系の代替が無いもの。
    PMDモデルは値の表現を持たず、実体を得る提供対象の経路も無いので、代替の有無を問わず
    除外する。実測の該当6件はVMEとVMDの初期化・参照系である)/
    Cプラグイン引数(引数にCプラグイン
    実装本体のインターフェース型 IPXCPlugin を含む。接続用の IPXCPluginConnector は
    該当せず提供対象)/デリゲート系(引数または戻り値にデリゲート型を値として含む。
    ハンドラ型がデリゲートであるイベントの宣言そのものは、購読の仕組みで扱うので該当しない。
    下記のVMEの提供範囲が定める
    とおり、デリゲート・ラムダを要するオーバーロードと任意式の評価は提供しないので、代替の
    有無を問わず除外する。**対象アセンブリの外で宣言されたデリゲート型もある**ので、列挙は
    シグネチャが参照する型の分類も併せて書き出し、判定はその分類から引く——名前で数えると表に
    無い型を黙って提供対象にしてしまう。実測では対象アセンブリのデリゲート型を取る34件に加えて
    `System.Action` と `System.Func` を取る7件と、対象アセンブリのデリゲート型の配列を返す
    1件があり、計42件が該当する。いずれも宣言型の
    名前空間は `PEPlugin.Vme` である)/
    コンストラクタ重複(**その型と異なる宣言型のメソッド**で、戻り値の型がその型に一致し、
    それ自体が除外されていないものが存在する公開コンストラクタ。静的かどうかは問わない——
    実測の該当4件には静的な `PXUIModelHelper.CreateTextControl` と非静的な
    `IPXSystemControl.GetCPluginInfo` の双方がある。同じ型に宣言された `Clone` のような
    複製メンバーは、呼ぶのに実体が要るので生成の代わりにならず該当しない)。**Stream系は
    除外カテゴリに含めない**——後述のとおり資格(ii)として生成することが無く、独立した停止
    条件として扱うためである。照合スクリプトは、資格(ii)の各除外項目がそのカテゴリの
    述語を満たすことを機械検査し、述語を満たさない除外(恣意的な除外)を不合格とする。
    除外一覧に載せられるのは、(i)ベースライン正本が凍結した能力IDと行キーの組に含まれる
    シグネチャ、(ii)上記の閉集合のカテゴリに該当するシグネチャ(カテゴリごとに課す追加の
    条件——PMD系の代替の存在とコンストラクタ重複の生成メンバーの存在——は後述する。
    PMDモデル系は、PMD系の述語を満たしつつ代替が見つからなかったものだけを受け持つので、
    両者の順序で一意に決まる。
    台帳備考への追記はステップ1で行う)、の機械可読化だけとする(それ以外の新たな除外は
    追加しない。実装中に新たな除外やカテゴリが必要になった場合は、計画外の設計変更として
    ユーザーへ報告する)。PMD系の「代替の存在」とコンストラクタ重複の「ファクトリの存在」は
    照合スクリプトが機械検査し、代替・ファクトリが無い場合はその除外を不合格とする。
    この検査は資格で区別する: **資格(i)(ベースライン正本が凍結した組)に基づく項目は
    `alternative` を要求しない**(根拠フィールドにベースライン正本の能力IDを記録し、正本との
    対応照合のみ行う。CAP-390/398のPMD版のような既存の対象外注記はそのまま正とする)。
    **資格(ii)で新規に追加するPMD系の項目**は代替となるPMXシグネチャの行キーを必須
    フィールド `alternative` に持ち、照合規則は「同一宣言型・同一メンバー名・同一引数個数・
    同一総称型引数数で、各引数の向きが一致し、戻り値が一致し、型が異なる引数がすべて下記の
    PMD↔PMX対応表の組で置き換わっている(置き換えが1つ以上ある)提供対象シグネチャが存在する」
    こととする。**配列で受け渡す引数は要素の型へ対応表を当て、配列の形が両側で同じであることを
    併せて要求する**——形が同じとは、型名から要素の型を除いた角括弧の並びが文字列として一致する
    ことで、ジャグ配列と多次元配列や、階層ごとの次元数の並びが違うものは同じ形にならない。
    **対応表は `PEPlugin.Pmd.IPEPmd` ↔ `PEPlugin.Pmx.IPXPmx` の1組**で、
    表に無いPMD型を取るシグネチャは代替を持たない。**型名の機械的な置換で対応表を作れない**——
    名前空間の Pmd を Pmx に、接頭辞 IPE を IPX に読み替えても `PEPlugin.Pmx.IPXPmd` にしか
    ならず、実在する `PEPlugin.Pmx.IPXPmx` にならない。表に組を足せるのは、一次資料で対応を
    確かめたときに限る。**戻り値は置換せず一致を要求する**——実測では、戻り値の置換まで許しても
    該当は同じ2件(`IPEVmd.Init` と `IPEVme.Preview`)で変わらず、許さない側が根拠の要る除外を
    増やさないためである。実測では非凍結のPMD型シグネチャ38件のうち代替が在るのはこの2件だけで、
    いずれもこの1組で決まる。この規則で対応が取れない新規のPMD型シグネチャ——リスト表示更新の
    PMD版引数やPMDボーン種別設定等——は除外できず提供対象とし、PMD型は値表現・型役割の通常規則で
    写像する。
    **Stream系は資格(ii)として生成しない**。Streamを含むというだけで除外できるようにすると、
    非Stream版の代替を持たない直列化APIまで機械検査を素通りして能力が欠落しうるので、除外の
    条件は「同一宣言型で、読み書きの向きが一致し、扱うデータ形式が同じ非Streamシグネチャが
    提供対象に存在する」ことになる(一次資料で確認済みの該当: `IPXPmx` の `FromStream`・
    `ToStream` に対する `FromFile`・`ToFile`)。しかし**「データ形式が同じ」はリフレクションの
    型列だけでは判定できず**、形式識別子(PMX・PMD・画像等)とその判定根拠は一次資料を読んで
    初めて決まるため、生成器が機械で作れない。**実測でも資格(ii)のStream系は0件である**——
    PEPlugin 0.0.8.9 の公開シグネチャ1745件のうちStream型を引数か戻り値に持つのは `IPEPmd` の
    `FromStreamEx`・`ToStreamEx` と `IPXPmx` の `FromStream`・`ToStream` の4件だけで、いずれも
    ベースライン正本が凍結済みで資格(i)になる。**Stream型かどうかは、`System.IO.Stream` 自身と、
    それを継承する型で判定する**。継承関係も列挙が書き出す型の分類から引くので、対象アセンブリの
    外で宣言された派生型も取りこぼさない。そこで生成器は、**ベースライン正本に無いStreamシグネチャ
    を見つけたらその行キーを示して停止する**。停止は計画外の設計変更としてユーザーへ
    諮る合図であり、根拠の無いStream除外が黙って混入することも、必要な除外が黙って漏れることも
    起きない。なお `PmxLib` の低水準直列化APIは代替を
    持たないが、能力台帳の母集合に現れない。**このことは逆向きの照合で確かめる**——台帳の
    全CAPが列挙結果に現れることの検査は台帳から列挙への包含しか見ないので、**SDKの型列挙で
    得た全公開型が台帳の行が指す型か対象外一覧の型のどちらかに、全公開シグネチャが台帳の母集合か
    対象外一覧のどちらかに、それぞれ必ず現れること**を別途検査し、どちらにも現れない型・
    シグネチャをゲートで不合格とする。
    除外一覧の各項目が台帳備考の非対応記載と対応していることもスクリプトで照合する。台帳側は、
    分類が提供の能力のうち除外一覧の項目を指すものについて、その件数を固定接頭辞「非対応件数:」に
    続けて備考の先頭に書く。数えるのはその能力が指すシグネチャのうち除外一覧に載るものの数で、
    基底型から継いだものも含める——行が「全公開メンバー」を指す以上、継いだメンバーもその能力の
    一部だからである。1件も無い能力には書かない。接頭辞を備考の先頭に置くのは、契約注記が
    接頭辞から備考の末尾までを本文とするためである。数字の後は備考の終わりか句点だけとし、
    続きを書くときは句点で区切る。接頭辞は備考に一度しか書かない。照合は、分類が提供の全行に
    ついて、備考が書いた件数と、その行が指すシグネチャのうち除外一覧に載る数が一致することを
    検査し、書ける行以外に接頭辞があれば不合格とする。
    資格(i)の照合は**ベースライン正本**(追跡ファイル
    `docs/specs/pmx-editor-mcp-excluded-baseline.json`。資格(i)で除外できるCAP IDと
    非対応シグネチャの集合を列挙した、内容そのものが正本のファイル)に対して行う。
    台帳の備考は資格(i)の根拠にならない——備考は後から追記できるため、資格(i)の集合が
    後付けで広がりうるからである。ベースライン正本に無いものは資格(ii)として
    カテゴリ述語・`alternative` 等の検査を受ける。**ただしStream系は例外で**、ベースライン正本に
    無いStreamシグネチャは資格(ii)にせず、その行キーを示して停止する。
  - **台帳の母集合の確定**: 台帳の各行が指すシグネチャは、対象の列に書かれた名前を公開API列挙と
    突き合わせて機械的に決める。名前の解決は次の順で行い、どれにも当たらない名前は不合格とする。
    1. 対象の列を、前後に空白を伴う斜線で分割し、得た各名前を独立に解決する。
    2. 名前をまず型の名前として引く。照合は列挙結果の型名との完全一致と、名前空間を落とした
       名前との一致を見る。入れ子の型は列挙結果ではプラス記号で区切られるので、名前空間を落と
       した名前を作るときに区切りを点へ直して比べる(`PXEventArgs.UIModelMouse` は
       `PXCPlugin.Event.PXEventArgs+UIModelMouse` に当たる)。総称型は、台帳が書く逆引用符と
       個数の接尾辞を落とした名前と、列挙結果から山括弧の型引数を落とした名前とで比べる
       (`IPEVmePrimaryValue` は `PEPlugin.Vme.IPEVmePrimaryValue<TValue>` に当たる)。型引数の
       数は照合に使わないので、数だけが違う同名の型が在れば次の規則で不合格になる。実測では、
       公開型199件からこの規則で作った名前に重なりは無い。
       **複数の型に当たった名前は指す先が定まらないので不合格とする。**
    3. 型として引けなかった名前だけを、最後の点でメンバー名と型名に分け、型名を上と同じ規則で
       引いてから同名の公開メンバーを取る。**型として引けた名前をメンバーとして読み直さない**
       ——入れ子の型をメンバーと読み違えるのを防ぐためである。
    解決の対象は、名前の引き方で分ける。**型の名前で引いた行は、その型と全基底型が宣言する
    公開シグネチャのすべて**とする。**型とメンバー名の組で引いた行は、その型と全基底型が宣言
    するもののうち、そのメンバー名を持つ公開シグネチャだけ**とする。基底型は、基底クラスと
    実装インターフェースの双方を推移的に辿る。基底型まで見るのは、`IPXPmxViewConnector` の
    ように派生インターフェースの名前で書かれた行のメンバーが基底の `IPEPMDViewConnector` に
    宣言されているためで、列挙は宣言型で行を作るからである。
    名前空間をまとめて指す2行は名前へ機械展開できないので、行ごとに述語を置く。PMDレガシーの行が
    指すシグネチャは「宣言型の名前が `PEPlugin.Pmd.` で始まるシグネチャ」と「宣言型が
    `PEPlugin.IPEBuilder` で、戻り値の型の名前が `PEPlugin.Pmd.` で始まるシグネチャ」の和、
    SDXの行が指すシグネチャは「宣言型の名前が `PEPlugin.SDX.` で始まるシグネチャ」とする。
    この2行が指す**型**は、それぞれの名前空間に属する公開型のうち列挙型でないものとする。
    シグネチャの宣言型から逆算しないのは、公開メンバーを1件も宣言しない型が名前空間に在ると
    逆算では落ちるからである。列挙型を除くのは、凡例が列挙型を数えないと定めており、どの行も
    列挙型を指さないためである(実測では、この2つの名前空間の公開型39件のうち公開メンバーを
    宣言しないのは列挙型5件だけで、除いた結果はシグネチャの宣言型から逆算した集合と一致する)。
    母集合はこれらの解決結果の和とし、1つのシグネチャが複数の行に属することを許す——基底型が
    宣言する表示制御を、複数の派生型の行が同じく指すためで、実測では1件が最大9行に属する。
    **ただし提供対象のシグネチャが属する提供行の担当は1つに定まらなければならない**(実測では
    提供対象1,053件すべてが担当1つに解決した)。行が指す先は上の規則で解決できなければならないが、
    解決結果が0件になることは許す——`IPXMorphOffset` と `PXEventArgs` は公開メンバーを自分では
    宣言しない入れ物の型で、実測でこの2行だけが0件になる。実測では台帳の476行すべてが解決する。
  - **明示的な対象外一覧の正本**: 台帳が行を作らない対象は、機械可読の一覧
    `docs/specs/pmx-editor-mcp-ledger-out-of-scope.json` へ列挙する。**一覧は型単位と
    シグネチャ単位の2段で持つ。** シグネチャの差集合だけで照合すると、公開メンバーを1件も
    宣言しない型が台帳から漏れても差集合に何も現れず素通りする——`IPXMorphOffset` と
    `PXEventArgs` のようにそういう型が実在する——ので、型単位の照合を併せて置く。
    ファイルのルートはオブジェクトで、`types` と `signatures` の2つの配列を必須で持つ。`types`
    の各項目は列挙結果の型名 `name` と `reason`、`signatures` の各項目は上記「行キーの材料」の
    組で特定する `key` と `reason` を必須で持つ。どちらの配列も、項目を識別子の序数昇順で並べる。
    読み取りは既存の正本JSONの読み取りと同じ厳しさとし、**未知のフィールド・欠けたフィールド・
    型の不一致・空文字列・`name` と `key` の重複を拒否する**。
    合格条件は次の3つとする。
    - 公開型の集合が、台帳の行が指す型と `types` の和にちょうど一致すること(過不足ゼロ。
      2つは互いに素であること——同じ型を台帳の行と対象外一覧の両方へ載せることを許さない)。
    - 公開シグネチャの集合が、母集合と、`types` の型が宣言するシグネチャと、`signatures` の和に
      ちょうど一致すること(過不足ゼロ。3つは互いに素であること)。
    - 各項目の `reason` が、下記の順序付きの分類器が算出した最初の理由と一致すること。
      **記録した理由の述語を満たすことだけを見ると、先に評価される理由にも当たる項目を
      見逃す**ので、算出値との一致を条件にする。
    理由は次の閉集合とし、挙げた順に評価して最初に該当した一つを採る。
    - `enumType`(列挙型): 型が列挙型である。台帳の凡例が数えないと定める。型単位のみ。実測18件。
    - `delegateType`(デリゲート型): 型がデリゲートである。凡例が定めるとおり実行時オブジェクトは
      直接は受け渡せない。型単位のみ。実測6件。
    - `route`(経路): メンバーの述語は「メンバー種別がメソッド・プロパティ・フィールドの
      いずれかで、公開引数を持たず、値の型が対象アセンブリの公開型で、かつ宣言型と異なる」。
      イベントとコンストラクタは種別で除く——イベントはハンドラの登録であって値を返さず、
      列挙が書き出す値の型はハンドラのデリゲート型だからであり、コンストラクタは自分の型を
      作るからである。宣言型と異なることまで見るのは、引数のないコンストラクタや `Clone` が
      自分の型を返して経路に見えるのを防ぐためである。型の述語は「公開メンバーを1件以上宣言し、
      そのすべてがメンバーの述語を満たす」か「公開メンバーを1件も宣言せず、基底型を1つ以上
      持ち、その**すべて**が型の述語を満たす」。凡例が定めるとおり、能力は参照先の型の行で
      計上する。実測は型4件・シグネチャ2件で、該当メンバーはすべてプロパティであり、種別条件を
      外してもイベント・コンストラクタで残りの条件を満たすものは0件である。
    - `argumentOnly`(引数専用型): その型を宣言型とするシグネチャが母集合に1件も無く、かつその型
      自身が宣言するシグネチャを除くと、その型が他のシグネチャの引数の型としてだけ現れ、そのような
      引数が1件以上ある。型の一致は参照渡しの印を外した完全一致で見て、配列や総称型の型引数の中は
      見ない。凡例が数えないと定める引数専用型に当たる。型単位のみ。実測1件。
    実測では、公開型199件のうち170件が台帳の行に指され29件が型単位の対象外、公開シグネチャ
    1,745件のうち1,711件が母集合・32件が型単位対象外の型が宣言するもの・2件がシグネチャ単位の
    対象外である。
  - **台帳が行を作らない根拠と、除外一覧の資格は別の軸である**: 前者は台帳の凡例が持ち、能力として
    数える対象そのものを定める。後者は数えた能力のうち提供対象から除くシグネチャを定める。
    **非対応と分類した行が指すシグネチャは、除外一覧に載っていなくても提供対象に入らない**
    ——提供対象は、提供と分類した行が指すシグネチャから除外一覧を引いたものだからである(実測では
    非対応行だけが指すシグネチャが592件ある)。したがって台帳へ非対応の行を足すことは、ベースライン
    正本の凍結とも除外一覧の閉じた資格とも衝突しない。行を作るか対象外一覧へ入れるかは、凡例の
    定めだけで決める。
  - **照合の置き場所と起動の形**: 照合(シグネチャ列挙・型単位とシグネチャ単位の逆向きの照合・
    除外一覧のカテゴリ述語の検査・能力対応表との過不足照合・規則適合検査・スキーマ対応検査)は、
    列挙器と同じ net48 のコンソールツール `src/SignatureDump` の下位コマンドとして持つ。SDKは
    混在モードのアセンブリに依存し、列挙は net48 のプロセス内でしか成立しないためである。
    `scripts/` はREADMEの配置表が定めるとおり実機動作確認の補助に限り、常設の照合は置かない。
    逆向きの照合の下位コマンドは `ledger-coverage` とし、引数は順にPMXエディタの導入ディレクトリ・
    能力台帳・ベースライン正本・除外一覧・対象外一覧のパスとする。**このコマンドは逆向きの照合と
    除外一覧の照合の両方を行う**。除外一覧の照合は、**記載された各項目が正しいことだけでなく、
    期待集合との完全一致**を合格条件とする——片方向の検査だと、除外一覧から項目を1件消しても
    残った項目の資格検査は成功し、消えたシグネチャが提供行の母集合に在れば逆向きの照合でも
    未分類にならないので、除外されるべきものが黙って提供対象へ戻るからである。期待集合は、
    除外一覧の生成と同じ順序付きの分類を公開シグネチャ全件へ当てて算出し、次の3つを検査する。
    資格(i)の集合がベースライン正本の能力IDと行キーの組の集合と一致すること。資格(ii)の集合が
    算出した非ベースライン除外の集合と一致すること。ベースライン正本に無いStreamシグネチャが
    在れば行キーを示して停止すること。資格(ii)の付随条件(PMD系の代替の存在・コンストラクタ
    重複のファクトリの存在)は、この算出の中で判定する。ベースライン正本を入力に採るのは、
    資格(i)の根拠が台帳の備考ではなくこの正本だからであり、除外一覧を書き出す
    `excluded-signatures` は生成のコマンドなので常設の検査には使わない。ファイルは書き出さず、
    合否は終了コードで返し、既存の下位コマンドと同じ割り当て(0 成功 / 2 引数不正 /
    3 入力を読めない / 5 照合の不一致)を使う。成功したときは、照合した型・シグネチャ・除外の
    件数を1行の要約として標準出力へ書く。不一致のときは、どの集合のどの識別子が余ったか
    足りないかを標準エラー出力へ書く。実行コマンドは検証手順書へ登録する。
- **シグネチャ単位ゲート**: 次のすべてをスクリプトで機械検査し、各群の完了条件とする:
  - 機械生成集合との一致(過不足ゼロ)・全行への担当割当(未割当ゼロ)・編集分類の
    未分類・多重分類ゼロ・**操作の向きの未割当ゼロ**・**型別既定サンプル値表
    の全型が既定と第2の2件を持ち、2件が異なること**・複製編集型の行の更新指定欠落ゼロ・ツール名の群内重複ゼロ・
    **写像の粒度**(要素型を対象とする取得・更新・削除ツールが集合の対象指定を持つこと・
    単数取得 `get_<単数形>` とプロパティ単位の `set_<名>` がツール名の集合に存在しないこと・
    コネクタ型ごとのプロパティ集約ツールがちょうど2件で、そのコネクタの全公開プロパティの行が
    その2件を埋め込み先としていること・**入れ子リストのツールが親の集合指定を持つこと**
    (`add_` は代わりに `assignments` を持つこと)・**ハンドル操作型のレシーバーが集合で
    あること**・**ハンドル発行能力の全シグネチャが `issuanceKind` と公開引数の有無に応じた
    入力を持つこと**(`constructor`・`factory` は、公開引数0件なら `count` のみ、1件以上なら
    `args` と `count` の組か `argsList` の二分岐。`receiverBound` はレシーバー集合を持ち
    `count` を持たず、公開引数が1件以上のときだけ `args`・`argsList` を持つ)・
    **`session_release_handle` が
    ハンドル配列を取ること**・**VMEテンプレートが `targets` の配列を取ること**)・
    **`rowKind` の未分類・多重分類ゼロ**・**`rowKind` ごとの必須フィールド**(`composed` と
    `directDispatch` は `postcondition`、`commonContract` は `assignment`(3値の判別和)と
    `slotBinding` の両方、`schemaEmbedded` は
    埋め込み先の配列(1件以上)と方向の一致、`eventBranch` は必須の追加フィールドなし)・**`postcondition` を持つ行での
    欠落ゼロと構造適合**(`effectType` が9値のいずれかで
    `(effectType, effectKey)` が配列内で重複しないこと・`kind` が4値のいずれか・`kind` ごとの必須項目が
    そろっていること・`observerTool` が実在するツール名であること・`observerArgs` の対応表の
    左辺が5つの参照元の書式に合い参照先が実在すること(`arg:` は検査対象の行のトップレベル
    引数名、または `assignments` に限って許す射影2形——`arg:assignments[].<項目名>` と
    `arg:assignments[].<配列項目名>[]`——で、射影は当該項目がグループのスキーマに実在すること
    まで検査する。以下、トップレベル
    引数名、`sdkArg:` はSDKシグネチャの引数名、
    `result:` と `valuePath` は該当スキーマの `value` 直下、`setupOut:` は `setup` の
    いずれかの操作の出力名。集合を対象とするツールでは `sdkArg:<引数名>` が
    `args`・`argsList` のどちらの分岐でも解決できること)、右辺が観測ツールの引数名で
    あること・`valuePath` が空文字か
    観測ツールの出力スキーマの `value` 直下に実在するプロパティ名か、一覧応答の
    `items[].<プロパティ名>` の形で当該プロパティが要素スキーマに実在すること・
    **`postcondition` の配列が、シグネチャ・型役割・コレクション区分から機械導出した効果の
    集合と完全一致すること(`(effectType, effectKey)` をキーとした過不足ゼロ)**・
    各要素の `kind` と観測方法が `effectType` から定まる値と一致すること・
    `comparison` が6値のいずれかで、`equals` のとき `expected` がJSONリテラルか実在する
    参照元1つであること・`deltaEquals` のとき `expected` が数値または実在する参照元1つで
    あること・`anyChanged` の
    とき `observerTool`・`observerArgs`・`valuePath`・`expected` がいずれも無く、その要素の
    `effectType` が `observableChange` であること・`setup` と `teardown` の各操作のタグが5つのいずれかで必須
    フィールドがそろい、出力名が重複しないこと・
    `effectType` が `none` の要素は配列に単独でのみ現れ、その行はどの導出条件にも当たらない
    行であること)。自然文や解釈の要る記述は構造適合で不合格とする。
  - **規則適合検査**: 行ごとに担当の種別が写像規則から導出される期待種別と一致すること
    (コネクタメソッド→ツール/コネクタのプロパティ→宣言型ごとの取得・更新ツールへの
    スキーマ埋め込み/操作対象型・ハンドル操作型のプロパティ→CRUDツールへのスキーマ
    埋め込み/イベント→
    `type` 分岐/イベント引数型・DTO型のプロパティ→スキーマへの埋め込み/ビルダ生成→
    ハンドル返却ツールまたは対象切り替え引数/共通契約割当対象——現在PMXの取得・反映・
    Undoロック・ビルダ初期化・常駐アクセスオブジェクトの取得・解放メソッド——→
    共通契約への割当)。
  - **スキーマ対応検査**: 行のシグネチャの全引数・戻り値**とインスタンスメソッドの
    レシーバー**がスキーマ正本(JSON)の項目と対応すること(名前と型表現の機械照合。
    レシーバーの期待は型役割から導出する——操作対象型=集合の対象指定
    (`indices`/`range`/`all`/`handles`・`pmxHandle`)/ハンドル操作型=必須入力
    `handles`(集合)/コネクタ=ホスト保持のため公開入力
    なし——、対象入力を欠く行は不合格。共通契約割当の行は除き、同行は束縛内容との
    全単射・方向一致・束縛先種類の妥当性を検査する)、埋め込み型はプロパティが方向別の
    期待(応答=
    読み取り可能な全項目・入力=書き込み可能な全項目)どおり埋め込み先スキーマに
    存在すること。スキーマ正本の項目は必須/任意・既定値・値範囲を持ち、共通契約が定める
    共通引数と制約(ページングの境界値・`confirm`・`suppressUndo`・`pmxHandle` の有無・
    集合の対象指定(`indices`/`range`/`all`/`handles`)と親の集合指定
    (`parentIndices`/`parentRange`/`parentAll`/`parentHandles`)の必須性と相互排他・
    親セレクターと子セレクターの許容組合せが操作種別から導出した直積と一致すること
    (子の `handles` 経路が親セレクターを持たないことと、`add_` が親セレクター・子セレクターの
    代わりに `assignments` を持つことを、直積からの除外として含める)・**操作種別ごとに許容する対象指定の形が機械導出の期待集合と一致すること**・
    一括更新の `values`/`value` とメソッドの `args`/`argsList` の相互排他と長さ検査・
    参照コレクションの `add_` が対象指定を持たず `refIndices` を持つことと、型役割表の当該項目が
    参照先の要素型・参照先リストの所在・その判定根拠を持つこと・**スキーマ正本の配列
    フィールド全体**が逆算した要素数上限を持ち、`minItems` 1 の有無が上記の区別
    (対象を決める配列は持ち、対象集合に長さが従う配列は持たない)と一致すること・親キーと
    `pmxHandle` の許容組合せ)が該当ツールのスキーマへ正しく現れていることも検査する。SDK固有の既定値・値範囲は
    一次資料(ドキュメントXML)の記載から転記し、転記元の記載をスキーマ正本の項目に
    記録する(値制約を持つ項目の転記元欠落をゲートで検査する)。
  - **ホスト側の結び付け**: ツールからSDKメンバーへのディスパッチは、機械生成した行キー
    (上記「行キーの材料」)に基づいてコード生成またはリフレクション解決で行い、
    手書きの対応付けをしない(取り違えを排除する)。

### 実装の進め方(群ごとの縦切り)

**規模の実測**(上記「台帳の母集合の確定」の解決規則で台帳の提供行を公開API列挙へ突き合わせ、
除外一覧を引いて計数した): 提供対象シグネチャ 1,053(モデル314/変形・モーション392/ビュー277/
セッション70)。**ツール数と、行単位の検査の対象となる経路別行展開後の行数は、写像規則を
確定してから再計数する**。行はシグネチャ単位なので、経路別に展開した行数は提供対象シグネチャの
件数を下回らない——すなわち少なくとも1,053行になる。1,053行を人手で1行ずつ確認することは
現実的でない。したがって**行単位の確認は実機エディタに対して自動実行するE2E検査で行い、人手の
確認は機械化できない類型に限る**(下記「行単位の検査は自動実行する」)。人手の作業は「エディタを
起動して検査コマンドを1回実行する」ことに固定し、行数に比例させない。

群の実装順は モデル → セッション → ビュー → 変形・モーション。各群で次を繰り返す:
1. 群の能力対応表を作成する(シグネチャ単位へ展開し、全行に担当・入出力スキーマ・
   編集分類を確定する。シグネチャ単位ゲートを検査する)。
2. ホスト側に群のメソッドを実装する(ホストの `McpMethodTable` へ登録する。UIスレッド委譲・
   共通契約に従う)。
3. ブリッジ側に群のツールを登録する(アーキテクチャ仕様書の登録機構でホストへ中継)。
4. 群の行のうち `observableChange` を持つものを**変化の基準結果へ初回追記**したうえで、
   **自動E2E検査**(下記)にかける。群の完了条件は**最終ゲートと同じ一般形**とする——群の
   全行が、**自動E2E検査で合格**・**イベント行の payload 変換のようにxUnitテストで全行
   網羅**・**手動確認一覧に種別「行」として登録され類型の機械述語を満たし実施先が特定されて
   いる**、のいずれかを満たすこと(未分類ゼロ・不合格ゼロ)。**その内側で、`observableChange`
   を持つ行だけ**に次の書き分けを適用する——基準結果が非空なら自動E2E検査で基準の集合との
   一致を判定し、基準結果が空なら手動確認一覧へ類型(c)として登録する。人手が行うのは**MCP経路の代表確認**(その群の代表1ツールを
   Claude Code から呼び出して応答を得る)だけで、**手動確認一覧の行は登録と実施先の特定まで**
   とし、実施と結果の記録は受入検証が行う。結果を進捗ドキュメントへ記録する。
   群内全行と群に属する接続経路キーの検査記録が揃うことが群の完了条件(記録の網羅は
   「能力行キーと操作経路キーの組」と「接続経路キー」の2つの集合の照合で機械検査する)。
5. 群の対応表とスキーマをツール仕様書へ追記する。

### 行単位の検査は自動実行する

行単位の確認は、起動中の実機PMXエディタに対して**パイプ経由で行を実行する自動E2E検査**
(`scripts/e2e-tools.mjs`。既存の `scripts/e2e-check.mjs` と同じ経路)を主たる手段として行う。
**例外はイベント行**で、payload 変換の網羅は xUnit テストが担う(下記「イベント行の扱い」)。検査項目は
能力対応表とスキーマ正本から**機械生成**する(スキーマ正本は必須/任意・既定値・値範囲・
方向を持つため、下記の検査はすべてそこから導出できる)。人手が行うのは「エディタを起動して
検査コマンドを1回実行し、結果を記録する」ことだけとする。

**行種別(`rowKind`)ごとの検査の適用域**を先に決める(効果の導出はSDKメンバーへの
ディスパッチがある行にしか成り立たないので、行種別を分けずに「全行」と書くと適用できない行が
残る)。`rowKind` は能力対応表の必須フィールドで、担当の写像規則から**次の順に評価し最初に
該当した一つ**とする(排他)。**全行がちょうど1種別を持つこと(未分類・多重分類ゼロ)**を
ゲートで検査する。
- **1. `composed`(合成ツール)**: SDKメンバーへの1対1の中継でないツール。**`view_poll_events`
  と `session_release_handle` の2件**で、この2件がすべてであることをゲートで検査する。効果は
  導出せず**個別に確定する**——`view_poll_events` は読み取りで `postcondition` は `none`
  (排出の正しさはイベント行のxUnitと受入シナリオが担う)、`session_release_handle` は
  `handleConsumed` と `handleCascaded` を持つ。ディスパッチの呼び出し記録は求めない。
- **2. `commonContract`(共通契約割当行)**: 特別規則で共通契約側へ割り当てられる行。個別の
  ツールを持たないので `postcondition` を持たない。**`assignment` を次の3値の判別和で必須
  フィールドとして持つ**(単一のツールへ束縛される行だけではないため、ツール名だけを必須に
  すると正しい割当が表せない)。**対象名の実在を見るだけでは割当の取り違えを防げない**ので、
  **特別規則の表**を機械可読の正本として置き、`assignment` と `slotBinding` が
  そこから導出される値と完全に一致することをゲートで検査する。表は共通契約の整備で作る
  `docs/specs/pmx-editor-mcp-common-assignments.json` を正本とし、**項目はシグネチャ単位**と
  する——識別子は能力対応表の行キーと同じ組(上記「行キーの材料」)で、
  宣言型とメンバー名だけの述語では持たない(同名オーバーロードは期待スロットが異なるため。
  全体更新と部分更新では更新種別・更新インデックスの有無が違い、メンバー名だけでは
  どちらの期待値を当てるか決まらない)。各項目は**その識別子**・**`assignment` の分岐と対象名**・
  **`slotBinding` の期待スロット**を必須で持つ。母集合は特別規則が対象とするシグネチャの全体
  (解放・破棄、Undo制御、現在状態の取得、更新の適用、ビルダの初期化、常駐アクセスオブジェクトの
  取得)を**オーバーロードまで展開したもの**とし、**この表の項目集合が能力対応表の
  `commonContract` 行の集合と行キーで完全一致すること**もゲートに含める(項目数と行数が
  一致するので、1項目が複数行を兼ねることも、同じ識別子の項目が複数並ぶこともない)。あわせて**対象側からの逆参照**
  を検査する——照合先は**この表を再集計した集合ではなく、実際のツール定義・共通引数の定義・
  内部フローの定義がそれぞれ保持する行集合**とする(同じ表から集めた集合どうしを比べても、
  表ごと誤っている場合に独立した歯止めにならないため)。
  - `tool`(ツールへの束縛。公開の解放・破棄メソッド等): ツール名を持ち、**そのツールが実在し
    `rowKind` が `composed` または `directDispatch` であること**をゲートで検査する(解放は
    合成ツールへ束縛されるので、直接ディスパッチ行に限定しない)。
  - `commonArg`(共通引数への割当。Undo抑止の指定等): 共通引数名を持ち、**その引数が共通契約の
    確定値に実在すること**をゲートで検査する。担保は共通契約のxUnitテストが行う。
  - `internalFlow`(内部フローへの割当。現在状態の取得・更新の適用・ビルダの初期化・常駐
    アクセスオブジェクトの取得等): フロー名を持ち、**そのフローが共通契約で確定した名前の
    集合に実在すること**をゲートで検査する。担保は編集分類の状態契約の検査が行う。
- **3. `eventBranch`(イベント行)**: `view_poll_events` の `type` 分岐。独立したツール呼び出しを
  持たないので `postcondition` を持たない。payload変換の網羅はxUnitテストが担い、経路の成立は
  受入シナリオのリスナ確認が担う。
- **4. `schemaEmbedded`(スキーマ埋め込み行)**: 独立したツールを持たず、他のツールの入出力
  スキーマへ埋め込まれる行。**DTO型のプロパティ**に限らず、**操作対象型・ハンドル操作型の
  プロパティ/フィールドをCRUDツールへ集約した行**と**コネクタ型のプロパティを型ごとの
  取得・更新ツールへ集約した行**と**イベント引数型のプロパティ**も
  ここに入る。`postcondition` を持たず、**埋め込み先(ツール名、またはイベントの `type`)の
  1件以上の配列を必須フィールドで持ち**、その検査で担保する。**配列にするのは、1つの
  プロパティ行が取得ツールと更新ツールの両方へ埋め込まれるためである**(アクセサ方向で行を
  分けない——行はシグネチャ単位であり、get/setプロパティは1つのシグネチャだからである)。
  ゲートは各埋め込み先が実在することに加え、**方向の一致**——読み取り可能な項目は取得側の
  埋め込み先を持ち、書き込み可能な項目は更新側の埋め込み先を持つこと——を検査する
  (読み取り専用・書き込み専用の項目は配列が1件になる)。
- **5. `directDispatch`(直接ディスパッチ行)**: 上記のいずれでもなく、**独立したツールを持ち
  SDKメンバーへ中継する行**。コネクタメソッド由来のツール・型の公開メソッド由来のツール・
  公開コンストラクタ由来の生成ツールがここに入る(プロパティ由来の行は集約先へ埋め込まれる
  ので、ここには入らない)。`postcondition` と
  下記のディスパッチの呼び出し記録を**必須**とし、効果の導出規則はこの行種別にだけ適用する。

行の性質に応じて生成する検査:
- **ディスパッチの呼び出し記録**(直接ディスパッチ行の全行・全分類): `ok:true` とスキーマ適合だけを合格条件に
  すると、**何も呼ばずに正常応答を返す実装が全分類で合格してしまう**(戻り値のない行の
  成功値は `null` なので、とくに素通りしやすい)。これを塞ぐため、`PMX_EDITOR_MCP_DEBUG_HOOKS=1` の
  ときホストは次を行う。
  - **記録の場所と発行の順序を固定する**: ログ出力は、ツールからSDKメンバーへのディスパッチを
    行う**共通のディスパッチ処理の内側**で、かつ**SDK呼び出しから制御が正常に戻った後の
    継続でのみ**発行する(呼び出しと記録を同一の生成単位に置き、SDK側が例外を投げた場合は
    記録しない)。行ごとの実装コードからは出力しない。**この順序は観測できる**: 例外を
    起こす引数で呼んだときに記録が増えないことを検査に含めれば、記録が呼び出しの後段にある
    ことが確かめられる(記録だけを先に書く実装はここで落ちる)。
  - **記録の内容**: 行キー・**呼び出し連番**(ホストの生存期間で単調増加)・**応答値の
    ハッシュ**。ハッシュの対象は、**共通ディスパッチがSDKの戻り値を値の表現へ写した直後の
    正規化値**とする(写す前の生の戻り値ではない——列挙は列挙子名、SDKオブジェクトはindexか
    ハンドル、SDX・Drawing値は数値配列、というように写像で別物になるため、写す前の値と応答の
    `value` は原理的に突き合わせられない)。`out`・`ref` を持つ行は、応答として組み立てる
    オブジェクトを正規化値とする。戻り値のない行の正規化値は JSON の `null` とする
    (**要素型の公開メソッドの集合呼び出しは値の表現が定める例外に従い、`{invoked: <件数>}`
    を正規化値とする**)。
    **正規化とハッシュの規則**: 直列化は **RFC 8785(JSON Canonicalization Scheme)** に
    従う(キーの整列は入れ子にも再帰適用、数値の表記、文字列のエスケープ、Unicodeの扱いが
    その仕様で一意に決まる。ホストはC#・実行器はNodeと別ランタイムなので、独自規則を置くと
    直列化結果が食い違いうる)。非有限数は正規化の前に不正として扱い、その行を不合格とする。
    そのUTF-8バイト列の SHA-256 の16進表記(小文字)をハッシュとする。引数と戻り値そのものは記録しない
    (モデルデータをログへ出さない方針を守る)。
  - **検査の判定**: 自動E2E検査は、呼び出しの直前にログ位置を記録し、呼び出し後に
    **その位置より後ろへ当該行の記録が新しく1件増えたこと**と、**応答の `value` から同じ
    正規化・ハッシュ規則で算出した値がログのハッシュと一致すること**を判定する。
  - **保証の境界を3つに分ける**(過大に主張しない)。検査の手段ごとに、何を証明できるかが
    違う。
    - **ホストの外で独立に確認できる**: `kind` が `file` の判定だけ。ファイルの存在・サイズ・
      読み直しはOSとファイルシステムが答えるので、**外部にファイル効果が起きたこと**は
      ホストの実装に依らず確かめられる。**ただしこれもSDKを呼んだ証拠ではない**——SDKへ
      渡さずホスト側で書き出す実装や、用意しておいたファイルを複製する実装も同じ効果を
      作れる。**どの検査もSDKへ到達したことは証明しない**——それを示すにはSDK境界へ
      差し替え層を置く設計が要り、本計画の範囲を超える。外部から独立に確かめられるのは
      ファイル効果までである。どの行がここに入るかは「書き込み先を指定する引数を持つ」と
      いう機械述語で決まり、集合として固定される。
    - **ツール間の契約整合を確かめる**: `readback`・`handle`・画像の `changed`、および上記の
      呼び出し記録とハッシュ照合。**これらはいずれも同じホスト実装を経由する**ので、SDKへ
      渡さずホスト内の代替状態へ保存し読み取りツールがそれを返す実装や、ハンドル台帳だけを
      操作する実装は通る。**したがってSDKへ到達した証拠ではなく**、ツール間の整合と実装の
      取り違え(ディスパッチが作った値の後段での差し替え、記録と応答の経路のずれ、過去の
      記録の使い回し)の検出である。
    - **呼べること以上は確かめられない**: `callLogOnly`。妥当な引数で呼べて応答が
      出力スキーマに適合することは全行と同じく確かめるが、**意味のある結果を返したかは
      確かめられない**。
    この3区分の内訳——各行がどれに入るか——をツール仕様書へ記載する。**「全行を検査した」とは
    書かず、行ごとに何がどこまで確かめられているかを示す**。
- **呼び出し可能性**(全行): 妥当な引数で呼び、エンベロープが `ok:true` で `value` が出力
  スキーマに適合すること(JSON Schemaによる検証)。行の**操作経路キー**(PMX要素の index/handle ×
  **親軸の index/handle**(入れ子リストのツールのみ)× pmx軸、pmx直下、軸なし)ごとに
  実行する。**妥当な引数の合成規則**: 値範囲・長さ上限を持つ
  項目はその範囲内の中央値(整数項目は **床関数((最小 + 最大) ÷ 2)**。負の範囲でも
  一意に決まる)、列挙は先頭の
  列挙子、参照インデックスは事前操作で用意した要素の
  index、ハンドルは事前操作で生成したハンドル、共通引数は危険操作行の `confirm` を true、
  `suppressUndo` は未指定(受け付ける行では true の分岐も併せて実行)、`pmxHandle` は行の
  操作経路キーどおり(current経路なら未指定、handle経路なら発行済みハンドル)、
  **集合の対象指定は行の要素軸どおり**(`elem=index` の行は事前操作で用意した要素1件の
  `indices`、`elem=handle` の行は生成したハンドル1件の `handles`。**加えて、どちらの軸でも
  対象2件の分岐を実行する**——`elem=index` の行は要素を2件用意して `indices` に2件を与え、
  さらに `range`・`all` の分岐も実行する。`elem=handle` の行は**異なるハンドルを2件生成して
  `handles` に2件を与える**。対象2件の分岐では、`values`・`argsList` の順序が解決後の順に
  対応すること、後方の要素の値が不正なら先頭の要素も変更されないこと、2件目にも適用が及ぶこと
  を判定する——ハンドル経路を1件でしか通さないと、先頭だけを処理する実装が実機経路で検出
  できない)、**一括更新の値は `values`(対象と同数)と `value`(共通)の
  両分岐**を実行(更新可能な項目が2つ以上ある型では、2項目を同時に指定する分岐も実行して、
  全項目の反映と省略項目の不変を判定する)、**要素型の公開メソッド由来の行は `args`(共通)と
  `argsList`(対象と同数)の両分岐**を実行、**ハンドル操作型のレシーバーは生成した異なる
  ハンドル2件の `handles`**(集合の分岐は要素型と同じ判定を行う)、**親キーを取る行は
  **行の親軸どおりに与える**——`parent=index` の行は `parentIndices` に2件・`parentRange`・
  `parentAll` の各分岐、`parent=handle` の行は**発行済みの直接所有者ハンドル2件**の
  `parentHandles`——**を実行して親をまたぐ順序と一覧項目の親識別子・親内位置を判定する
  (入れ子への `add_` は代わりに **`assignments` を2グループ**で実行し、各グループの親項目には
  行の親軸に応じて `parentIndex` か `parentHandle` を入れる)、**ハンドル発行の行は `issuanceKind` に応じて——`constructor`・
  `factory` は公開引数0件なら `count` 2件のみ、1件以上なら `args` と `count` 2件の組と
  `argsList` 2件の両分岐、`receiverBound` はレシーバー2件(公開引数が1件以上なら `args` と
  `argsList` 2件の両分岐を実行し、要素ごとの引数が同じ順序で各レシーバーへ渡ることと、後方の
  引数が不正なとき1件も生成されないことを判定する)——**を実行、`fields` は未指定(全項目)と1項目指定の両分岐を実行、
  それ以外は**型別既定サンプル値表**
  `docs/specs/pmx-editor-mcp-sample-values.json`(共通契約の整備で作成。全公開型に既定と
  第2の2件ずつ。2件は必ず異なる値とし、同一をゲートで不合格とする。
  文字列・パス・`System.Object`・入力画像Base64を含む)から採る。同表は値表現の網羅ゲートと
  同じ型集合を母集団とし、未割当ゼロをゲートで検査する(手書きの取りこぼしを防ぐ)。
- **事前操作**(検査に必要な最小の要素の用意): 行の操作経路キーと引数の参照先から機械導出する。
  PMX要素の集合指定を取る行はその要素型を**2件**追加(集合の分岐——1件・2件・`range`・
  `all`——を同じ事前操作で賄うため)、親キーを取る行は**親を2件追加してそれぞれに子要素を
  用意する**(親をまたぐ順序・親の後方が不正なときの全件事前検証・`parentAll` の解決を
  通すため)。**親キーを取る行の事前操作は親軸で分ける**——所有コレクションへの追加は
  ハンドルを消費するので、現在PMXへ追加した親は `parentHandles` では参照できず、逆に未追加の
  親ハンドルを生成しただけでは中の子リストが空で子の位置を渡せないためである。
  - `parent=index` の行: 現在のPMXへ親を2件追加し、それぞれに子要素を用意する。
  - `parent=handle` の行: **未追加の親ハンドルを2件生成し、そのハンドルを親とする `add_` で
    各親の中へ子要素を用意する**(親はハンドルのまま残るので `parentHandles` で参照できる)。
  - `parent=handle` の `add_` 行: 子を入れず、**空の親ハンドル2件と、要求に使う子ハンドル
    または参照値**だけを用意する。
  入れ子への `add_` は親セレクターを持たないので、用意した親2件をそれぞれ `assignments` の
  グループの親に充てる。
  ハンドル引数を取る行は型役割表のハンドル発行能力でハンドルを1件生成(集合のハンドル指定を
  取る行とハンドル操作型をレシーバーに取る行は**2件**生成する)、抽象基底のコレクションを親に持つ行は**同じ具象型の要素を2件と、
  別の具象型の要素を1件**追加する(正常な2件の分岐は同じ具象型の2件を対象にし、型不一致の
  分岐は別の具象型を含む対象集合を作る。異なる具象型を2件だけ用意すると、正常系の2件分岐が
  型不一致で弾かれて2件目への適用を確かめられない)、入力ファイルのパスを取る行は
  検査専用の一時フォルダへ当該形式のファイルを1件用意、を導出規則とする(行ごとの手書きの
  手順は持たない)。**入力ファイルの用意は形式ごとに生成手段を定める**: 拡張子だけの空ファイル
  では正常呼び出しにならないため、形式→生成手段の対応を
  `docs/specs/pmx-editor-mcp-sample-files.json`(共通契約の整備で作成)へ持ち、各項目に次を必須で
  記す: 形式(拡張子)・生成手段の種別(本プロジェクトの保存系ツールで作る/型別既定サンプル値表の
  Base64を復号して書き出す)・**生成に使うツール名と引数**(前者の場合。例: PMXは空モデルを
  `SavePMXFile` 相当のツールで一時フォルダへ保存する)・**参照する型別既定サンプル値表のキー**
  (後者の場合)・生成前に必要な事前状態。提供対象のシグネチャに現れる入力ファイル形式の集合を母集団とし、未割当ゼロを
  ゲートで検査する。導出できない行は下記の手動確認一覧の対象とせず、導出規則の不足として
  規則を拡張する。
- **往復一致**(get/set対・複製編集型): 設定した値を読み戻して一致すること。値表現
  (SDXベクトル・列挙・色・Drawing値等)の相互変換の実地検査を兼ねる。
- **境界値**(値範囲を持つ項目): 最小・最大は成功、最小の1つ外・最大の1つ外は
  `TOOL_INDEX_OUT_OF_RANGE` または `TOOL_INVALID_ARGUMENT`。
- **既定値**(既定値を持つ任意項目): その項目を省略して呼び、読み戻しが既定値と一致する
  こと(一次資料からの誤転記の検出)。
- **null解除**(参照型項目): null を渡し、読み戻しが null であること。
- **所有/参照コレクション**: 追加で `total` が1増え、削除で戻ること。所有は消費した
  ハンドルが以後 `TOOL_INVALID_HANDLE` になること、参照はインデックス値が増減すること。
- **型役割**: ハンドル操作型は2個生成して異なるハンドルになり、解放後は
  `TOOL_INVALID_HANDLE` になること。コネクタ型は公開入力なしで呼べること。
- **編集分類**: 複製編集型は呼び出し後の読み戻しで反映を確認する。失敗時状態(未変更/
  変更済み/結果不明)は、意図的に失敗させられる行(範囲外・不正ハンドル・不正値)で
  読み戻しが分類どおりであることを確認する。
  **外部から観測できる効果を持つ行には、分類を問わず正常系の事後条件を必須とする**
  (直接変更型に限らない。表示・セッション型でも、カメラ視点の設定のように読み取り可能な値を
  変えるものは対象)。行ごとに `postcondition` を能力対応表の必須フィールドとして持たせる。
  **`postcondition` は下記の構造を持つ判定の配列**とし、1行に複数の効果があるときは複数
  並べて**すべて成り立つこと**を合格条件とする(所有コレクションへの追加なら「`total` が
  応答の追加件数だけ増える」と「消費した全ハンドルが失効する」の2つ、ハンドル解放なら対象と
  依存する子ハンドルの失効を並べる)。各判定は**次の閉じた構造**で機械可読に定める。
  - `effectType`・`effectKey`: その要素がどの効果に対応するかの識別子。値と決め方は下記の
    効果の表による。この組が配列内で重複してはならない。
  - `kind`: `readback`(読み取りツールで読み戻して比べる)/`file`(ファイルの生成)/
    `handle`(ハンドルの状態変化)/`callLogOnly`(外部から観測できる効果を持たず、上記の
    ディスパッチの呼び出し記録だけを合格条件とする)の4値。
  - `observerTool`・`observerArgs`: `readback` と `handle` で必須(ただし `comparison` が
    `anyChanged` の要素では**書かない**——観測群は検査器が機械導出するので、行ごとの記述を
    持たない。書かれていたら構造違反として不合格とする)。観測に使うツール名と、
    引数の束縛。**束縛は式を持たず、「参照元 → 観測ツールの引数名」の対応表だけ**とする。
    **参照元は次の5つの名前空間に限る**(接頭辞で区別する。式・演算は持たない)。
    - `arg:<引数名>` — 検査対象の行へ渡した**ツールのトップレベル引数**(対象指定・共通引数等)。
      **グループを取る入力(`assignments`)に限り、その内側を指す射影を2形だけ許す**:
      `arg:assignments[].<項目名>`(各グループの当該項目を並べた配列)と
      `arg:assignments[].<配列項目名>[]`(各グループの当該配列を**平坦化**した配列)。
      これが無いと、グループ追加の効果——親ごとの件数の読み戻しに渡す親集合、消費した
      ハンドルの集合——を観測側へ束縛できない。射影は要素を取り出して並べるだけで新しい値を
      作らないので、合成を持たない規則のままである。`effectKey` も、グループ内部のハンドル
      配列を指す効果ではこの射影の文字列を用いる(トップレベルの引数名 `assignments` では、
      どの項目の効果かが一意にならない)。
    - `sdkArg:<引数名>` — **SDKシグネチャの引数**。集合を対象とするツールでは
      `args`・`argsList` の中に入るので、トップレベル引数とは別の名前空間にする——**同じ名前が
      両方に現れうる**ためである(対象指定の `indices` と、SDKメソッドが取る `indices` という
      引数が実在する)。名前空間を分けておけば衝突の優先順位を決める必要がない。
      集合を対象としないツールでは、SDK引数がトップレベルに現れるが、参照はこの名前空間で書く。
    - `result:<valuePath>` — 検査対象の行の応答から取った値(新規発行されたハンドルなどを
      観測ツールへ渡すために要る)。`valuePath` の書式は下記と同じ。
    - `setupOut:<名前>` — `setup` の操作が出力した値(操作ごとに出力名を1つ持つ)。
    - `before` — `comparison` が `changed` のときに、`setup` の後・検査対象の行の呼び出し前に
      観測ツールで取っておいた値。この保存は検査器が自動で行う(記述は要らない)。
  - `valuePath`: `readback` で必須(`comparison` が `anyChanged` の要素では書かない)。
    比較する値の位置。**観測ツールの応答の `value` そのもの
    (空文字で表す)か、`value` 直下のプロパティ名1つ、または一覧応答からの配列射影
    `items[].<プロパティ名>`**のいずれかとし、これ以外の入れ子の指定は持たない
    (指定できない深さの値を比べる必要が出た場合は、その値を直接返す観測ツールを選ぶ)。
    配列射影は、集合を対象とするツールの効果を対象件数によらず1回の比較で判定するために
    要る(要素ごとに効果を並べると効果集合が対象件数に依存してしまう)。射影の結果は
    `items` の順、すなわち解決後の対象集合の順の配列になる。
  - `setup`: 既知状態の用意が要る場合に、**次の閉じたタグの操作の列**を指定する(自由な手順は
    持たない)。各操作は `out`(出力名。省略時は出力なし)を持てる。失敗した場合は当該行を
    不合格とする。
    - `initPmx` — 空モデルへ初期化する。出力なし。
    - `addElement`(`elementType`) — その要素型を1件追加する。出力は追加した要素のindex。
    - `createHandle`(`toolName`, `count`, `args`, `receiverHandles`, `handleBindings`) —
      型役割表のハンドル発行能力でハンドルを作る(この5つが閉じた項目)。
      **`receiverHandles` は `{sourceOut}`**(先行する `createHandle` の `out` を1つ指す)と
      し、その出力の全件をレシーバーに使う。先行する生成操作が複数あるときにどれを使うかを
      構造で指定できるようにするためで、構造適合ゲートは `sourceOut` が実在し、その操作が
      発行するハンドルの型がレシーバーの型と一致することを検査する。
      出力は生成順のハンドルID配列(1件でも配列)。項目は
      `issuanceKind` と公開引数の有無から決まる——`constructor`・`factory` は `count`(既定1)を
      使い、公開引数が1件以上なら `args`(サンプル値表から合成した1組を全件へ配る)も併せて
      使う。`receiverBound` は `receiverHandles`(先行する `createHandle` の出力を渡す)を使い、
      `count` は取らない(生成数はレシーバー数で決まる)。**`receiverBound` でも公開引数が
      1件以上あれば `args` を使う**(UIモデルのイベントリスナ生成のように、レシーバーのほかに
      必須の引数を持つ発行が実在する)。その `issuanceKind` と公開引数の組合せで使わない項目を
      指定した場合は、検査生成の誤りとして不合格にする。
      **ハンドル型の公開引数は、`args` の中へ直接書かず、専用フィールド
      `handleBindings`(配列。各要素は `{argName, sourceOut, index}`)で束縛する**——
      `sourceOut` は先行する `createHandle` の `out`、`index` はその出力配列の0始まりの位置で
      ある。`createHandle` の出力は1件生成でも配列なので、どの1件を使うかを位置で明示しないと
      束縛が一意にならない。実行時は解決したハンドルIDを `args` の当該項目へ代入する
      (`args` には値だけが載り、束縛の記述は混ざらない)。ハンドル型でない公開引数は
      サンプル値表から採る。この形は `receiverBound` に限らず、`factory` がハンドル型の公開
      引数を取る場合(ブリッジのUIモデル登録のような発行)にも同じく適用する——サンプル値表
      からはハンドルを作れないためである。構造適合ゲートは次を検査する: `sourceOut` が先行
      操作に実在すること・その操作が発行するハンドルの型が `argName` の引数の型と一致する
      こと・`index` がその操作の生成数の範囲内であること・**`handleBindings` の `argName` の
      集合が、そのシグネチャのハンドル型公開引数の集合と全単射である**こと(過不足ゼロ・
      同じ `argName` の重複なし・ハンドル型でない引数への束縛なし)。
    - `callTool`(`toolName`・`args`) — ツールを1回呼ぶ。`args` の束縛は上記の参照元に
      加えて、**JSONリテラル**と**型別既定サンプル値表への参照**(`sample:<型名>` で既定値、
      `sample2:<型名>` で第2の値)を取れる(読み取り行の既知状態を作るには、書き込み行へ
      渡す具体値がここに要る)。出力は応答の `value`。
    - `makeFile`(`format`) — サンプルファイル表の規則で一時フォルダへファイルを1件作る。
      出力はそのパス。
  - `comparison`: `changed`(事前値と異なる)/`equals`(`expected` と一致)/`exists`(存在
    する)/`invalidated`(以後その値をハンドルとして使う呼び出しが `TOOL_INVALID_HANDLE` に
    なる)/**`deltaEquals`**(事前値との差が `expected` に等しい。`expected` は数値リテラル
    のほか**参照元1つ**を採れる。集合を対象とする追加・削除では増減が対象件数で決まるので、
    **応答が返す件数** `result:added` / `result:removed` を指す——入力の配列長を指す形は採らない。
    削除は差が負なのに入力の長さは正で、符号を反転する式は持てず、`range`・`all` には長さを
    取れる入力が無く、親をまたぐ削除では解決後の件数が入力の1フィールドから決まらないためで
    ある。**符号は効果の向きから検査器が決める**(追加は正、削除は負)。固定値を書けるのは
    件数が入力にも解決結果にも依らない行だけである)/**`anyChanged`**(機械導出した観測群のうち少なくとも1つが事前値と異なる。
    `observableChange` 専用で、観測先を行ごとには書かない)の6値。
  - `expected`: `equals` のとき必須。**JSONのリテラル**、または上記の参照元1つ(接頭辞付き)
    のいずれか。参照元を使えば「設定後の値が、その行へ渡した入力値と等しい」を
    `sdkArg:<引数名>` で表せる。**演算や合成は持たない**。
    **集合を対象とするツールでの解決と突き合わせ**は次で閉じる(比較種別を増やさず、
    行の書き方も引数名のままにするため)。参照元 `sdkArg:<引数名>` は、検査生成器が実行する
    分岐から解決する——`args` 指定の分岐では `args` 直下の**単一値**、`argsList` 指定の
    分岐では `argsList` の各要素から取った**配列**になる。突き合わせは `equals` のままで、
    **期待が単一値・観測が配列のときは配列の全要素を単一値と比べ**、期待が配列のときは
    同じ長さ・同じ順序で要素ごとに比べる。どちらも新しい値を作らないので、合成を持たない
    規則のままである。ゲートは、集合を対象とするツールの `equals` の要素で `valuePath` が
    配列射影であることを検査する(観測が単一値のままでは分岐のどちらも判定できないため)。
  - `teardown`: 検査後に戻す必要がある場合に、`setup` と同じタグの操作の列を指定する。
  - `kind` が `file` のとき、検査するパスは `effectKey` が指す引数から取る(別途の指定を
    持たない)。
  **`kind` は行全体ではなく効果ごとに選ぶ**(配列の各要素が1つの効果に対応する)。効果は
  **`effectType` と `effectKey` の組で同定する**——`effectType` は下表の閉じた9値、
  `effectKey` はその効果を行の中で一意にする文字列(引数名・**`assignments` の射影文字列**・
  取得メンバー名・コレクション名のいずれかで、`effectType` ごとに下表が定める。射影文字列を
  使うのは、グループ内部の配列を指す効果でトップレベルの引数名 `assignments` では
  どの項目の効果かが一意にならない場合である)。行が持つ効果の集合はシグネチャ・型役割・
  コレクション区分から**機械導出**し、**`postcondition` の配列がその集合と
  `(effectType, effectKey)` をキーとして完全一致すること(過不足ゼロ)**をゲートで検査する
  (所有コレクションへの追加で「`total` の増加」だけ書いて「ハンドルの失効」を落とすと、
  集合と一致しないので不合格になる)。**`kind` と観測方法は `effectType` から一意に定まる**
  ので、述語の適用順は要らない。1つの行が複数の `effectType` を同時に持つのは効果が複数ある
  ということであり、競合ではない(所有コレクションへの追加は `countChanged` と
  `handleConsumed` の2つを別要素として持つ)。ただし `observableChange` だけは他の効果種別が
  1つでも導出された行には付かない(下記のフォールバック限定)。

  **操作の向き**(`read`/`write`)を能力対応表の必須フィールドとし、下表の導出条件が使う。
  判定は機械的に行う——**一次資料でプロパティの get アクセサーとして現れるメンバー、および、戻り値が
  `void` でなく `out`・`ref` を持たず、名前が `Get`・`Is`・`Has`・`Can`・`Find`・`Search` の
  いずれかで始まるメソッド**を `read`、それ以外を `write` とする。未割当ゼロをゲートで検査
  する。`write` を `read` と誤って判定した行は、対になる書き込み行が見つからず `none` へ
  落ちるだけで、誤って合格にはならない。

  | `effectType` | 導出条件 | `effectKey` | `kind` | 観測と `comparison` |
  | --- | --- | --- | --- | --- |
  | `fileWritten` | **危険操作の基準(b)の述語に該当**し、パス型の引数を持つ | そのパス引数名 | `file` | そのパスに対する `exists` |
  | `handleConsumed` | 引数にハンドルを取り、型役割表でそのハンドルを消費・失効させると定義されている(所有コレクションへの追加を含む) | その引数名。**ハンドルがグループの内側にある場合(`assignments`)は射影文字列** `arg:assignments[].handles[]` | `handle` | 指定した**全ハンドル**が `invalidated` |
  | `handleCascaded` | 解放・除去によって、引数に取っていないハンドルが従属して失効する | 失効する側を発行した生成ツール名 | `handle` | `invalidated` |
  | `handleCreated` | 型役割表で**ハンドル発行能力**と定義されたメンバー(ハンドルを新規に発行する行) | 発行するハンドルの型名 | `handle` | **応答のハンドル配列の全要素**が有効であること(`exists`)。**配列の長さが要求の生成数と一致することは効果に含めない**——事後条件の要素は `comparison` を1つしか持てず、同一の効果を2つの判定へ分けることも禁じているためで、長さの照合は出力スキーマの検証と行単位の自動E2E検査が担う。**同一性(2個生成して異なること)と解放後の失効は型役割の検査が担う** |
  | `countChanged` | コレクションへの追加または除去(所有・参照の区分を問わない) | コレクション名 | `readback` | そのコレクションの件数を返すツールで `deltaEquals` |
  | `stateWritten` | 同一宣言型に、そのメンバーが書く状態を返す公開の取得メンバーが**1つに定まる**(対応規則は下記の3つ) | その取得メンバー名 | `readback` | `equals`(`expected` は書き込んだ引数を `sdkArg:` で指す)。**集合を対象とするツールの行でも `equals` の1種類**で、`valuePath` は配列射影を採る(分岐ごとの解決と突き合わせは `expected` の規則が定める) |
  | `observableChange` | 操作の向きが `write` で、**他のどの効果種別も導出されず**、**観測群**(下記)が空でない | 空文字 | `readback` | 観測群を呼び出しの前後で読み、**少なくとも1つが変化**(`anyChanged`) |
  | `valueRead` | 操作の向きが `read` で、その値を書く行が下記の対応規則で1つに定まる | そのメンバー名 | `readback` | `setup` で既知状態を作り `equals` |
  | `none` | 上のいずれも導出されない | 空文字 | `callLogOnly` | ディスパッチの呼び出し記録のみ |

  `fileWritten` は**述語を新たに置かず、危険操作の基準(b)をそのまま正本として使う**
  (述語を二重に持つと、片方だけが取りこぼしを直したときに食い違うため)。基準(b)に該当しても
  パス型引数を持たない行(書き込み先を選べない保存)は `fileWritten` を導出しない——この
  分岐の扱いは危険操作の4分岐が定める。`Open`・`Load`・`Import` のパス引数は入力元なので
  該当しない。

  **書き込みと読み取りの対応規則**(`stateWritten` と `valueRead` が共有する。いずれも
  対応先が**1つに定まる場合に限り**導出し、複数候補があれば導出しない)。
  - **set接頭辞とget接頭辞の同名**。
  - **`Update` 接頭辞と同名の取得メンバー**。
  - **引数の一致**: 取得メンバーが**1つ以上の引数を持ち**、その全引数が書き込みメンバーの
    引数と**名前と型で一致**し、書き込みメンバーがそれに加えて持つ値の引数が**ちょうど1つ**
    である。名前が対称でない対(キーを取る共有データの設定と取得のような組)を、宣言表を
    作らずに機械で結べる。引数を取らない取得メンバーをこの規則で結ばないのは、条件が空虚に
    成立して無関係な対ができるのを防ぐため。追加の値引数が2つ以上ある書き込みは、`expected`
    が参照元を1つしか取れないので対応を導出せず、`observableChange` が引き受ける。

  **観測群**は次の3つの合併から機械導出した集合とする。
  - **(i)** 検査対象の行の宣言型に属する、**引数を取らない公開の取得メンバー**。
  - **(ii)** 同じ宣言型の取得メンバーのうち、**全引数が検査対象の行の引数と名前・型で
    一致するもの**(キー付きの設定・除去を、そのキーの取得メンバーで観測する)。
  - **(iii)** **モデルの持続状態を表す固定の集合**——PMXルートの全要素型の件数と現在パス。
    宣言型の外へ広げるのはここだけで、集合は固定なので行ごとの判断を要さない。
    宣言型がフォームコネクタである操作(ファイルを開く・初期化・元に戻す/やり直す)は、
    効果がモデル側に出るため、この集合が無いと観測できない。
  `observableChange` はこの集合を呼び出しの前後で一括して読み、少なくとも1つが変化する
  ことを求める。**これが、対になる取得メンバーが1つに定まらない書き込み——除去・元に戻す・
  やり直す・ファイルを開く・初期化のような操作——を、宣言表を作らずに no-op から守る**。
  **ただし守れる範囲は「他の編集や遅延更新が無い静止環境での検出」に限る**——事前観測・
  対象の呼び出し・事後観測の3手を不可分にする契約は無いので、2回目の事前観測より後に別要因で
  値が動けば、対象が何もしていなくても変化が観測されうる(自動E2E検査は他の操作を同時に
  走らせない専用の実行として動かすが、それは運用上の前提であって契約ではない)。厳密に
  塞ぐには観測列の不可分化か因果を識別する仕組みが要り、本計画の範囲を超える。この限界も
  保証の境界としてツール仕様書へ記載する。

  **偶然の変化を拾わないための3つの制限**を置く。
  - **フォールバックに限る**: 他の効果種別が1つでも導出された行には `observableChange` を
    導出しない。保存やハンドル生成・解放へ無関係な変化の要求が加わって、正しい実装が
    落ちるのを防ぐ。
  - **静止確認**: 呼び出しの前に観測群を**2回続けて読み**、その間に値が動いたメンバーは
    その行の観測群から外す(遅延反映や別要因で動く値を判定に使わない)。外した結果として
    観測群が空になった行は `none` へ落とす。
  - **合否に算入する条件**: 観測群に**引数一致のメンバー**((ii))が1つ以上あれば、それは
    その書き込みが指した状態そのものを読むので、**変化しないことを不合格とする**。引数を
    取らないメンバーだけの場合は、**編集分類が複製編集型・直接変更型のときに限り不合格と
    する**——この2分類は定義上モデルデータ・長寿命オブジェクト・外部へ作用するので、
    読み取れる値のどれかが動くことを要求できる。**表示・セッション型で引数一致のメンバーも
    無い行**(再描画の指示のように、読み取れる値を変えない契約のもの)は、変化の有無を
    **記録するだけで合否に算入しない**——正しい実装を落とさないため。この記録は残余の
    一覧を狭めるのに使う。
  **変化が起こりうる事前状態を作る**。`observableChange` の `setup` は**固定の並び**とする
  ——`initPmx` → **到達できる全要素型を1件ずつ `addElement`** → **さらに1件 `addElement`**
  → **その追加を元に戻す**(`callTool` で元に戻す操作を1回)。最後の2手を置くのは、**元に
  戻す履歴とやり直す履歴の両方を空でなくする**ためである(全要素型を追加しただけでは
  やり直す履歴が空のままで、正しく実装されたやり直しでも何も変わらず落ちる)。この並びで
  モデルは初期状態から離れ、初期化は各要素の件数が、元に戻す/やり直すはそれぞれの操作回数と
  モデル内容が、ファイルを開くは現在パスとモデル内容が、動きうる状態になる(空のモデルへの
  初期化・履歴が空での元に戻す/やり直す・同じファイルを開き直す、のように**もともと何も
  変わらない呼び出しを検査対象にしない**ため)。**書き込む値も事前値と必ず異なるものを選ぶ**——型別
  既定サンプル値表は全公開型に**既定と第2の2件**を持ち、事前値が既定と一致する場合は第2を
  使う(同じ値を書いて変化が出ないのを、実装の不作為と取り違えないため)。
  **それでも変化が起きない行がある**——既に正規化済みのモデルへの正規化、実質的に何も
  加えない取り込みのように、**正しい実装でも冪等になる**呼び出しである。事前状態が起動直後と
  違うかを見るだけではこれを区別できない(事前操作は必ず状態を動かすので、除外条件に入らない)。
  そこで `anyChanged` は**基準結果との比較**で判定する。
  - **基準結果の初回生成は群ごとに行う**(全対象行を一度に呼べるのは全群の実装が終わった後
    なので、一括生成にすると各群の完了条件を評価できない)。**群の実装が終わった時点で、その群の
    行だけを実行して基準結果へ追記する**。追記は当該群の行に限り、既存の行を書き換えない
    (書き換えは下記の再生成の条件でのみ許す)。群の完了条件は、追記後の基準結果を使った
    自動E2E検査の合格と、類型(c)の登録である。
  - **基準結果の取得**: 上記の群ごとの実行で、対象行について固定の事前操作と呼び出しを
    実行し、**観測群のうちどのメンバーが変化したかの集合**を行ごとに記録して
    `docs/specs/pmx-editor-mcp-change-baseline.json` へ書き出す(機械生成。手書きしない)。
    真偽値でなく**変化したメンバーの集合**を持つのは、期待した状態でなく無関係な観測値を
    変える実装を通さないためである。
  - **以後の判定**: 基準結果が空でない行は、**その実行で変化したメンバーの集合が、基準結果に
    記録された集合と完全に一致すること**を合格条件とする(欠けても増えても不合格。欠けるのは
    働かなくなった回帰、増えるのは意図しない副作用なので、どちらも見逃さない)。基準結果が
    空の行は、その事前状態では変化を要求できないので**記録のみ**とする。
  - **再生成は初回生成と別の操作として扱う**(初回生成は未記録の行への追記、再生成は記録済みの
    行の書き換え)。**基準結果の再生成を制限する**: 再生成してよいのは**SDKの版・固定の事前操作・
    サンプルファイル表・ツール契約の設計値・**観測群の規則・効果の導出規則・基準結果の生成
    規則**が変わったとき**だけとする(類型(c)の超過を解消するために観測群の規則を広げる操作は、
    記録済みの空集合を書き換えるので再生成に当たる。この条件を含めないと超過時の第一の出口を
    選べない)。通常の実装修正では
    再生成しない(再生成を許すと、回帰をそのまま新しい基準へ取り込んでしまう)。再生成時は
    **前後の差分を出力し、変化しなくなった行と、空集合から変化した行を明示する**。この条件と手順を検証手順書へ登録する。
  - **基準結果の限界を明記する**: 基準結果は初回の実装に対する実測なので、**初回から何も
    していない行は基準結果ごと空になり、no-op のまま記録のみへ入る**。基準結果は
    **回帰の検出には使えるが、初回の実装が正しいことのオラクルにはならない**。実装から独立した
    オラクル(検証専用の経路でSDKを直接呼び、同じ事前操作に対する観測群の変化を基準にする)を
    置けばここは閉じるが、それは本計画の範囲を超える設計追加である。この境界を保証の境界へ
    加えて記載する。
  - **基準結果が空の行は手動確認一覧へ登録する**: 種別「行」・**類型(c) 自動判定の基準が
    無い**として登録し、実施と結果の記録を受入検証が行う(登録・実施・記録の枠から外さない。
    件数がどれだけになるかは実測前なので、ここでは大きさを見積もらない)。
  - 入力ファイルを取る行で冪等になるのを減らすため、**サンプルファイル表の各項目に
    「生成後のファイルが0バイトでないこと」と、モデル形式の項目では「頂点を1件以上含むこと」
    を必須要件として課す**(空ファイルを生成手段に選ばない)。この2つは生成物を読めば機械で
    判定できるのでゲートに載せる。

  `valueRead` の `setup` は**固定の並び**とする——`initPmx` → 引数の参照先の要素型ごとに
  `addElement` → 対応する書き込み行を `callTool` で1回。長さは参照先の数で決まるので探索を
  要しない(対応する書き込み行が1つに定まらない読み取り行は `valueRead` を導出しない)。
  SDKの戻り値を捨てて固定値を返す実装は既知状態と食い違うので落ちる。

  **`none` に落ちる行**は、`postcondition` を `effectType` が `none`・`kind` が `callLogOnly`
  の要素1つだけ(`effectKey` は空文字)とする。これは上の8つの導出条件の否定なので**集合と
  して一意に決まる**(事前の列挙を要しない)。**残余には読み取りと書き込みの両方が入る**——
  ホストの実行パスや登録プラグイン数のように引数も可変状態も持たない読み取りと、静止確認で
  観測群が空になった書き込みである。加えて、`observableChange` を導出はしたが**合否に
  算入しない行**(表示・セッション型で引数一致の観測メンバーが無いもの)も、判定という点では
  残余と同じ扱いになる。**これらは意味のある結果を外部の検査では確かめられない範囲であり、
  行ごとにその旨をツール仕様書へ明記する**(検査していないものを検査済みに見せない)。

  **画像差分による残余の切り分け**(合否を増やすためではなく、残余として列挙する範囲を
  狭めるため):
  `callLogOnly` に落ちた行のうち**書き込み操作を表すもの**と、`observableChange` を合否に
  算入しない行について、検査器は呼び出しの前後で
  ビューポート画像を取得して比較し、**変化したかどうかを記録する**。変化していればその行は
  少なくとも描画へ届く効果を持ったことになるので、残余の一覧から外して記録する。
  **変化しないことは不合格条件にしない**——描画に出ない項目なのか画像に出ない小ささなのかを
  外から区別できないため。どのメンバーが描画へ現れるかを事前に宣言する表は置かない(一次資料の
  記述から見た目の変化を読み取る作業は転記ではなく意味判断になり、実装時の判断を残すため)。
  **比較の条件は固定する**——`initPmx` の後、頂点3つ([0,0,0]・[10,0,0]・[0,10,0])と
  その3頂点を参照する面1つ・材質1つを追加した最小のモデルを可視・最前面で原点に置き、視点を
  カメラ位置 [0,0,-30]・注視点 [0,0,0] とする(空モデルには描画される形状が無く、何を変えても
  画像が同じになるため)。この記録は残余の一覧を**生成する**ために使い、合否には算入しない。
  `postcondition` の欠落・構造違反・`callLogOnly` の根拠欠落は、いずれもゲートで不合格とする。
- **危険操作**: `confirm` なしの呼び出しが `TOOL_CONFIRM_REQUIRED` になること。`confirm:
  true` 側の実行は、危険操作の3基準と引数の形から**行ごとに機械的に**次の4分岐へ振り分ける
  (基準はシグネチャ単位なので、1つの能力が複数の分岐に入ることがある。既知の該当能力は
  CAP-006/007/012/013/014/016/166/339/445 の9件で、すべていずれかの分岐に入る)。
  - **書き込み先を指定できるファイル書き込み**。述語: 基準(b)に該当し、かつ書き込み先を
    指定する引数を持つ(既知の該当: CAP-012 `SavePMDFile`・CAP-016 `SavePMXFile`・
    CAP-339 の `IPXPmx.ToFile`・CAP-445 のVMD書き出し・CAP-013 `SaveMaterialName`・
    CAP-166 `SaveViewSetting`)。検査専用の一時フォルダ(`%TEMP%` 配下。検査の前後で
    作成・削除する)を宛先にして実行する。**合格条件に副作用の観測を含める**: 呼び出し前に
    宛先が存在しないことを確かめ、呼び出し後に**宛先ファイルが生成され、サイズが0でない**
    ことを判定する。さらに、**本プロジェクトの読み込み系ツールで開き直せる形式**
    (PMX・PMD)は、開き直しが成功することまでを合格条件とする。`ok:true` が返ることだけを
    条件にすると、SDKを呼ばずに正常応答を返す実装でも合格してしまう。
  - **書き込み先を指定できないファイル書き込み**。述語: 基準(b)に該当し、かつ書き込み先を
    指定する引数を持たない。**一次資料(SDKドキュメントXML)の確認により、提供対象にこの述語を
    満たすシグネチャは存在しない**: 保存系はいずれも書き込み先のパスを引数に取る
    (CAP-012 `SavePMDFile` はPMDファイルパス、CAP-016 `SavePMXFile` はPMXファイルパス、
    CAP-013 `SaveMaterialName` は保存材質名ファイルパス、CAP-166 `SaveViewSetting` は
    設定ファイルパス)。よってこの分岐は空であり、実行検査を省く扱いは設けない。
    提供対象に該当が現れた場合は設計変更としてユーザーへ報告する。
  - **現在のPMX状態の一括初期化**。述語: 基準(c)の明示集合に該当する(CAP-007
    `InitializePMD`・CAP-014 `InitializePMX`・CAP-339 の `IPXPmx.Clear` の current 経路)。
    検査の前後で空モデルの初期化を行っているので、実行しても他の検査へ影響しない。ただし
    CAP-007 は**PMD形式**への初期化で実行後の状態が空PMXモデルと異なるため、この行の検査の
    直後に `InitializePMX` を実行して状態を戻す。
  - **エディタ終了**。述語: 基準(a)に該当する(CAP-006)。実行するとエディタが落ちるため、
    検査実行の**最終段で単独実行**し、パイプが切断されホストのログに停止の記録があることを
    もって合格とする(以後の検査は行わない)。

検査間の独立性: 各検査は「空モデルの初期化 → 事前操作 → 実行 → 空モデルの初期化」を単位と
し、検査どうしが状態で依存しないようにする。

**群単位の検査(検索での発見可能性)**: 行単位ではなく群単位で行う(行ごとに想定質問文を
用意すると人手が行数に比例するため)。群ごとに**想定質問文5件**を用意する。
**選定は機械的規則で行う**(担当者の主観で易しい5件を選べないようにする): その群の能力対応表
から、**ツール数の多い順に上位5つの要素型**を取り、各要素型について「その要素型の一覧を
取得したい」「その要素型を1件追加したい」のうち**その群に実在する操作の側**を定型文へ当てる
(両方あれば一覧側)。定型文は「モデルの<要素名詞>を<動作>にはどうすればよいか」とし、
SDKの型名・メンバー名・ツール名を含めない。上位5つに満たない群は、実在する要素型の数だけ
用意する。
**加えて、プロパティを名指しした質問文を群ごとに5件用意する**(合計10件)。プロパティ単位の
ツールを設けない写像では、利用者が思い浮かべる語(UV座標・頂点の位置・材質の色など)が
ツール名から消え、集約ツールの説明文だけが索引語になるためである——要素型単位の質問文だけ
では、この経路が引けなくても全ゲートが合格してしまう。選定も機械的規則で行う: 上位5つの
要素型それぞれについて、その要素型の**読み取り可能な項目のうち想定文字数が最大のもの**を
1つ取り(**同値の項目が複数あるときはメンバー名の序数昇順で最初のもの**を採る。頂点の
追加UVのように同型の項目が並ぶ型があるため、タイブレークを置かないと選定が一意にならない)、
定型文「モデルの<要素名詞>の<項目の日本語名>を調べるにはどうすればよいか」へ当てる。
質問文にはSDKのメンバー名・ツール名を含めない。期待ツールはその要素型の取得ツールである。
**項目の日本語名は[日本語名の正本](../specs/pmx-editor-mcp-property-names.json)を正本とする**
(名前の決め方・根拠の種別・解決の規則・重複の禁止は
[日本語名仕様書](../specs/pmx-editor-mcp-property-names.md)が定める)。
期待するツール名は写像規則から一意に決まる(各文に対応付けて
`docs/specs/pmx-editor-mcp-search-probes.json` へ記録する。**質問文と期待ツール名は、その群の
実装に着手する前に確定して記録し、以後変更しない**——検査結果を見てから質問文を選べると、
発見可能性の独立した検査にならない。不合格時に見直してよいのはツールの説明文だけで、質問文を
変えるなら設計変更としてユーザーへ報告する。選定規則は「その群の代表的な用途を、SDKの型名・
メンバー名を使わずにユーザーの語彙で述べる」とする)、MCPサーバーを登録した状態で
`claude -p` をヘッドレス実行し、実行結果に含まれるツール呼び出し名に期待ツールが現れるかを
機械判定する。**実行形式は次で確定する**(`--output-format json` の応答は最終結果・セッションID・
メタデータだけで `tool_use` を含まないため、ツール名を取り出せない):
`--output-format stream-json --verbose` で受け、`assistant` メッセージの `tool_use` ブロックの
`name` を集める。あわせて再現性のため、`--model`(前提7の対応モデルのいずれか)・
`ENABLE_TOOL_SEARCH=true`・`--allowedTools` による当該MCPツールの自動許可を固定する
(`-p` の既定の許可モードは Manual で、指定しないとツール呼び出しが許可されずに終わる)。
**モデルは前提7の対応モデルのうち `claude-sonnet-4-6` を検証手順書の固定値とする**
(利用者が別のモデルを常用する場合は、その識別子へ置き換えて再実行する旨も手順書に記す)。
**合格条件は、要素型の質問文とプロパティの質問文を合わせた10件のうち8件以上**とする
(2種類の質問文をまとめて1つの割合で判定する。種類ごとに別の閾値を置くと、群によって
用意できる件数が違うときに分母が食い違う)。上位5つに満たない群は用意した件数が分母になり、
**閾値はその8割(切り上げ)**とする。下回った場合は説明文の
語彙を見直して再実行し、**3回で合格しなければユーザーへ報告して判断を仰ぐ**(語彙の設計
判断であり、自動では決められないため)。合否はゲートの「不合格ゼロ」に数える。

**手動確認一覧** `docs/specs/pmx-editor-mcp-manual-checks.json`: 自動で判定できないものを
登録する。項目は**種別**で2つに分け、必須項目と歯止めを種別ごとに定める。

- **種別「行」**: 能力対応表の行のうち自動E2E検査で判定できないもの。**能力行キーと操作経路キー
  を必須**とし(ゲートはこのキーで対応表と突き合わせる)、類型・手順・期待結果・実施先も
  必須とする。
- **種別「契約」**: 行の合否ではなく、共通契約や前提の未検証項目のうち人手の確認を要する
  もの(Undo粒度がUpdate単位であること、画像応答がクライアント側で警告になるか等)。
  **対象の契約名と、それを定めた設計値の所在**を必須とし、類型・手順・期待結果・実施先も
  必須とする。行キーは持たないので対応表との突き合わせの対象外とする。

理由の類型は次の**3つだけ**の閉集合とする(種別に依らず共通)。

- **(a) UI操作の発火を要する**: 検査対象の挙動が、マウス・キー入力をエディタのUIへ与えない
  限り起きない。種別「行」の機械述語: 当該行が View イベント・UIモデルイベントの発火を
  確認対象に含むこと(イベント種別の識別子が `type` 分岐に存在すること)、または当該行の
  シグネチャがエディタのモーダルUI(ファイルダイアログ等)を開くこと。
  **件数の上限**: この述語だけではイベント行をすべて手動へ移せてしまい(公開イベントが増える
  ほど手動項目が増える)、人手が行数に比例する。そこでイベント行の検査を3つに分け、
  **全イベント行を機械で網羅し、手動はリスナ型ごとの代表1件に固定する**。
  - **payload 変換の全行網羅(xUnitテスト)**: SDKのイベント引数からJSON値への変換は、
    エディタを起動せずに検査できる純粋な写像である。**変換器の入力を、SDKのイベント引数型
    そのものではなく、ホスト側で定義する中立の入力型(イベント種別の識別子と、値表現へ写す前の
    プロパティ値の組)とする**——SDKのイベント引数型に公開コンストラクタや設定可能なプロパティが
    あるかは一次資料に記載が無く、テストから組み立てられる保証がないため、変換器の入口を
    SDK型に固定しない。実機のリスナは受け取ったSDKのイベント引数をこの中立入力型へ詰め替えて
    変換器へ渡す。**この詰め替えは手書きせず、SDKのイベント引数型のリフレクションから機械生成
    する**(ツールからSDKメンバーへのディスパッチと同じ原則。手書きの対応付けがなければ
    プロパティの欠落・取り違えは起こらない)。そのうえで、**各イベント引数型の公開プロパティが
    もれなく中立入力型の項目に現れることを、型の公開プロパティ集合との突き合わせでゲート検査
    する**(インスタンスを構築できなくても、型の形は静的に検査できる)。中立入力型を用いれば
    全 `type` 分岐へ入力を投入でき、全イベント行の payload 変換を網羅できる。行の網羅は
    この2つ——詰め替えの機械生成と公開プロパティ網羅ゲート、および変換の全行xUnit——で担保する。
  - **キュー契約の検査(自動E2E)**: `seq` 連番・`dropped`・`remaining`・`sourceHandle` の
    対応は、キューへイベントが入らないと観測できない。ホストにテスト用の投入経路を設け、
    自動E2E検査から投入して検査する。**投入経路の契約**: IPCの専用メソッド
    `debug_enqueue_event`(中立入力型とリスナのハンドルIDを受け取る)とし、MCPツールとしては
    公開しない。**実イベントと同じ enqueue 関数を通す**(通らなければキュー契約の検査に
    ならない)。有効になるのは環境変数 `PMX_EDITOR_MCP_DEBUG_HOOKS=1` を設定して起動した
    ホストに限り、未設定時は未知メソッド(-32601)を返す。配布パッケージから除外はせず、
    既定で無効という形でこの一点に集約する(ビルド構成を分けると配布物と検査対象が別物になる)。
  - **実機のイベント発火(手動)**: SDKのイベントが実際に発火し、ホストのリスナがそれを
    受け取ってキューへ積むこと。**リスナ型ごとに代表1件**を手動確認一覧へ登録する。
    登録件数が**リスナ型の集合と完全一致すること**(過不足ゼロ)をゲートで検査する
    (上限だけでは0件でも合格してしまうため、下限も課す)。
  この3分割により、イベント行に「検査しない行」は生じない。
- **(b) 目視での判断・観察を要する**: 期待結果が値の一致でなく、描画結果の内容が意図どおりか、
  またはMCPクライアント側の表示がどうなるかの観察であるもの。種別「行」の機械述語: 当該行の
  出力スキーマが画像コンテンツを含み、**かつ**期待結果が値の一致でないこと(値の一致で判定
  できる項目——`warnings` の文字列一致など——は述語を満たさず、自動E2E検査側に置く)。
  **件数の上限**: 画像を返す行を多数手動へ移せないよう、登録は**画像を返すツールごとに
  代表1件**とし、登録件数が画像を返すツールの集合と**完全一致**することをゲートで検査する
  (同じツールの経路違いを個別に手動へ積み増せない)。

- **(c) 自動判定の基準が無い**: 効果が外部から観測できるはずだが、変化の基準結果が空で、
  自動では合否を決められないもの。種別「行」の機械述語: 当該行の `postcondition` に
  `observableChange` の要素があり、**かつ**変化の基準結果の当該行の集合が空であること。
  **件数の上限は50件**とし、**該当する行は全件を登録したうえで、登録件数が50件を超えたら
  ゲートを不合格にする**(超過した行を一覧から落とすのではない。落とすと検査もされないまま
  消えるため、登録は全件必須のままにして、超過そのものを不合格にする)。**50件は、1件あたり
  数分の手作業として一巡が1日に収まる規模として置く取り決めである**——この値の当否は検査の
  正しさに関わらず、人手の量が実行可能な範囲に収まるかだけを決める。**不合格になった場合の
  出口は、観測群の規則を広げてこの類型から外すか、設計変更としてユーザーへ報告するか**の
  いずれかとし、手動へ積み増して通すことはしない。あわせて**件数と内訳を成果物として出す**:
  基準結果の生成時に、この類型の行数と内訳をツール仕様書へ記載する。

利用者の環境を不可逆に書き換える危険操作は、人手に移しても破壊は同じなので手動確認一覧には
入れない(上記「危険操作」の扱いで決着させる)。

**歯止め**: 種別「行」は類型の機械述語で検査し、述語を満たさない登録は不合格とする(恣意的な
手動化を止める。除外一覧・危険操作フラグと同じ様式)。種別「契約」は機械述語を持てないため
**閉集合**とし、一覧に載る種別「契約」の項目がその閉集合と一致することをゲートで検査する
(件数・対象名の両方)。**閉集合は次の2件で確定する**。以後の追加は設計変更としてユーザーへ
報告する。閉集合との一致検査は、項目を登録するのが群ごとの実装なので**本計画の完了条件でのみ**
判定する(閉集合を確定した時点では登録が0件で、一致を問えない)。
- 「Undo粒度がUpdate単位であること」(類型(a))。
- 「既定の `maxLongSide` の画像応答がクライアント側で警告になるか」(類型(b))。

**結果の記録**: 各項目は結果を機械判定できる形で持つ。必須フィールドは次の3つとする。
- `result`: `pass` / `fail` / `not_run` の3値。初期値は `not_run`。
- `evidence`: 結果の裏付け。目視の項目は観察した内容の記述、値で判定する項目は観測値。
  `result` が `not_run` のときは空文字、それ以外では**空文字を許さない**。
- `checkedAt`: **受入検証の実行識別子**。形式は RFC 4122 の8-4-4-4-12桁の16進表記(小文字)
  で、この形式は手動確認一覧のスキーマが強制する。値は受入検証を開始するときに1つ生成し、
  その回の全項目へ同じ値を書く(日時ではなく実行ごとに一意な識別子とする)。`result` が
  `not_run` のときは空文字、それ以外では空文字を許さない。ゲートは、進捗ドキュメントの
  見出し「受入検証の実行識別子」の節に記録された値を基準として照合する(記録先が固定なので
  判断なしに読める。生成と記録の手順は検証手順書が正本)。

受入検証の網羅ゲートは、**全項目の `result` が `pass` であること**(`not_run` と `fail` が
ゼロ)・`evidence` が空でないこと・**全項目の `checkedAt` が、受入検証の開始時に進捗
ドキュメントへ記録した実行識別子と一致すること**(過去の実行の記録を残したまま合格に
できないようにする)を検査する。
**この機械判定が保証するのは、人手による合否記録が全項目そろっていて、その実行識別子の回の
ものであることまでである**——目視した内容そのものの当否を機械が判定するわけではない。

種別「契約」の必須項目「設計値の所在」は、**移送後の恒久文書の見出し**(共通契約仕様書
またはツール仕様書の該当見出し)で記録する。計画書の節名では記録しない。
どちらの種別でも、歯止めを満たさない
のに自動化できないものが現れた場合は、手動へ逃がさず検査生成規則を拡張する。

**所要時間がかかるだけの検査は手動確認一覧に入れない**。長時間放置後のコネクタ再利用
(前提5)は実行器の `--soak` 経路で自動化する。この経路は通常の一巡とは**別の最小構成の実行**と
する(通常の一巡は最終段でエディタを終了させるため、その後に待機できない): 接続して
handshake を行い、**前提5が実測した7経路**(`PXCBridge.BuilderInitialize`・
`PrimitiveBuilderInitialize`・選択頂点インデックス取得・`GetCurrentPmx`・`ViewCtrl`・
`SystemCtrl`・イベントコネクタの生成と解放)を1回実行して成立を確かめ、**8時間**接続を
張ったまま待機し、その後に同じ7経路を保持中のコネクタで再実行して全て成功することを合格
条件とする(ビルダ初期化の2経路も前提5の実測対象なので、長時間保持の回帰検査から落とさない)。**あわせて、ホストのログ全体(起動から終了まで)に「Cプラグインコネクタの取得」の
記録がちょうど1件だけ存在すること**も合格条件に含める——「開始位置以降に増えていない」だけでは
基準位置より前の複数回取得も、取得記録が一度も出ていない場合も通ってしまう。API成功だけを条件に
するのも同様に不足で、待機後に再取得する実装でも合格してしまい「取得1回・常駐保持」の回帰検査に
ならない。**PMXファイルを開き直す検査にも同じログ判定(全区間でちょうど1件)を課す**。
8時間は**1作業日を検証の対象範囲とする方針値**であり、実測や要求仕様から導いた値ではない
(実測は93秒までであることは前提5のとおり)。前提5の未検証のうち
数時間の放置はこれで覆えるが、**24時間の放置は覆えず未検証のまま残る**(残存リスクとして
受容する)。
既定の実行では回さず、受入検証で**単独で**1回実行する(所要8時間で、他の検査と同じエディタ
インスタンスを共有すると通常の検査一式のエディタ終了に巻き込まれるため、同時に走らせない)。
**専用インスタンスで実行する**: 実行前に稼働中のエディタをすべて終了し、この検査のためだけに
エディタを起動する。完走後はそのインスタンスを終了する(取得ログが全区間でちょうど1件である
ことを判定するには、起動から終了までが1つのホストプロセスの生存期間として閉じている必要が
ある)。

手動確認一覧の各項目は受入検証で消化する。**実施先は検証手順書の見出しで記録する**——
類型(a)(b)は受入シナリオの見出し、**類型(c)は専用の見出し「自動判定の基準が無い項目の確認」**
とする(計画書は完了時に削除されるため、計画書名・計画書内のシナリオ番号では記録しない)。記録の主体は受入検証の側とし、本計画は実施先の見出しを登録するところまでを行う。

**MCP経路(Claude Code経由)を全ツールで確認しないことの根拠**: ブリッジの中継はツールに
依らず同一の機構(ブリッジ基盤が確定した「1ツール=ホストの1メソッドへの中継」とエラー変換)である
ため、経路の成立は群ごとの代表1ツールと受入シナリオで担保する。ツール個別の検査は
上記の自動E2E検査(ホストのパイプ経路)が担う(何がどこまで確かめられるかは上記の
保証の境界のとおりで、全行が同じ強さで確かめられるわけではない)。ただし**クライアント側でしか観測できない
事柄**(応答サイズのしきい値が宣言値へ置き換わること・検索で意図したツールを引けること)は
パイプ経路では確かめられないため、`claude -p` のヘッドレス実行による検査で扱う。

**ゲート**: 能力対応表の全行が「自動E2E検査で合格」「xUnitテストで全行網羅されている
(イベント行の payload 変換)」「手動確認一覧に種別『行』として登録され、類型の機械述語を
満たし、実施先の見出しが特定されている」のいずれかであること(未分類ゼロ・不合格ゼロ)。
イベントの代表発火の登録は**リスナ型の集合と完全一致**していること。接続経路キーについても
同じ網羅を検査する。種別「契約」の項目は
上記の閉集合との一致を検査する。**手動確認の実施と結果の記録は受入検証が行う**ため、
本計画のゲートは登録と実施先の特定までを見る。

## 実装ステップ

1. 共通契約(エンベロープと `warnings`・エラーコード・編集分類の状態契約・ページングと
   応答サイズ予算・値表現と網羅ゲート・画像変換・ハンドル管理(型別解放・依存順)・
   イベントキューとpoll契約・suppressUndo・confirm検査)のホスト側/ブリッジ側の共通処理を
   実装し、xUnitテストを書く(対象: ページング境界・応答サイズ予算のゲートと
   予算超過時の件数縮小(`nextOffset` と `warnings` の内容)・先頭1件超過時の
   TOOL_RESPONSE_TOO_LARGE・型別に逆算した `limit` の既定/最大がスキーマ正本の値と一致する
   こと(最大が最小1項目での逆算値・既定が全項目での逆算値の半分であること)・
   **集合の対象指定の解決**(4形の相互排他・操作種別ごとの許容形と許容外指定のエラー・
   空配列と重複指定が `TOOL_INVALID_ARGUMENT` になること・解決後の順序が形ごとの規則どおりで
   あること・`offset`/`limit` が解決後の集合に対して働くこと)・
   **事前検証と部分適用しないこと**(対象の1件でも範囲外/不正ハンドルなら状態が未変更で
   失敗すること・**後方の要素の更新オブジェクトに不正な値・範囲外の参照があるとき、先頭の
   要素も変更されていないこと**・直接変更型の経路——`pmxHandle` 指定と `handles`——でも同じ
   保証が成り立つこと・`add_` に不正ハンドルを混ぜたとき1件も追加されず全ハンドルが有効な
   まま残ること)・
   **一括更新**(`values`/`value` の相互排他・`values` の長さ不一致が
   `TOOL_INVALID_ARGUMENT` になること・複製編集型の経路では対象が何件でも `Update` の
   呼び出しが1回であること・直接変更型の経路では `Update` を呼ばないこと・応答が `updated`
   だけで対象の位置やハンドルの列を含まないこと・**`updated` が解決後の対象件数と一致し、
   同じ値を再設定した要求でも全項目を省略した要求でも同じ件数になること**・
   **スキーマ正本が持つ配列フィールド全体**(対象指定・親指定・`values`・`argsList`・
   `assignments` とその中の子配列・VMEの `targets` を含む。名前の手書き列挙で判定しない。
   **ハンドル発行ツールの `argsList`・レシーバー配列はこの一般検査の対象から外す**——
   それらは応答側を含む分岐別の生成件数上限を持つので、要求側だけの逆算値とは一致しない。
   発行ツールの上限は下記の発行の検査だけで判定する)の
   要素数上限が要求サイズ予算からの逆算値と一致することと超過時のエラー・
   `assignments` では子件数の合計に上限が掛かること・要求全体のUTF-8バイト数が
   予算を超えたときに `TOOL_REQUEST_TOO_LARGE` となり状態が未変更であること・**Undoロック
   残留があるサイズ超過の呼び出しで回収が試みられず警告も付かないこと、および続けて受理可能な
   呼び出しを行うとその冒頭で回収されること**)・
   **空の対象集合**(`all` が空のリストを解決したときの挙動を、`all` を許容する操作種別すべてで
   検査する: 取得は `total` 0 と空の `items`・更新は `updated` 0・削除は無操作として正常に
   受理されること・`return`/`out`/`ref` を持つ要素メソッドは**空配列**、いずれも持たない
   メソッドは `invoked` 0 を返すこと。要素ごと指定は空配列が受理され、共通指定は対象0件でも
   値の全項目が検証されて、正常な値なら上記の正常応答・不正な値ならエラーになること)・
   **親の集合指定**(許容形の相互排他と、`parentHandles` を持つかどうかが直接所有者型の
   ハンドル発行能力の有無から導出されること(発行能力の無いコレクションでは3形になり、
   `assignments` も `parentHandle` の分岐を持たないこと)・親をまたぐ解決順が親の順・その中で
   子の順になること・同じ親を2グループへ書いた要求が `TOOL_INVALID_ARGUMENT` になること・
   一覧の各項目が親の識別子と親内位置を持ち、ページングが親の境界の途中から始まっても所属親を
   復元できること・ページングが平坦化後の集合へ適用されること・親の後方が範囲外なら1件も
   変更されないこと・子の `handles` 経路で親を指定すると `TOOL_INVALID_ARGUMENT` になること・
   走査段階の列による親の解決(単数段の値が無い要素が親の列に含まれないこと・親の位置が
   走査後の列の位置であること))・
   **入れ子リストへの追加**(`assignments` の親と生成物のグループ・親軸の混在が
   `TOOL_INVALID_ARGUMENT` になること・子配列がコレクション区分どおりの判別和であること・
   グループをまたいでハンドルが重複する要求で1件も追加されないこと・1件でも不正なら1件も
   追加されないこと・応答の `indices` がグループ順とその中の子の順に対応すること・子件数の
   合計に上限が掛かること・Undoが1回であること)・
   **ハンドル発行の集合化**(`constructor`・`factory` は公開引数0件なら `count` のみを取り、
   1件以上なら `args` と `count` の組と `argsList` が相互排他であること、`receiverBound` は
   レシーバー1件につき1個で `count` を持たず応答がレシーバーと
   同じ長さ・同じ順序であること・公開引数が0件の発行が `args`/`argsList` を
   受け取らないこと(自動注入される引数だけを持つシグネチャが公開引数0件と判定されること)・応答が生成順のハンドル配列で長さが生成数と一致すること・
   引数の1件が不正なら1件も生成されないこと・生成途中の
   例外で生成済みハンドルが解放・失効し台帳に残らないこと・件数上限が分岐ごとの逆算値
   (公開引数0件の `count` のみ、および `args` と `count` の組は応答側のみ、`argsList` は
   要求側と応答側の小さい方、`receiverBound` はレシーバー配列と応答側と `argsList` の最小)と
   一致し、スキーマの `count` の `maximum` と各配列の `maxItems` がその値で生成されている
   こと・
   ディスパッチの呼び出し記録が生成数によらず1件であること)・
   **ハンドル解放の集合化**(1件以上・重複なしの検査・1件でも不正なら何も解放されないこと・
   親と子を同時指定しても各ハンドルの解放が1回だけであること・応答が台帳上で失効させた順で
   あり、SDKの解放が例外になったハンドルもその一覧に含まれること・
   解放に失敗したハンドルが `warnings` に列挙され後続の解放が続行されること)・
   **ハンドル操作型の集合レシーバー**(取得が入力順の配列・更新が共通値と要素ごと値の二択で
   応答が件数・メソッドが共通引数と要素ごと引数の二択・後方要素の不正で1件も適用されないこと・
   ディスパッチの呼び出し記録が対象件数によらず1件であること)・
   **追加・削除の応答**(`added` と `indices` が追加した順で返り、`indices` を使った参照の更新が
   成立すること・削除が `removed` に取り除いた件数を返すこと)・
   **VMEの対象集合**(`targets` の1件以上・重複なし・適用順・全件事前検証で後方の対象が不正なら
   1件も適用されないこと・応答が適用件数であること)・
   **参照コレクション**(`refIndices` の空配列が `TOOL_INVALID_ARGUMENT` になること・後方に
   範囲外の参照値を含む要求で1件も追加されないこと・複数の参照値が要求に並べた順で末尾へ
   追加されること・親がハンドルの経路でも参照先の範囲が現在のPMX状態のリストで検証される
   こと・削除が位置で解決した要素を取り除き残りの並びが保たれること)・
   **複数項目の同時更新**(2項目以上を1つの更新オブジェクトへ入れたとき全項目が反映され、
   省略した項目が変更されないこと。操作対象型のCRUD更新ツールとコネクタのプロパティ集約
   更新ツールの双方で行う。1項目ずつの検査では先頭の1項目しか適用しない実装を検出できない
   ため、この組合せ検査を別に置く)・
   **要素型の公開メソッドの集合呼び出し**(`args`/`argsList` の相互排他と長さ検査・
   `in`/`ref` の引数を持たないメソッドがどちらも受け取らないこと・`return`/`out`/`ref` を
   持つメソッドの応答が対象集合と同じ長さ・同じ順序であること・いずれも持たないメソッドの
   応答が `invoked` であること・対象件数の上限と超過時のエラー・**編集分類と経路の組合せから
   導出した `Update` 回数**(複製編集型の行の複製編集型経路だけが1回で、読み取り分類の行と
   直接変更型の経路は0回)・**レシーバーの実行時型が宣言型と適合しない要素を混ぜた対象集合が
   `TOOL_NOT_APPLICABLE` になり1件も適用されないこと**・**ディスパッチの呼び出し記録が
   対象件数によらず1件で、その正規化値が応答として組み立てた値と一致すること**)・
   **`fields`**(指定項目だけが応答に現れること・読み取り不能な項目名の
   指定が `TOOL_INVALID_ARGUMENT` になること)・`_meta["anthropic/maxResultSizeChars"]` が全ツール定義に予算と同値で付くこと・
   デバッグフック未設定時は無効化の指定があっても宣言が維持されること(予算の環境変数の
   構文・範囲・不正時の挙動・ホストとの一致は、ホスト基盤とブリッジ基盤のテストが担う)・
   値表現の相互変換・画像の変換と `maxLongSide` による縮小(境界値と `warnings` の内容)・
   ハンドルの発行/解放/失効/二重解放/型不一致/親子の逆依存順解放/
   解放例外時の失効記録と後続の解放処理の続行/
   切断検出とホスト基盤のDispose停止順序から全ハンドル自動解放が起動されること/タイムアウト
   破棄時に遅延生成ハンドルが解放・失効されること・
   イベントキューの上限と破棄計上・pollの
   seq連番/dropped リセット/サイズ内で分割しない排出/`sourceHandle` が購読リスナのハンドルIDに
   なること・suppressUndoの対での実行(例外時のUnlock・
   Unlock失敗時の残留記録と回収を含む)・各エラーコード・エンベロープ変換)。シグネチャ行の機械生成・照合スクリプト
   (プロセス内リフレクション列挙。置き場所は上記の分けかたに従う)と型役割表
   `docs/specs/pmx-editor-mcp-type-roles.json`(役割対象の型の役割・要素名詞・CRUDツール名・
   参照コレクションの参照先の要素型とリストの所在・親の所有経路
   (走査段階の列)・ハンドル発行能力の `issuanceKind` と判定根拠)と、
   公開プロパティごとの日本語名の正本 `docs/specs/pmx-editor-mcp-property-names.json` と
   その規則を定める `docs/specs/pmx-editor-mcp-property-names.md` と、
   特別規則の表 `docs/specs/pmx-editor-mcp-common-assignments.json`(共通契約割当の期待値。
   シグネチャ単位の識別子・`assignment` の分岐と対象名・`slotBinding` の期待スロット)を
   整備し、対応表・スキーマ正本のJSON構造定義を共通契約仕様書に記載する。**ベースライン正本
   `docs/specs/pmx-editor-mcp-excluded-baseline.json` を、除外一覧の整備に着手する前の台帳の
   非対応記載から作成する**(以後はこのファイルの内容が正本で、台帳の備考は資格(i)の根拠に
   ならない)。照合スクリプトの
   型列挙は**入れ子の公開型を含める**(`IsPublic` だけで絞ると入れ子公開型は `IsPublic` が
   false のため落ちる。台帳の CAP-467〜472 は入れ子の公開データ型であり、これを落とすと
   過不足ゼロのゲートが通ってしまう。列挙の起点は台帳生成時と同じ範囲とし、台帳の全CAPが
   列挙結果に現れることをゲートの前提条件として検査する。**逆向きの照合も併せて行う**——
   列挙で得た全公開型と全公開シグネチャが、台帳の母集合か明示的な対象外一覧のどちらかに現れる
   ことを検査し、どちらにも現れないものを不合格とする。台帳から列挙への包含だけでは、SDKに実在
   するが台帳が拾っていないシグネチャ——`PmxLib` の低水準直列化APIのような——を検出できない)。
   除外対象
   (ベースライン正本が凍結した組と、本計画確定のカテゴリ: PMD系(代替あり)・
   Cプラグイン引数・デリゲート系・コンストラクタ重複・PMDモデル系。Stream系は全シグネチャを洗い出して
   ベースライン正本への包含を検査し、含まれないものが在れば停止する——資格(ii)の項目としては
   出力しない)を一次資料から洗い出して
   除外一覧ファイル `docs/specs/pmx-editor-mcp-excluded-signatures.json` を作成し、台帳の
   該当CAP備考へ非対応シグネチャの内容と件数を追記する。台帳が行を作らない型とシグネチャは
   `docs/specs/pmx-editor-mcp-ledger-out-of-scope.json` へ書き出し、型単位とシグネチャ単位の
   逆向きの照合を通す。
   台帳の契約注記(利用非推奨等)を洗い出し、該当備考へ固定接頭辞「契約注記:」を付けて
   構造化する。要素型ごとに
   「1件あたりの想定文字数」を上記の算出規則で機械算出して `limit` の既定/最大を逆算し、
   スキーマ正本へ書き出す。ツール説明文の生成規則(先頭に対象・動作・出所の型名/契約注記の
   転記)と2,000バイト上限・出所修飾のゲートを実装する。提供対象全シグネチャの型列挙に
   よる値表現の網羅ゲートを実行して、共通契約仕様書を作成する(接続初期化の経路一覧の
   機械可読での確定を含む)。**移送時の置換**: 共通契約仕様書・ツール仕様書へ移送する本文
   から、本計画書に依存する参照を置換して残さない。**置換の趣旨**は、移送元の工程順・
   前提の節・自己参照・他の一時文書への依存を、恒久成果物への参照か自己完結した記述へ
   置き換えることである。以下に並ぶ鉤括弧付きの語は、その依存を機械検出するための**検査
   データとしての字形**であり、依存先そのものを指す参照ではない。対象と置換先は次のとおり。
   - 「前提N」→ 確かめた事実そのものの記述。
   - **計画書番号を含む参照**(番号に助詞を続けた形) → 移送先の恒久文書(IPC仕様書・
     アーキテクチャ仕様書)の該当契約への参照。
   - 「ステップN」「実装ステップ」→ 到達状態の記述(いつ行うかでなく、何が成り立っているか)。
   - 「本計画」「本計画書」→ 移送先の恒久文書を指す語、または自己完結した記述。
   - **本計画書の節見出しによる参照**(「下記『…』」「上記『…』」)のうち、**移送対象外の節を
     指すもの** → 参照先の内容そのものの記述、または移送先の恒久文書の該当箇所への参照。
     移送対象の節どうしの参照は移送後も成立するので置換しない。
   置換先を持たない記述——工程そのものが前提の規則(除外一覧のベースライン取得等)と、
   **検証・検査の進め方の記述**(どの検査でどう判定するか)——は**移送せず**、移送先には
   結果だけを書く(恒久仕様書は到達状態を規定し、検証の過程を書かない)。
   移送後の本文に、「前提」+数字・「ステップ」+数字・「実装ステップ」・「本計画」・
   計画書番号の参照、および移送対象外の節を指す鉤括弧付きの節見出し参照が残っていないことを、
   移送の完了条件として検査する(検査文字列を数字付きに限るのは、「前提」「ステップ」の
   普通名詞としての用法を誤検出しないため)。
   この置換は群ごとのツール仕様書追記にも同じく適用する。照合スクリプトの実行
   コマンドと合格条件(過不足ゼロ・未割当ゼロ・分類の未分類/多重分類ゼロ・複製編集型の
   更新指定欠落ゼロ・危険操作フラグの三者一致・型役割の未割当ゼロ・群内重複ゼロ・
   写像の粒度の検査(集合の対象指定・親の集合指定・入れ子への追加のグループ指定・
   ハンドル操作型の集合レシーバー・ハンドル発行と解放の集合化・VMEの対象集合・
   単数取得ツールとプロパティ単位の更新ツールの不在・
   コネクタのプロパティ集約)の合格・
   規則適合検査とスキーマ対応検査の合格・除外一覧と台帳の件数一致・台帳の全CAPが
   型列挙結果に現れること・**型列挙の全公開型と全公開シグネチャが台帳の母集合か明示的な
   対象外一覧のどちらかに現れること(逆向きの照合)**・説明文の2,000バイト上限と出所修飾の検査の合格・
   型別 `limit` の逆算値がスキーマ正本と一致すること・**`rowKind` の未分類・多重分類ゼロと
   種別ごとの必須フィールドの充足・`postcondition` を持つ行での欠落ゼロと構造適合・
   直接ディスパッチ行の全行にディスパッチの呼び出し記録があること・`postcondition` の判定が
   それを持つ全行で合格であること**)を
   検証手順書へ登録する。
   **自動E2E検査の基盤**を整備する: 能力対応表とスキーマ正本から検査項目を機械生成する
   生成器と、起動中の実機エディタへパイプ経由で実行して行キー・操作経路キー・接続経路キーごとの合否を出力する
   実行器 `scripts/e2e-tools.mjs`(`--soak` の実行経路を含む)、型別既定サンプル値表
   `docs/specs/pmx-editor-mcp-sample-values.json`、サンプルファイル表
   `docs/specs/pmx-editor-mcp-sample-files.json`、**変化の基準結果**
   `docs/specs/pmx-editor-mcp-change-baseline.json`(`--baseline` の実行経路で機械生成し、
   `anyChanged` の判定に使う。生成と再生成の手順を検証手順書へ登録する)、想定質問文の記録
   `docs/specs/pmx-editor-mcp-search-probes.json`、および手動確認一覧
   `docs/specs/pmx-editor-mcp-manual-checks.json`(空で作成し、**種別**・類型の閉集合・
   手順・期待結果・実施先を全項目の必須とし、種別「行」には能力行キーと操作経路キー、
   種別「契約」には対象の契約名と設計値の所在を、種別ごとの必須としてスキーマで強制する)。生成規則は
   「行単位の検査は自動実行する」の各項目とし、手動確認一覧の類型の機械述語も同じ照合
   スクリプトで検査する。共通契約の整備の時点では、共通契約に属する行(危険操作の `confirm`
   検査・ハンドルの型役割・ページング境界)で生成器と実行器が動くことを確認する。あわせて、
   宣言あり・なしのA/B対照で宣言が効いていること(前提6の未検証)を、
   パイプ経路では観測できないため `claude -p` のヘッドレス実行で確認する。**確認の具体**:
   **同一の応答に対する宣言あり・なしのA/B対照**で判定する。トークン数と文字数の換算は
   一次資料が定めておらず(保証されているのは既定が25,000「トークン」であることと注釈値が
   「文字」であることだけ)、換算の仮定を置くと検査が成立しないためである。
   - fixture: 一覧応答を **400,000文字**にする(空モデルへ要素を追加し、件数で近づけたうえで
     末尾の文字列項目の長さで調整する。CLIへ投入する前に実際の文字数を測る)。注釈のハード
     上限500,000文字の範囲内である。**この値が25,000トークンを超えることは換算では主張
     しない**——超えたかどうかは下記の判定そのものが示す。
   - 検査専用の設定を2つ用意する。どちらも**環境変数でホストとブリッジへ与え、既定値は
     本番の値**とする(検査以外では効かない)。
     - `PMX_EDITOR_MCP_BUDGET_CHARS`: 応答サイズ予算の文字数(名前・既定 100,000・構文・
       有効範囲・不正時の挙動は、ホスト基盤とブリッジ基盤の設計値として確定済み)。
       検査時は **420,000** を与える。ホストの `value`
       ゲートは常に「この値 − 警告枠2,000文字」で算出する(検査時は418,000文字となり
       400,000文字は通る)。ブリッジが宣言する `anthropic/maxResultSizeChars` の値も同じ
       設定から取る。**環境変数の名前・既定値・構文・有効範囲・不正時の挙動、および
       handshake の `budgetChars` によるホストとの一致の担保は、ホスト基盤とブリッジ基盤の
       設計値として確定済みである**(ここでは再定義しない)。本計画が定めるのは、この予算値を
       どう使うか——警告枠2,000文字と一覧応答の枠1,000文字の内訳、型別 `limit` の逆算、
       100,000文字を選んだ理由——と、検査時の一時的な引き上げだけである。
     - `PMX_EDITOR_MCP_DECLARE_META`: ブリッジが `_meta` を付与するかどうか。既定 1(付与
       する)。A/B対照の「宣言なし」側でのみ 0 を与える。**この設定は
       `PMX_EDITOR_MCP_DEBUG_HOOKS=1` のときだけ有効**とし、未設定時は 0 を与えても付与を
       止めない(テスト用の投入経路と同じ入口に集約する)。**これは外部からの無効化を
       禁止するものではない**——どちらも環境変数なので、利用者が両方を同時に設定すれば
       宣言は止まる。デバッグフックを明示的に有効にした状態は契約の外であり、その状態で
       ツールの `_meta` が付かないことをツール仕様書へ明記する。二重の設定を要求するのは、
       通常の配布・運用で意図せず契約が破れることを防ぐためである。
   - **外部設定の統制**: 宣言なし側の挙動は `MAX_MCP_OUTPUT_TOKENS` に左右されるため、
     A/Bの**両側でこの環境変数を 25000 に固定して実行する**(利用環境に大きな値が設定されて
     いると、結果の差がfixtureや宣言ではなく外部設定に由来してしまう)。
   - 同じ fixture を、宣言あり(`PMX_EDITOR_MCP_DECLARE_META=1`)と宣言なし(同 0)の2通りで、
     上記の検索検査と同じ実行形式(`--output-format stream-json --verbose`・モデル固定・
     `--allowedTools`)で走らせる。判定は次の4通りで閉じる。
     - **宣言なし=ファイル参照 / 宣言あり=本文** → **合格**(宣言が効いている)。
     - 両方とも本文 → 不合格。fixture が既定のしきい値に届いていないか、宣言が無意味かの
       いずれか。fixture はハード上限500,000文字までしか大きくできないので、これ以上の
       切り分けは計画内ではできない。
     - 両方ともファイル参照 → 不合格。しきい値が宣言値まで引き上がっていない。
     - 宣言なし=本文 / 宣言あり=ファイル参照 → 不合格。想定と逆で、宣言が有害か観測系の
       誤りかのいずれか。
     **合格の1通り以外はすべて不合格**とし、4通りのどれであったかと両側の応答を添えて
     設計変更としてユーザーへ報告する。
   また、実行コマンドと合格条件(全行が自動E2E合格・xUnitの全行網羅・手動確認一覧の種別「行」への
   登録のいずれかであること、手動登録は述語を満たし実施先が特定されていること・イベントの
   リスナ型ごとの代表イベントの発火確認の登録がリスナ型の集合と完全一致すること・未分類ゼロ・不合格ゼロ・
   **「能力行キーと操作経路キーの組」の全件と「接続経路キー」の全件**の網羅・サンプル値表とサンプルファイル表の未割当ゼロ)も検証手順書へ
   登録する。あわせて**MCP経路の2検査**——群単位の検索での発見可能性(実行コマンド・
   想定質問文の所在・用意した件数の8割(切り上げ)以上という合格条件・3回で不合格ならユーザーへ
   報告)と、
   宣言あり・なしのA/B対照(fixtureの作り方・検査専用の環境変数3つ・4通りの判定と
   合格の1通り)——も、実行コマンドと合格条件つきで検証手順書へ登録する(進捗ドキュメントは実行結果の記録先であり、再実行可能な手順の正本は検証手順書と
   する)。
   **ホスト基盤への追加要件**を2件立て、**本ステップの実装対象に含めた上で**アーキテクチャ
   仕様書へ追記する: (1)IPERunArgsの常駐保持、(2)ホストのログの記録内容へ「Cプラグイン
   コネクタの取得」「Cプラグインコネクタの破棄」「ハンドルの解放」を加えること(終了時の後始末を機械判定するために要る。
   実行器はエディタのプロセスIDから `%TEMP%\pmx-editor-mcp-host-<プロセスID>.log` を
   組み立ててログを特定する)。文書への追記だけでは判定に必要な出力が存在しないため、
   どちらもホスト側の実装を伴う。
   一次資料(SDKドキュメントXML)で確認済みの引数の形——CAP-015 `OpenPMXFile` はPMXファイル
   パスを取り真偽値を返す、CAP-013 `SaveMaterialName` は保存材質名ファイルパスを取る、
   CAP-166 `SaveViewSetting` は設定ファイルパスを取る——を台帳の該当備考へ記す(引数の形は
   確定済みで、実装時に調べ直す工程は持たない)。
   危険操作の3基準に該当する能力(既知の該当:
   CAP-006/007/012/013/014/016/166/339/445)の台帳備考に「危険操作」と該当シグネチャを
   追記する。
2. モデル群を「実装の進め方」の縦切りで実装する(複製編集型の状態契約——Update成功後の
   表示更新失敗の警告・Update前例外の未変更保証・Update例外の結果不明報告——のテストと
   自動E2E検査を含む)。Undo粒度の検証はエディタのUndo操作を要するため、手動確認一覧へ
   **種別「契約」・(a)の類型**で登録し、実施先を検証手順書の受入シナリオ「編集と取り消し」
   とする(実施と結果の記録は受入検証が行う)。
3. セッション群を同様に実装する(危険操作の confirm 検査・直接変更型の状態契約を含む)。
4. ビュー群を同様に実装する(ビューポート画像のPNG/Base64返却・イベント購読とpoll・
   UIモデルとテキストコントロールのハンドル運用を含む)。手動確認一覧への登録は次の3種を
   それぞれ独立に行う。
   - **イベント発火(計2件)**: `IPXViewEventListener` と `IPXUIModelEventListener` の
     リスナ型ごとに1件。内容は「実機の操作でイベントが発火し、`sourceHandle` が購読した
     リスナのハンドルIDと一致すること」。種別「行」・類型(a)。実施先は受入シナリオ
     「イベント」。
   - **画像の目視(計3件)**: 画像を返すツールごとに1件(CAP-120・CAP-168・CAP-239)。
     内容は「画像に意図した視点の内容が描かれていること」。種別「行」・類型(b)。実施先は
     受入シナリオ「ビュー」。
   - **画像警告の契約(計1件)**: 「既定の `maxLongSide` の画像応答がクライアント側で警告に
     なるか」。種別「契約」・類型(b)。実施先は受入シナリオ「ビュー」。`maxLongSide` による
   縮小時の `warnings` は文字列の一致で判定できるため手動へ回さず、自動E2E検査で確認する。
5. 変形・モーション群を同様に実装する(ビルダのハンドル運用・VMEテンプレートの列挙・確定を
   含む。デリゲート系シグネチャの除外はステップ1の除外一覧に基づく)。
6. 全群完了後、機械生成した提供対象シグネチャ集合と能力対応表の全行を照合スクリプトで
   突き合わせ、過不足ゼロ・未割当ゼロを全群横断で確認する(この確認は受入検証
   (実装計画書06)でも再実施する)。
7. 全検査を実行して成立を確認し、進捗ドキュメントへ実行コマンドと合否を記録する。
   完了後、本計画書を削除候補として報告する(削除の実行はユーザーの指示があった
   ときのみ)。

## 完了条件

- 台帳の提供全能力(シグネチャ単位まで展開。台帳備考で非対応と明記したシグネチャを除く)が
  能力対応表に割り当てられている。
- 能力対応表の全行が、実機のPMXエディタに対する**自動E2E検査で合格**しているか、
  **イベント行の payload 変換のようにxUnitテストで全行網羅されている**か、
  **手動確認一覧に種別「行」として登録され、類型の機械述語を満たし、実施先の見出しが
  特定されている**かのいずれかである(未分類ゼロ・不合格ゼロ。網羅は
  **「能力行キーと操作経路キーの組」の全件と「接続経路キー」の全件**で機械検査する——
  能力行キーだけを見ると、同じ行の親インデックス経路にだけ記録があって親ハンドル経路が
  未検査でも通ってしまう)。類型は(a)UI操作の発火/(b)目視での判断・観察/
  (c)自動判定の基準が無い の3つに限る。
- 手動確認一覧の種別「契約」の項目が、実装ステップで確定した閉集合と一致している。
- **手動確認の実施と結果の記録は受入検証が行う**ため、本計画の完了条件は
  登録と実施先の特定までとする。
- 群単位の検索での発見可能性が全群で合格している。
- MCP経路(Claude Code経由)が群ごとの代表1ツールで確認済みである。
- 共通契約仕様書(値表現の網羅対応表を含む)とツール仕様書(能力対応表・全ツールの
  スキーマ・VMEテンプレートの提供範囲を含む)が作成され、本計画書にしか無い恒久情報が
  残っていない。
- 全検査が成立している。
